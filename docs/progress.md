# 进度日志

> 按时间倒序记录「做了什么、发现了什么、卡在哪」。这是项目的"流水账",越勤快越值钱。
> 每条建议格式:日期 + 做了什么 + 下一步。

---

## 2026-09-20

- 发布前最终复核已补修旧确认可能重建已删目录/写入新位置的问题，隔离红绿与 11 项相关测试通过；正文标记状态同步重置。已完成全量发行检查并公开 v0.2.6 Build27，签名更新源与匿名下载核对通过；本机同版、资料保持、再次检查显示已是最新，临时旧 App 清理。详情窗口视觉仍因 CUA 断线未完成，边界如实记录。发行证据见 [发布记录](reviews/2026-09-20-release-026.md)。

- 本机更新至 Build 26：受管内容变化补齐比较与明确替换出口，橙色改为内容待确认；实机发现 ai-news WorkBuddy 仅 .DS_Store 不同，安装状态比较也已排除普通 Finder 文件。最终实机绿色已安装、正常确认/取消，6 文件内容和时间均保持。631 Swift 通过/1 跳过，28 Python 通过，release/隐私检查通过；临时旧包清理，未公开发布。见 [修复记录](reviews/2026-09-20-managed-replacement.md)。

- 修复已安装内容与中央当前版本一致但历史记录落后的错误拦截：先校正已确认一致的受管记录，不复制文件。红绿复现、完整回归 629 Swift 通过/1 跳过和 28 Python 通过，打包/隐私检查通过。本机 Build 24 真实显示 github-readme-design → GPT 已安装，正文/权限/修改时间不变，临时旧 App 已清理，未公开发布。见 [修复记录](reviews/2026-09-20-matching-installation.md)。

- 更新正文对比已接入并本机安装 0.2.5 Build 23：连续双栏、删除线/黄色文字背景、安装确认右上角只读详情。627 项 Swift 实际通过、1 项公网跳过，Python 28 项通过，release/隐私检查通过。真实启动与资料核对通过；点击更新时 CUA 连接中断，重连失败，两个入口的最终视觉与关闭返回验收仍待补。未公开发布。见 [接入记录](reviews/2026-09-20-diff-integration.md)。

- 🧪 应用户要求完成更新对比原生成本实测：105行独立实验，无新依赖，删除线/黄色背景/连续文本/关闭返回通过；300行差异约2–3ms，3000行全改约247–254ms，全文两列排版分别约27–29ms与210–220ms。结论整体小到中等接入，长文需后台计算与避免重复排版；仅实验未改产品或安装。见[实测报告](reviews/2026-09-20-diff-feasibility.md)。

- 🎨 按更新对比第二轮反馈调整预览：左右改为连续文本窗口，移除逐行格子及空白对齐，删除线/浅黄仅标记变化文字；安装确认查看更新移到右上角。浏览器视觉及进入/关闭返回验证通过，截图和方案同步，仅预览未改产品。

- 🎨 完成更新内容对比[方案与预览](../design/ui/update-diff-20260920/README.md)：旧行删除线、新行浅黄背景，安装确认内增加查看更新并由当前弹窗打开详情，关闭返回原确认。区分来源更新与应用副本替换的比较对象。可点击样例经浏览器验证进入/关闭返回，尚未改产品代码或安装；待用户确认方案。

- ✅ 修复管理应用中添加/编辑窗口排队：由管理窗口直接呈现，添加按钮固定在顶部完成左侧。原生复现旧问题并验证新版添加、目录选择/取消、取消返回、滚动固定与完成关闭；全量检查通过，本机已原位置安装0.2.5 Build22，资料内容保持，临时旧App清理。上一项Finder误报同时生效，未公开发布。见[交互修复与实机预览](reviews/2026-09-20-custom-app-sheet.md)。

## 2026-09-19

- ✅ 修复本地开发源把普通 `.DS_Store` 算入更新：打包、顶层选择与开发变化比较统一排除，保留真实隐藏模板和默认完整校验。16项来源回归、整体检查、Build21构建与包检查通过；真实 vibe-project-foundation 只读检查已返回 current，27个源文件保持。Mac锁屏阻止本机替换与前台验证，已请求解锁；正式App仍Build20，未发布。见[修复记录](reviews/2026-09-19-finder-metadata.md)。

## 2026-09-17

- ✅ 已按用户要求发布 v0.2.5 Build20，GitHub Latest 与签名更新源已上线，五个附件经匿名下载核对一致。本机通过真实公开源由 Build19 在线升级到20，再次检查显示“已是最新版本”；5份 Skill 保持、175个资料文件无新增缺失，未创建完整备份。修复了服务不可用误提示检查网络，全量发行检查通过。见[发布与在线验收](reviews/2026-09-17-release-025.md)。

- ✅ 用户确认后修复最终复核两项P2：后台新版工具栏提醒、稍后更新结束检查会话。真实Sparkle三次连续检查及原生点击通过，全量发行检查通过。本机已更新0.2.5 Build19，175个资料文件在替换期间保持，临时旧App清理；发行产物已重生成但未公开发布。见[补修与验证](reviews/2026-09-17-update-review-fixes.md)。

- 🔎 发布前复核 `6dd60f4`：发现2项P2，后台新版缺少可见提醒、发现新版后没有“稍后”出口导致检查会话持续等待。真实Sparkle后台探针已复现；27项相关测试、独立公钥签名验证和包态一致性通过。结论暂缓发布，本轮未改产品/重装/发布。见[最终复核](reviews/2026-09-17-about-updates-final-review.md)。

- ✅ 按用户确认完成 Spec v2.61：原生关于页、Sparkle签名应用内更新、删除提示6秒隐藏和历史恢复入口。普通App更新不复制用户资料，不额外建立七天备份。全量测试/包态检查通过；实际隔离升级同路径重启、签名拒绝、较新版本不降级及原生删除恢复通过。本机原位置更新至0.2.5(18)，资料保持；本地发行产物已准备，尚未公开发布。详见[验收记录](reviews/2026-09-17-about-updates.md)和[技术选择](decisions/0023-application-updates-with-sparkle.md)。以下三条为此前设计阶段记录。

- 🎨 按用户要求简化应用内更新方案：普通 App 替换不复制应用外的 Skill 资料，不另建保留七天的旧 App 与全量资料备份；只使用替换所需的临时空间，成功后清理，失败时保留可用旧应用。预览及说明已同步，未执行任何真实备份或清理。此调整只针对新应用内更新设计；未来确有资料格式变化时再针对受影响文件处理。

- 🎨 按用户浏览器批注移除关于页更新卡片下方说明及空白占位。只读确认用户资料位于 App 外的 Application Support/SkillBox；设计说明补充原位置替换、禁止重复安装、资料与安装关系保留及升级验证合同。当前仍为预览，真实更新器尚未接入。

- 🎨 完成关于与应用内更新调研及[可点击设计预览](../design/ui/about-updates-20260917/preview.html)，参考 Raycast、Rectangle Pro、GitHub Desktop 官方说明。定位删除提示常驻原因：独立删除提示没有超时，启动还会重新展示历史删除；后续需分离提示可见状态与恢复记录，并补齐历史恢复出口。浏览器预览的更新/取消/重试、删除提示超时/悬停/关闭/撤销、资源与小窗口检查通过。OpenDesign daemon 不可连接，使用 HTML + PNG 兜底。根据本项目视觉确认规则等待设计确认，尚未改产品代码、更新 Spec、引入依赖、替换本机 App 或发布；设计尚待确认，暂不提交。接入条件、风险与验证计划见[设计说明](../design/ui/about-updates-20260917/README.md)。

- ✅ 修复手动删除修改过的安装副本后仍显示冲突：刷新和启动确认目录缺失后解除旧安装记录，不自动重装，异常目录和未完成恢复继续保护。636 项测试通过、1 项既有公网跳过；构建、签名、隐私检查通过。本机已备份更新至 0.2.4 Build 17，真实 WorkBuddy 格恢复蓝色加号并通过再次刷新。其余关系、分类和 110 个中央文件一致。未公开发布。见 [删除后状态修复](reviews/2026-09-17-deleted-installation-fix.md)。

- ✅ 用户确认后补修复核发现的中文目录 P2：保留中文括号、顿号及请求式目录名，只分离末尾完整请求。失败复现后相关测试通过，完整 633 项实际通过、1 项既有公网跳过；构建/签名/隐私检查通过。本机已备份更新至 0.2.4 Build 16，原句真实窗口仍准确找到 storage-analyzer，121 个既有内容与关系文件一致。未公开发布。见 [中文目录补修](reviews/2026-09-17-chinese-path-fix.md)。

- ✅ 按用户“做好修复”完成 GitHub 子目录添加与发现解析两项修复。添加/更新仅展开所选子目录，保留链接、路径与资源保护；网址紧接中文请求不再吞入路径。失败复现后验证通过，完整与补充测试合计 632 项实际通过、1 项既有公网跳过，Release/签名/隐私检查通过。
- ✅ 本机已备份更新至 0.2.4 Build 15。真实窗口用原链接成功添加 storage-analyzer，并在原失败会话重发原句后准确找到 1 个结果。原 Skill 内容、安装关系、分类和排序保留，新增 SkillBox 内容与 Codex 全局 7 个文件一致。未公开发布。见 [修复与验收记录](reviews/2026-09-17-github-subdirectory-fix.md)。

## 2026-09-14

- ✅ 按用户截图将 README 顶部标题、图标、简介、下载入口和系统要求整体居中；居中区域在正文前关闭，下方正文及链接内容保持。

- ✅ 用户确认采用并推送 github-readme-draft 优化稿，已替换 [仓库 README](../README.md) 并移除重复草稿。保留安装限制和验证范围，补清首次使用、来源/保存版本/安装副本关系、更新恢复、备份及 MIT 许可；开发说明折叠。生成器原始输出与检查回执仍在 `scratch/readme-draft-20260914/`。本次仅改文档，不改产品、标签或发行附件。

- ✅ 用户授权后已推送并公开发布 v0.2.4 Build 14，GitHub Latest 已更新、旧版保留。完整发行检查和 628 项测试通过、1 项公网跳过，草稿及匿名公开下载三个附件一致；公开 DMG 版本、程序摘要及签名通过。发行标签固定 `b2fb855`，本机保持已验收 Build 13，发布缓存已再次可恢复清理。见 [v0.2.4 发布记录](reviews/2026-09-14-release-024.md)。

- ✅ 按用户要求完成项目文件盘点、评估和可恢复清理：135 项、4,965 个文件约 1.42 GB 已移入废纸篓，含 4 个测试 App、旧实验副本及可再生构建缓存。正式源码/测试、设计原件、当前 Build 13、公开安装包、有效 Build 12 回退备份保留，移动前后内容一致；正式 App 已重开正常。项目约 164 MiB，废纸篓未清空。精确清单与恢复说明见 [项目清理结果](reviews/2026-09-14-project-cleanup.md)。

- ✅ 用户明确以代码测试及流程运行作为本轮验收依据，实际使用有问题再反馈；剩余隔离窗口人工点击不再作为阻断项。按此标准，本轮修复验收通过并结束，不再追加评审。
- ✅ 最后一遍完整 `app/Scripts/test-all.sh` 退出 0：Swift 600 项实际通过、1 项公网测试跳过，Python 28 项通过，共 628 项通过。日志 `scratch/2026-09-14-final-acceptance.log`。本机 0.2.3 Build 13 程序摘要仍与已验证修复版本一致。未改产品代码、未重新安装或旋转备份、未公开发布。测试应用已退出；未实际点击的隔离场景保留事实记录，不再列为待办。

## 2026-09-13

- ✅ 按用户确认完成限定完整检查并集中修复 Spec v2.60：更新内容/来源/结束记录和失败恢复统一处理；App 部分清理通过可验证清单接续，保持尚存内容及链接保护。有效失败测试后，相关 141 项、完整 Swift 600 项实际通过（1 项公网跳过）、Python 28 项通过；冻结后的独立复核通过。
- ✅ 本机已安装并启动 0.2.3 Build 13，程序 SHA `954d2fa76de65036135402ddf9dfd29c48fb617726852c449b8f465db2d8fac0`，严格签名通过。真实启动和设置中的手动备份检查通过；174 个受保护文件、4 份 Skill、分类及安装关系保持。回退仅留 Build 12 与配套资料一套，9 月 20 日 20:33 到期后于启动/手动检查清理，旧 Build 11 已按规则淘汰。
- 📌 隔离 QA 窗口仍导致 CUA 管道关闭，失败重试/取消/恢复的本轮真实按钮验收未完成，保留明确限制；正式 App 可以连接且已完成安装核对。未追加评审或修改冻结产品代码，未公开发布。见 [限定修复与验证](reviews/2026-09-13-bounded-update-recovery.md)。

- 🔎 按用户最后一轮子 Agent 评审要求，由三个新子 Agent 独立检查 `cdc59f7`，主 Agent 复跑确认：1 项 P1（更新失败后回拷也失败，错误回退状态导致后续删除唯一归档）、1 项 P2（App 部分删除失败后无法重试），另有非阻断 P3（恢复受阻后历史暂旧）。结论未通过，未修改产品或本机 App。66 项相关 Swift、21 项 Python 仍通过；详见 [Build 12 最终独立评审](reviews/2026-09-13-backup-final-review.md)。

- ✅ 按用户“先确认确实是问题，再修复”完成 Spec v2.59：第二轮六项问题均复核成立并修复。补齐真实恢复按钮、缺失/损坏来源、恢复中途失败补偿与外部修改、删除后登记重试、权限核对和失败后状态刷新；同步旧保留条款。独立复审指出的修复缺口均有失败复现及修复后通过证据。
- ✅ 最终完整 Swift 585 项实际通过、1 项公网跳过，Python 21 项通过；Release、严格签名和隐私检查通过。隔离原生界面实际取消/恢复、正式 App 启动及手动检查通过。174 个受保护文件、4 份 Skill、分类和安装关系保持一致。
- ✅ 本机已更新 0.2.3 Build 12，程序 SHA `e7c5f5075b95d01f71e923e083e46c3373fae43d4cd54e309e34a927967bd101`。App 回退仅留 Build 11 与配套资料一套，9 月 20 日 16:38 到期后于启动/手动检查清理；小时任务仍停用，没有公开发布。详见 [第二轮修复与验收](reviews/2026-09-13-backup-second-fixes.md)。

- 🔎 用户再次要求子 Agent 独立评审，针对 `f65e095` 的第二轮结论未通过：确认 6 项问题（1 项 P1、5 项 P2），覆盖启动恢复使用缺失/损坏备份、中央更新恢复按钮禁用、组合撤销部分执行、App 删除后登记失败无法重试、权限漏检和失败后恢复入口状态过旧。App 登记重试问题为上一轮新增回归，其余为此前漏检的既有流程问题；另发现 Spec 旧保留条款冲突。
- ✅ 本轮重跑 20 项 Swift 备份专项、12 项 Python 专项、1 项中央撤销测试均通过；异常场景另做隔离复现，主 Agent 复跑关键证据。只新增评审文档，产品、本机 App 和真实备份未改动。详见 [第二轮独立评审](reviews/2026-09-13-backup-second-review.md)。

- ✅ 按用户“修复，确保代码质量过关”授权完成 Spec v2.58：独立评审七项缺陷均已修复，覆盖失败救援与上一版名额、跨语言摘要、损坏替代备份保护、时区排序、登记入口、启动补检和维护错误反馈。三个子 Agent 交叉复审无新增具体缺陷；前台补验发现旧提示复用，已一并修正。
- ✅ 最终完整 Swift 560 项实际通过、1 项公网跳过，Python 12 项通过；Release、严格签名与隐私检查通过。本机已更新 0.2.3 Build 11，真实启动和手动检查反馈通过；真实备份隔离副本在到期前/到期/重复检查均符合规则。174 个受保护文件及分类安装关系一致。
- ✅ App 回退仅留 Build 10 与配套资料一套，9 月 20 日 15:25 到期；仍在启动或手动时检查，Codex 小时任务保持停用。未公开发布。最终程序 SHA `9725652bff794560bae3b5326b80b8c366dfddcf5ad4006a8fe1abb1990f3705`，完整证据见 [修复与复审记录](reviews/2026-09-13-backup-fixes.md)。

- 🔎 按用户要求调用三个子 Agent 独立评审备份维护，结论未通过：7 项问题（5 项隔离复现、2 项调用链确认），包括旧失败记录淘汰后来成功更新的上一版、原生/Python 摘要排序不兼容（当前真实备份已只读确认）、最新备份损坏仍删旧完整备份，以及时区、登记入口、启动漏检和错误反馈缺口。既有 9 项 Swift 与 6 项 Python 备份检查重跑仍通过，不能覆盖上述问题。本轮未修产品、替换 App 或清理真实备份；见 [独立评审](reviews/2026-09-13-backup-independent-review.md)。

- ✅ 按用户降低自动化成本的要求完成 Spec v2.57：取消备份每小时循环与 Codex 定时唤醒，改为应用启动时一次检查，并提供“设置 → 存储与记录 → 检查并清理备份”。已登记 App 回退资料也由本机原生检查处理，不使用 AI；新备份成功后仍立即淘汰更旧一份。七天到期后在下一次启动/手动检查删除。
- ✅ 完整 Swift 549 项通过、1 项公网跳过，Python 6 项通过；Release、签名及隐私通过。本机已装 0.2.3 Build 10，真实启动与手动入口反馈通过，174 个受保护文件及分类安装关系一致。Codex 任务已停用，回退资料只留 Build 9 一套；未公开发布。详见 [启动与手动检查验证](reviews/2026-09-13-backup-launch-check.md)。

- ✅ 按用户“一份上一版、满一周自动清理”要求完成 Spec v2.56：Skill 保存历史及每个安装位置只留上一份，满 7 天失效清理；操作成功后、启动和每小时维护。恢复前完整核对备份，保护当前内容与失败救援资料。本机 App 备份也统一登记成一套，已启用每小时安静清理任务。
- ✅ 完整 Swift 检查 545 项通过、1 项跳过，Python 6 项通过；Release、签名、隐私通过。本机已装 0.2.3 Build 9，真实启动清理 9 份旧事务备份，174 个受保护文件及分类安装关系一致；实际界面正确区分可恢复与过期记录，右上角刷新前台补验通过。App 上一版仅保留 Build 8 与配套资料一套，9 月 20 日 13:58 到期；未公开发布。详见 [回退备份验证](reviews/2026-09-13-backup-retention.md)。

- ✅ 按用户“修复”授权完成本地来源检查缺陷修复：恢复始终可见的检查入口，右上角刷新检查本地来源，启动后及每轮间隔 30 秒自动检查，旧一致状态标明上次检查，读取失败可重试；自动检查不替换保存版本或 Agent 副本，迟到结果不能覆盖新来源。
- ✅ 完整检查 539 项实际通过、1 项公网跳过；追加预览取消回归后新增 6 项全部通过。Release 构建、签名与隐私核对通过，本机已可恢复替换为 0.2.3 Build 8。175 个受保护文件及安装、分类关系保持；Vibe 项目地基已在真实界面自动提示更新，一致状态下的检查按钮也已实际点击验证。
- 📌 点击“查看更新”时界面控制连接中断，重连和工具重置未恢复，App 仍运行。差异窗口及右上角刷新的最后前台复核未完成，对应行为测试通过；没有公开发布。详见 [修复与验证记录](reviews/2026-09-13-local-source-refresh.md)。

## 2026-09-08

- ✅ 用户明确允许披露首次安装未验证后发布，v0.2.3 Build 7 已成为 GitHub 最新正式版本，旧版保留。源码标签、DMG、来源清单和校验文件一致；三个附件经草稿与匿名公开下载双重核对，公开 DMG 内程序签名和版本复核通过，主页入口已更新。详见 [实际发布记录](reviews/2026-09-08-release-023.md)。

- ✅ v0.2.3 Build 7 候选打包与检查完成：534 项实际通过、1 项公网跳过，签名／隐私／DMG／来源身份核对通过。本机已备份更新并真实验收，原有 7 份 Skill、分类及安装关系保留。公开最新仍为 v0.2.2，待处理干净账户首次安装这一发布缺口，见 [发布候选检查](reviews/2026-09-08-release-023.md)。

- 🚧 按用户要求准备 v0.2.3 Build 7 发布，汇总已交付的对话、启动安装、整理和图标改进；不改变 Skill 内容筛选规则。另一用户启动弹窗仍按未复现披露，干净账户首次安装验证单独保留，详见 [发布候选](reviews/2026-09-08-release-023.md)。

- ✅ 用户选择收纳盒后完成 v2.54：归类标题与拖动浮层统一使用系统收纳盒图标，保留本地来源文件夹、GitHub 图标及原布局／手势。完整 534 项实际通过、1 项公网跳过，Release／签名／隐私通过；本机真实窗口已核对两类图形区别。
- ✅ 已更新并打开本机 App，程序 SHA `ffef1ccb45902e4123ebdf58de364d2ae2192b41f499e7c5c7ca8baa2fcc4964`。748 个资料文件完整备份，当前内容、分类排序及安装关系保留。详见 [图标交付记录](reviews/2026-09-08-leading-source-icons.md)。归类图标候选已完成确认与实施，公开下载未更新。

- ✅ 用户确认后交付 v2.53 左侧来源图标：替代首字母、移除右侧重复图标，保留大小、行高和拖动方式。完整 534 项实际通过、1 项公网跳过，Release／签名／隐私通过；本机实际窗口核对两种图标、选中态及无障碍名称。程序 SHA `253c7d1eb5eec6f2e2a25ee2058725802801dae91380ea7829c985dc21f99888`，已打开，公开下载未更新。
- ✅ 更新前 748 个资料文件完整备份，当前安装关系与 Skill 内容保持；整理记录数组成员顺序有变化，但按身份核对全部分类和 sortIndex 一致，未恢复旧记录。详见 [左侧来源图标](reviews/2026-09-08-leading-source-icons.md)。
- 🎨 用户指出归类图标与本地来源图标重复，已打开 [归类图标预览](../design/ui/skill-source-leading-20260908/category-preview.html)，用户不采用叠层，现提供收纳盒／标签两种替代，与本地文件夹形成形状区别；仅更换归类标题图形，待用户选择。

- ✅ 用户确认后交付 v2.52 文件夹视觉层级：54 pt 标题、加大图标／名称、浅色底和数量标签，子 Skill 缩进 24 pt。统一显示与拖动行高，定向 22 项、完整 534 项实际通过，另 1 项公网跳过；原生隔离实拖、折叠与展开通过。Release／签名／隐私通过。
- ✅ 本机已更新并置前，程序 SHA `cb2b158588ddc883db2114abe2c44f0b6e33871512cf52c9e8223125bc3bae0a`；用户最新两个文件夹、7 份 Skill、排序及安装关系保留，748 个资料文件完整备份。详见 [文件夹层级交付](reviews/2026-09-08-folder-hierarchy.md)。用户随后提出左侧来源图标，已打开 [对比预览](../design/ui/skill-source-leading-20260908/preview.html)，待视觉答复；未改正式图标位置，未公开发布。

- 🎨 用户认可拖动手感、要求加强文件夹视觉层级。已制作 [外观对比预览](../design/ui/folder-hierarchy-20260908/preview.html) 与 [设计说明](../design/ui/folder-hierarchy-20260908/README.md)：54 pt 文件夹标题、加大图标与名称、浅灰蓝底色、内部 Skill 缩进 24 pt。浏览器已核对原来／调整后切换和折叠、展开；当前仅视觉候选，待用户确认后正式实现。原生源码与已安装 v2.51 保持，实施时须统一显示与拖动几何的行高，保护已认可手感。

- ✅ 按用户明确要求完成 v2.51：文件夹及其内容统一在上方，文件夹互相拖动排序，未分类统一在下方；同步末尾预览边界和未分类落点身份。完整 533 项实际通过、1 项公网跳过；隔离原生窗口实拖四个关键动作通过，Release/签名/隐私通过。
- ✅ 已更新并打开本机 App，程序 SHA `cf33bbcd790248f1d6dd465479b522ee5870355e1f04388939d8a9f6db5e0e5f`。保留用户当前开发文件夹、7 份 Skill、排序与安装关系；旧版与 748 个资料文件完整备份。详见 [文件夹默认置顶](reviews/2026-09-08-folders-first.md)。未公开发布。

- ✅ 用户回复“继续”后完成文件夹原生交互：标题归类、跨组与组内排序、文件夹末尾排序、邻行让位、边缘滚动、取消保护与名称排序；保留三栏和原操作。新增 8 项正式测试，最终 531 项实际通过、1 项公网跳过，Release/签名/隐私通过。隔离原生窗口验证主要落点、文件夹末尾与自动滚动；Esc 并发自动化的按键晚于松手，不计为真实按住拖动的验收。
- ✅ 本机正式 App 已更新并正常打开，SHA `8ba8bb4a3343858c783aff76a6dc4b1b7c6175cc76ef0dbb16a2e4c702142d15`。更新前完整备份 748 个资料文件，更新后核对 7 份 Skill、分类、安装关系和原件保留；排序菜单前台验证后恢复手动顺序。备份 `~/Library/Application Support/SkillBoxBackups/organizer-20260908-103813/`；公开下载包未发布。详见 [原生整理与本机更新](reviews/2026-09-08-organizer-native-fix.md)。另一用户启动弹窗的具体触发条件仍待版本与步骤。

- ✅ 修复启动与单项安装响应：按既有来源合同取消 Agent 目录中未知 Skill 的自动递归发现；单项确认只在界面线程外核对当前副本，结束后不等待其他应用目录扫描；确认窗显示处理中与失败原因。整理记录保存不再触发安装文件比较。
- ✅ 新增三项正式行为测试，完整检查 523 项实际通过、1 项公网用例跳过。Release 构建、严格签名与隐私检查通过。独立原生 QA 在包含 10,010 个无关目录的临时 Claude 安装位置，实际点击安装和卸载；两次弹窗关闭、矩阵与文件状态一致、两条事务成功，中央内容及无关目录保留。正式资料与正式 App 未替换。
- ✅ 候选位于 `scratch/installation-fix-20260908/SkillBox.app`，程序 SHA `352499505226ee9f65d5b117946d6ed3cb5f8c3ef9f3615493ebe80875f89193`，包含前一轮对话修复。原生 QA 使用独立入口和模拟 Skill，不代表另一台 Mac 的首次启动弹窗已经复现。
- ⏳ 文件夹完整拖放优化已完成 [交互预览](../design/ui/organizer-20260908/preview.html) 与 [设计说明](../design/ui/organizer-20260908/README.md)。真实浏览器核对跨文件夹移动与文件夹末尾排序；原生手势整合、行让位和长列表手感待用户确认方向后实施。原始“找到了 Skill，但读取时遇到问题”的弹窗只追到本地来源读取调用，尚不能证明对方机器的启动触发点，需具体版本或复现步骤。

## 2026-09-07

- ✅ 修复只读审查复现的三项对话缺陷：适用 Skill 问句进入查找并重置无关任务范围；切走再切回、停止或重启后保留旧补充，显式继续才恢复；撤回“离线”同时清除目标中的“不联网”等同义要求。保留名称、作者、中文身份别名、推荐条件、三栏和安装恢复机制。新增五项正式测试先失败再通过，最终完整检查 520 项实际通过、1 项公网用例跳过。
- ✅ Release 候选、严格签名与隐私检查通过，独立原生 QA 用模拟来源完成完整中文问句、切换暂停、手动继续与条件撤回验证。候选程序 SHA `4db8bd7a27b6d70ed064190cd7fa84487fd33b9427a35aa66015addb63667cc9`，位于 `scratch/discovery-review-fix-20260907/SkillBox.app`。正式 App、公开附件及正式资料未替换，未调用付费模型。整体语义能力尚未重构，缺口及验证边界见 `docs/reviews/2026-09-07-conversation-bug-fix.md`。

- ✅ 用户确认后执行独立盲审收紧的 19 项清单，约 540.201 MB 编译缓存、可再生成程序和重复下载附件已移入系统废纸篓，未清空。执行前逐项核对指纹与占用，执行后废纸篓内容、结构、权限均一致；项目保留内容、Git、正式 App 和用户资料共 2,618 个条目前后相同，随后仅新增本条记录。项目目录约从 681 MB 降至 142 MB（含核验记录）；磁盘实际释放需另行清空废纸篓。
- ✅ 保留旧排查程序及配套旧源码、公开下载原件、正式 DMG 三件套、打包 App、源码、正式测试和设计资源。正式 App 继续运行，严格签名及保留安装包校验通过。此前独立临时副本模拟清理后的准确测试结果为 515 项实际通过、1 项公网用例跳过；重建程序与正式 App 字节一致。为避免缓存立即再生成，本次实际清理后验证保留文件和应用完整性，未重复编译、安装或发布。清单、恢复位置和完整回执见 `scratch/cleanup-execution-20260907/report.md`；独立审核见 `scratch/cleanup-blind-review-20260907/independent-review.md`。

## 2026-09-06

- ✅ 用户知悉另一台 Mac／干净账户首次安装尚未实测后回复“可以，推进”。按此授权公开 [v0.2.2 Build 6](https://github.com/AidenXu-1/SkillBox/releases/tag/v0.2.2)，现为 GitHub 最新正式版本，旧 Release 保留；首次安装未验证项已写入下载说明，不记为已通过。冻结源码 `5ae3e8c1651bb4837358d8aa66e7756ae5481369` 与标签、来源清单对应，516 项检查通过，程序与本机真实验收内容一致。
- ✅ 三个正式附件均从 GitHub 草稿回读，再从公开链接匿名下载核对，内容与本地候选完全一致。公开 DMG SHA 为 `e1f012abc61614ad6467200b96a6dc6c83f96631d2b028dcbe0fc9ee7905a0ff`，程序 SHA 仍为 `a620076b613ebcc0ae0943e14aff86bb3a6c087842e38ec913a88aafb95a301a`。本次 DMG 从清洁的冻结提交重新生成，封装摘要与先前本机候选不同，程序内容没有变化；当前有效摘要以本条和同版公开来源清单为准。发布后的首页、Spec 与交接说明单独提交，不改变版本标签和安装包。

- 🔎 按用户授权完成 GitHub v0.2.2 最后发布预检：修正 README 构建号、工程约定和交接状态，移除验收报告的本机绝对路径，隐私门禁恢复通过；额外核对未上传历史，保留本地旧 master，公开分支只承接清理后的完整源码。未发现新增运行程序阻断缺陷，干净环境首次安装证据仍待用户反馈，正式发布尚未执行。详见 `docs/reviews/2026-09-06-github-release-review.md`。

- ✅ Spec v2.47 指定对象与连续追问修复已交付正式 App 0.2.2 Build 6。作者自然句、空格名称、中文来源别名、作者与名称同时限定以及连续追问的条件保留均先复现失败再修复。最终 516 项完整检查与同代 DMG 验证通过。正式窗口实测卡兹克两份 → 两份 → 仅其 human-writing；小黑中文原句唯一命中作者原始文件；旧 vibe 完整名称仍唯一命中，模糊需求包含四份指定作品，三栏保持。详情见 `docs/reviews/2026-09-06-named-request-fix.md`。
- ✅ 正式程序 SHA `a620076b613ebcc0ae0943e14aff86bb3a6c087842e38ec913a88aafb95a301a`，DMG SHA `2e10f963ee9fc6c30209a4a01bc4ed44bc1fd2f1e7daf5fb124e6d40b82e85c6`。替换备份位于 `~/Library/Application Support/SkillBoxBackups/20260906-190132/`；保留原用户内容和历史，原 AI 设置逐字节恢复。窗口停在小黑配图成功结果，没有新增付费模型调用。中文别名仍有收录范围，模糊本轮达到原请求上限，不宣称全网查全。旧失败记录不伪造成功；原有多轮未提交改动全部保留，本轮未混合提交或上传发布。

- 🔎 用户再次提供精准查找失败截图，正式记录与当前 Release 核心重放确认：卡兹克缺少一个“的”即丢失作者，human writing 空格形式未识别，两次追问均切断旧任务；小黑配图缺中文叫法解析。本轮无预算耗尽，正式程序与清理前一致，两个识别文件与上次盲审修复前内容也一致，属于此前漏验。受控指定完整名称可以找到 human-writing 和小黑原作者作品，但不等同正式流程已修复；撤回精准查找全面通过的验收结论。本轮仅调查，没有产品源码、正式应用或设置变更；详见 `docs/reviews/2026-09-06-named-request-recheck.md`。

- ✅ 用户确认后，按已盘点清单将 21 个旧安装包、测试程序／缓存和 6 份中间版本备份移入系统废纸篓，清理前占用约 1.68 GB；未清空废纸篓。清理前后 5,701 个保留条目比对一致，源码、正式测试、Git 记录、正式用户数据、当前 App、最终 DMG 和三份保留备份未变；21 项均核对可恢复，正式 App 继续运行且签名有效。仅开发缓存需在下次编译时重建，本轮未重新编译；另行新增本条日志。明细与恢复路径见 `scratch/package-cleanup-inventory-20260906/清理结果.md`。

- ✅ Spec v2.46 宣发前独立复核与修复已完成：原三项风险成立但收窄表述，新增新查找分流、候选指代、覆盖误报三类问题一并修复；模型不能自增用户硬条件，旧记录按原话核对，撤回不重加。最终完整 509 项检查、Release、签名、隐私、系统图标、DMG 与挂载同一性通过。详见 `docs/reviews/2026-09-06-release-blind-review.md`。
- ✅ 正式 App 最终为 `0a25a9440c2ce1c6e48cede1bdc05dbf449beb240d4a83c87c5aa78d89604b39`，PID 45998；精准原句唯一命中，模糊原句实际找到四份指定作品，使用量/Star与三栏正确，未凑数量。模糊本轮 185 次直接请求／0 次 API／2 次外部工具调用，三路来源未完成如实说明；精准 15 次直接请求／1 次 API。最终窗口停在 no-ai-slop，原 AI 开关与设置已恢复。没有付费模型调用；AI 启用路径使用真实历史规划字段的模拟回放验证。
- ✅ 最终 DMG 三件套位于 `app/.build/distribution/`，DMG 摘要 `882a288ab9adb474f1f9461246b32ca4424a35179b54a28c9b5ad035559f869f`。最初备份 `~/Library/Application Support/SkillBoxBackups/20260906-154123/`；最终替换备份 `~/Library/Application Support/SkillBoxBackups/20260906-155121/`。未移除原文件，原资料只重建派生风险 ID，新增四份验收会话。没有安装 Skill、改密钥或发布；工作区包含多个已有未提交切片，未混合提交。另一台 Mac／干净账户首次放行及上线后的附件回读仍是独立发行验收项。

- 🔎 当前发现机制只读风险审查确认三个缺口：离线／禁止上传等必须条件可被去 AI 味用途命中绕过；GitHub 搜索与普通额度混存导致 remaining=9 时误挡后续请求；非 GitHub 请求没有统一累计上限且部分媒体访问不进入统计。当前核心的本地模拟均有原始输出，精准原句仍保留完整名称。报告 `docs/reviews/2026-09-06-discovery-risk-review.md`；正式包、源码和设置未修改，未调用付费模型，未自行开始修复。上一轮 240 次是已统计的资料请求，不能作为全网访问总数或总上限。

- ✅ Spec v2.45 模糊需求漏失与 Star 缺失已完成：用途索引补齐四份独立作品，修复正文能力、双用途改稿、多行简介与语言范围判断；已有详情页补齐仓库 Star。真实验收又捕捉搜索接口超时、详情页可读却零推荐的问题，新增最多四份的独立直读通道，仍核验作者正文和使用量，近似数量不伪造精度。完整 474 项检查、Release、隐私与严格签名通过，详情见 `docs/reviews/2026-09-06-scenario-coverage.md`。
- ✅ 最终正式包 `fe3e2a555fb786590733cf525c590f5fab9587170e3b346feaad3d958c48791d` 已替换并置前，PID 19689。原句 `推荐去文案AI味的skill。` 约 34 秒推荐六份，含 Humanizer、no-ai-slop 和卡兹克两份作品，六份均显示使用量与 Star；240 次资料请求／12 次 API，原上限保持，未声称请求减少或全网查全。精准原句约一秒命中唯一正确对象，15 次资料请求／1 次 API／零模型调用。模糊验收未调用模型，AI 设置已恢复，不把搜索验收等同于模型回答质量通过。
- ✅ 最终备份 `~/Library/Application Support/SkillBoxBackups/20260906-141524/` 保留旧包与完整数据，替换时 755 项文件／链接未变；旧记录、安装和管理关系保留，启动扫描只更新风险条目 ID。窗口停在六份推荐与 no-ai-slop 详情，截图 `scratch/scenario-discovery-fix/scenario-formal-final.jpg`。没有改三栏布局、安装 Skill、混合提交或发布；工作区存在之前的多个未提交切片，故本轮未单独提交。

- ✅ 用户授权后完成 Spec v2.44：完整名称不依赖固定句首，贯穿精确查询与最终结果身份核对；歧义和仅否定名称本地澄清、不发请求，保留作者／用途／口碑路线与三栏。新增失败回归转绿，432 种表达组合和应用结果／澄清检查通过，最终全量 464 项通过。
- ✅ 正式安装已原子替换并打开，指纹 `fac505f6ab93b9981e49975d47d5aebab26019f94ca06ba144ab04ca58d0a1b9`，PID 5028。用户原句 `我找一下vibe-project-foundation-skill这个skill` 在真实应用约 2 秒找到唯一正确对象，15 次资料请求／1 次 API／零模型调用，右侧说明与三栏一致；52 个未核验目录候选如实保留覆盖提示。旧应用与完整数据备份 `~/Library/Application Support/SkillBoxBackups/20260906-125403/`，详情见 `docs/reviews/2026-09-06-vibe-name-root-cause.md`。未发布、未混合提交，未把准确命中扩展成全网必达保证。

- 🔎 用户新的 `我找一下vibe-project-foundation-skill这个skill` 再次失败，确认正式版确为上次版本。固定句首规则漏掉“我找一下”且中文相邻边界不成立，导致 scenario；名称又被拆成 project 等通用词，官方目录对象因此混入推荐。上次两例通过不能代表完整名称查找已解决。
- ✅ 本轮按用户要求只查因和验证可行性，未修改产品源码、替换应用或付费调用。强制保留完整名称的生产搜索能找到真实对象；隔离对象提取实验的 432 个组合从当前 78 个正确提高到 432 个，另 5 个反例通过。加载正式同等 921 条目录后，原句 11.469 秒／15 次资料请求／1 次 API 得到准确对象。方案可行不等于正式版已修复，证据与后续方案见 `docs/reviews/2026-09-06-vibe-name-root-cause.md`。

- ✅ 具体名称修复已完成正式替换与真实窗口验收，安装指纹 `573cf52af0a7e7b7478efa552faa1b6d138b9e4924200c6b35558191032d3099`，PID 96056 来自正式路径。Agent-Team-Skill 约 6 秒显示两份同名对象且本人仓库在首位；wechat-layout-publisher 约 2 秒显示准确同名对象。正式计数分别为 25／16 次资料请求，各 1 次 GitHub API、零模型调用；比独立核心重放各多 12 次完整内置目录读取。来源未全部核验的状态如实保留，三栏与详情一致。
- ✅ 旧应用和完整数据已备份到 `~/Library/Application Support/SkillBoxBackups/20260906-113644/`，模型设置及主要管理关系文件逐字节未变。正式窗口停在准确的公众号 Skill 结果。证据追加于 `docs/reviews/2026-09-06-exact-name-discovery.md`，以下构建中记录已由本条完成状态更新。

- ✅ 具体名称漏找根因：名称前置／紧贴中文／“叫某某”未进入精确路线，Agent Team 又被截成 Team；目录近似结果吞掉核验预算，GitHub 缺少匿名仓库名召回。Spec v2.43 补齐完整名称、独立精确来源组合、核验前同名过滤及根 Skill 的仓库别名，保留作者／模糊需求与三栏。完整 458 项测试通过。
- ✅ 匿名真实重放：`wechat-layout-publisher找一下这个skill` 找到同名公开仓库，1.103 秒／4 次资料请求／1 次 API；`帮我找一个叫Agent-Team-Skill的开发Skill。` 找到两个同名真实对象（含 AidenXu-1），5.887 秒／13 次资料请求／1 次 API，另三个仓库未完成正文核验。详细报告 `docs/reviews/2026-09-06-exact-name-discovery.md`。此节点正式安装仍为上一版，正在构建本次替换包。

- ✅ 用户明确“直接替换，我在真实的正式应用里面体验”后，重新构建当前 Release，通过隐私检查与严格签名，可恢复替换 `~/Applications/SkillBox.app`。安装后 SHA-256 为 `f62b3d80162838b00aa284c7317e46472ac250281bcba5ef97257e83be8d72b2`，与构建一致；精确路径从 Finder 打开，进程 PID 92562 确认来自正式安装位置。
- ✅ 旧应用及完整应用数据已备份至 `~/Library/Application Support/SkillBoxBackups/20260906-111030/`。正式窗口保留原记录、模型 `agnes-2.5-flash` 和三栏；用户已自行开始新搜索，代理未发送搜索或付费模型请求。截图 `scratch/discovery-outcome-fix/formal-app-installed.jpg`；本次只完成正式替换及启动核验，不将进行中的用户搜索记为结果验收。未发布或混合提交已有切片。

- ✅ 用户回复“好的，OK”确认三栏预览，按 Spec v2.42 实现当前任务摘要、连续解释与比较、有来源的助手回复、澄清承接、条件撤回及持久化补充队列。回答复用已核验材料，不触发搜索；队列保持停止与跨对话边界，撤回作者也清除旧深挖计划。
- ✅ 最终 453 项全量测试通过。真实 QA 验证三栏、回复保存与重启、搜索中补充、自动回答、任务条件保留和同秒顺序。真实补充中文搜索为 43 次资料请求／1 次 GitHub API／7 次复用，随后比较不增加搜索。实际停止点击时搜索已完成，因此停止保留的结论仅来自自动化；完整证据见 `docs/reviews/2026-09-06-agent-conversation.md`。
- 📌 QA 已更新并启动，原生截图 `scratch/discovery-outcome-fix/agent-conversation-final.jpg`。测试版未启用 AI，实际显示明确的作者摘录，真实模型回答质量未实测；正式应用未替换，未付费调用、安装或发布，多切片未提交状态保留。

- ✅ Spec v2.41：复现并修复换任务串入旧作者、反馈中的新仓库沿用旧目标、旧对话补充中文丢任务，并兼容“写出活人感文案”交付物追问。现有 AI 规划加入最多 2,000 字符的近期消息与候选引用，当前选中优先；新任务不带旧意图和旧候选，不增加规划调用或搜索预算。
- ✅ 42 项定向、最终 442 项全量回归通过。既有 500 毫秒 UI 时限测试在并行负载下失败两次，单独运行通过；脚本改为单独执行该项而保留断言及测试存在检查。QA 已更新、严格签名并从 Finder 重新启动，实际三栏和两份作者结果完整恢复。正式安装版未替换，未调用付费 AI，未重新打包 Release；多切片未提交状态保留。
- 📌 已调研 Anthropic 上下文工程、LangGraph 记忆与 Cursor 跟进交互，交付 `design/ui/skillbox-agent-conversation-v41.md`、同名 HTML 与 PNG。OpenDesign daemon 无法连接，已用本地可点击预览兜底。新的比较回复、任务摘要显示、运行中排队和助手历史恢复尚待用户确认交互方向；详细边界及验证见 `docs/reviews/2026-09-06-discovery-context.md`。

- ✅ 用户指出卡兹克的 human-writing 漏找，核对确认它位于另一个独立仓库。实际记录暴露口语“卡兹克那个写作的Skill”被当通用场景、作者查询未跨仓库、结果未限制作者，以及作者词与“那个／不要用于写作”等误建用途匹配。现按真实作者账号跨仓库召回，核验前后检查归属，作者词不建立能力或品质，否定用途不进入推荐。Spec 更新至 v2.40。
- ✅ 最终全量回归 435 项通过。真实测试版原句重跑后，三栏恰好显示 khazix-writer 与 human-writing，选中后者显示官方仓库、502 次使用及实际中文说明；原先混入的其他作者和研究工具消失。资料请求 231→44，GitHub API 12→2，仍有 1 个来源和 3 个正文未完成并如实提示。截图 `scratch/discovery-outcome-fix/author-after.jpg`；完整证据 `docs/reviews/2026-09-06-author-discovery-fix.md`。
- ✅ 当前 QA 已重新链接、签名、启动，并通过 Finder 从精确路径唤起前台；Release 候选 9,456 KiB，指纹 `2225dd5988d597fb6ed4d626e3975c609e6259ed3621a54b412208fd281f6906`。正式应用仍为旧指纹，未替换或发布；无付费 AI、Skill 安装或依赖新增。既有未提交切片保留，未混合提交。

## 2026-09-05

- ✅ 用户明确“允许启动”后，当前 QA 包真实窗口验收已完成：三栏正常；完整仓库约 1 秒得到正确结果、1 次资料请求／0 次 GitHub API；中文追问保留原目标。加入从默认分支成功下载并到达“添加所选”确认页，未再被限流阻断，按预览范围取消写入。
- ✅ 用户原句“我需要找去文案AI味的skill”真实运行 33 秒，主列表交付 6 个推荐。记录为 225 次候选资料请求，其中 GitHub API 12 次并触发预算停止；部分来源未完成，界面准确保留结果及覆盖说明。不能把 12 次 API 上限误称为总请求上限，当前仍有公开路径探测较多的优化空间。截图和记录摘要见 `scratch/discovery-outcome-fix/budget-live-*`，具体证据见修复报告末节。
- 📌 启动审批阻塞已解除；本次没有改产品代码、调用付费 AI、实际添加或安装 Skill，也未替换正式应用。当前测试版相关用户出口通过，正式安装版仍需另行替换后才能使用修复；既有未提交改动混在工作区，未合并提交。

- ✅ 按用户“继续”落实整体请求策略，Spec v2.39、ADR-0022：同轮共享请求、GitHub API 每轮最多 12 次（含跳转）、匿名预留已知 12 次余量及进程内每小时 36 次上限；媒体名称逐层兜底、公开正文优先、停止后取消余下读取。固定 12 个候选双来源离线实验由 74 次总请求／24 次 API 降到 25／0，候选全部保留，实际应用组合测试也保留社区证据。
- ✅ 最终完整回归 431 项通过（395 普通＋36 隔离），Release 构建、隐私、严格签名和差异格式检查通过。新候选 9,440 KiB，比正式版增加 96 KiB，指纹 `0e43e8b478f4e6585d71142a5bf44b80452a18df91154e308aec9a0ec3a40f56`；正式安装版保持原指纹，未替换或发布。首轮既有计时断言失败及重跑证据详见修复报告，未放宽断言。
- ⚠️ 当前版本真实窗口验收未完成：自动审批拒绝启动本地构建 QA 应用，要求启动时明确确认。独立资料库、匿名且无付费 AI 的验收包已经重新链接准备好，未绕过审批。三栏布局规则未改；旧截图不能充当新版本验证。下一步取得启动许可后，验证实际搜索、覆盖计数与加入预览。详见 `docs/reviews/2026-09-05-skill-discovery-fix.md`。

- 🔎 根据用户对“一分钟调用量不符合策略”的追问，确认此前 60/60 仅为共享出口小时累计额度，不能作为本应用一分钟请求数。进一步发现媒体与普通来源各自缓存导致重复核验、skills.sh 正文直读前先查询仓库资料、媒体名称的逐层兜底实际仍为并发、额度门缺少用户操作级总预算。离线调用实际核心代码，固定相同 12 个候选：一个对象发出 12 次 GitHub API 请求，同对象重查为 0；两个新对象并行即为 24 次，最终仍只有 12 个候选。说明上一轮只修复了完整地址路径，尚未解决整体调用策略。证据追加至 `docs/reviews/2026-09-05-skill-discovery-fix.md`；本次追查未消耗真实 GitHub 额度、未改产品代码或替换应用。

- ✅ 按用户“我要最终结果”的要求，完成发现页四项已复现错误的修复：完整仓库先公开直读、根文件和分支身份保留；补充条件保留目标且新任务可切换；PPT/Excel 不再误当仓库；受限未核验不再显示确认不存在。详情补齐作者说明链接、应用操作步骤和启动提示词。Spec 更新至 v2.38，详见 `docs/reviews/2026-09-05-skill-discovery-fix.md`。
- ✅ 当前代码完整回归 420 项通过。隔离资料库的真实三栏窗口在 GitHub 普通匿名额度为 0 时，重新输入此前失败的仓库地址，11:10:34 开始、11:10:36 记录完成，工具约 8 秒回读到正确的 1 个候选；补充“最好支持中文”后原任务和仓库仍保留。没有调用真实付费 AI。
- ℹ️ 用户反馈三栏变两栏，现场确认原因是临时验收窗口宽度触发既有中等窗口布局；已放大恢复三栏，并重新核对正式应用亦为三栏，未修改三栏断点。GitHub 官方返回普通 API 额度 60/60 耗尽，分钟搜索额度仍为 10/10；同一出口 IP 共享，无法将全部消耗归于本次测试。
- ⚠️ Release 修复候选已构建并通过严格签名，占用增加 36 KiB，指纹 `b48642ff685228b9a01c4bc233e550848aaa334e2f6f62a7f44ff1da34f630e6`。加入仍因版本资料查询限流被阻断，未完成真实加入、安装或 AI 自然语言全流程，不报告完整主流程通过。正式应用指纹仍为 `4d855171a1d08287374b664e69ebe0fa836b07fdcd944e518db05c8423a75956`，本轮未替换、提交或发布；既有未提交改动全部保留。

- 🔎 对“发现 Skills”完成真实已安装窗口评审及 168 项相关自动化测试，记录于 `docs/reviews/2026-09-05-skill-discovery-review.md`。确认 4 项功能问题：已知仓库/根目录说明文件走普通搜索导致漏找；免费兜底丢失常见补充条件；PPT/Excel 被当仓库；限流导致的未核验被主界面写成没有找到。另记录候选缺少中文使用步骤的体验缺口。真实名称搜索 8 秒得到 9 项，停止和历史恢复正常；加入被 GitHub 限流阻断，未完成导入；未调用真实付费 AI，不能宣称新的自然语言主流程通过。本轮未改产品实现、未安装、未替换、未提交或发布，既有改动保留。

- ✅ 从用户真实记录定位并一起修复了发现页的三层根因：运行诊断曾被重复保存并整段显示成最终回复；中栏没有展示真实推荐总数，固定视窗造成“每次五个”的错觉；详情按是否取到数据选择性显示安装量或 Star，又没有说明前者属于 Skill、后者属于整个仓库。新版先显示“找到 N 个值得优先看的 Skill”，所有运行不足默认收进“查看本轮覆盖情况”；中栏动态显示“推荐 N 个”和“不凑数”；右侧固定并列展示“Skill 使用量”和“GitHub 仓库热度”，未知明确写“暂无公开数据”，不再冒充零。
- ✅ 场景推荐的品质门槛从根上收紧：真实 `SKILL.md` 与用途匹配只是基础，强推荐还必须满足作者官方、至少 500 次 Skill 级安装量或至少两位独立社区推荐者之一；近期维护和完整说明只帮助排序，不能把几十次使用量、没有外部认可的新项目抬成强推荐。点名具体 Skill 的精确查找继续不受热度门槛影响。没有达到品质门槛的候选不再消耗模型比较上下文。
- ✅ 补上模型覆盖本地查询计划的漏洞：“帮我找一个去文章AI味的Skill”等表达现在稳定补充 `humanize writing`、`remove AI writing style` 和 `natural writing rewrite`；即使模型返回宽泛的 `writing editing` 或 `content writing workflow`，也不能删除精准词或把宽泛词带回搜索。新增结果优先、动态 7 项无展示上限、失败态不冒充绿色成功、质量依据、热度口径、未知与零、模型查询协调等回归；完整门禁共发现 413 项测试，其中 377 项普通测试与 36 项 GitHub 来源隔离测试全部通过。
- 💾 新 Release 应用占用 9,344 KiB，比上一安装版增加 20 KiB；没有新增记录字段、数据库、索引、网页正文缓存或付费服务，寻找记录仍共用原有 64 MiB 总上限。取消重复保存运行诊断后，新记录通常会略微变小。候选包隐私检查、Info.plist、ad-hoc Hardened Runtime 严格签名和 Designated Requirement 均通过，可执行文件 SHA-256 为 `4d855171a1d08287374b664e69ebe0fa836b07fdcd944e518db05c8423a75956`。
- ✅ 新包已可恢复地替换到 `~/Applications/SkillBox.app` 并从唯一正式路径启动，当前进程 PID 为 `88326`；安装后指纹与 Release 候选完全一致。上一安装版完整保留在 `~/Documents/SkillBox Backups/2026-09-05/SkillBox-before-result-first-quality-fix-20260905-025526.app.backup`。替换前后 Skill 分配、安装关系、分类、来源、目标和事务记录摘要一致，寻找记录仍为 4 条。
- ⚠️ 最终前台验收尚未完成：两次读取真实窗口时 macOS 都明确返回“Mac 已锁定”。因此当前只确认代码、全量回归、Release 包和正式路径替换通过，不能把它写成用户可见界面已经验收。解锁后需在已安装版用原句“帮我找一个去文章AI味的Skill”验证动态推荐总数、折叠覆盖详情、双热度指标和真实候选质量，再决定是否提交本切片。
- ⚠️ 对已安装 v2.35 执行真实前台验收时，同一句“我需要找去文案AI 味的skill”在“正在理解你的需求”停留超过 2 分钟，寻找记录始终只有初始状态，点击停止也无法及时退出。这再次证明只有真实安装版的最终列表才是有效验收出口。
- ✅ 第二个根因是模型接口的 30 秒配置只能限制“网络完全没动静”，服务器只要断续传输数据就可继续占住任务，而 Swift 的结构化取消又会等不响应取消的子任务退出。现已为需求整理和每次候选比较加入 20 秒真实墙上时钟上限；到时或用户停止后立即释放主链路，改用发送前已形成的本地语义计划和公开来源结果。
- ✅ 新回归专门让模型无视取消并持续占用 1 秒，应用仍在 0.063 秒内触发人工设定的 40 毫秒截止、回退至公开来源并交付 `story-deslop`。`app/Scripts/test-all.sh` 整体退出码为 0，主测试组已增至 374 项，GitHub 来源隔离用例也全部通过。
- 💾 新安装版占用 9,324 KiB，比上一安装版增加 16 KiB；可执行文件增加 16,816 字节，SHA-256 为 `152da64b6329f47b9064d83e1c8a1ae40159af4e539667a95a30fe4f31900cfb`。这次超时收口不增加记录字段、缓存、索引、外部工具或搜索费用。
- ✅ 新版已可恢复地替换到 `~/Applications/SkillBox.app`，安装后指纹与 Release 构建完全一致，严格签名验证通过，并已从精确路径重新打开。上一安装版保留在 `~/Documents/SkillBox Backups/2026-09-05/SkillBox-before-ai-deadline-fix-20260905-0115.app.backup`。解锁后已在这个安装版的真实窗口再次输入“我需要找去文案AI 味的skill”：用时 64 秒，召回 23 个候选并在主列表交付 7 个真实推荐，依次包含 `remove-ai-style`、`humanizer-zh`、`ai-text-humanizer`、`qu-ai-wei`、`story-deslop`、`editor-revisor` 和 `jacky-de-ai`。右侧详情与可见首项 `remove-ai-style` 一致，`finnish-humanizer` 只留在折叠的非推荐证据中，没有再冒充最终结果。本轮 Agnes 需求整理记录为 534 输入 Token、86 输出 Token；候选比较未在截止前返回可用结果，应用如期保留本地质量排序。
- ⚠️ 纠正 9 月 4 日的过早结论：上一次只用后台重放证明“某些来源能搜到”，没有用已安装应用中的最终候选列表作为验收出口。用户新的真实记录用时约 4 分 51 秒，最终推荐为空，却在详情区保留了芬兰语专用 `finnish-humanizer`，并用绿色对勾告知“暂时没有足够可靠的推荐”。这份用户可见结果证明上一轮验收无效。
- ✅ 真正根因是四层叠加：skills.sh 把 6 个查询串行执行；55 秒外层截止会等不响应取消的底层任务真正退出，因而被拖到数分钟；来源超时后整路结果被丢弃；即使 skills.sh 召回 200 多项，初轮也只读前 4 份 `SKILL.md`，`remove-ai-style`、`story-deslop`、`humanizer-zh` 等强匹配恰好落在后面。
- ✅ 已将 skills.sh 各条查询改为并发，搜索请求限时 12 秒、资料核验限时 10 秒、raw 正文直读限时 8 秒；外层时限到达后可立即返回其他来源的已核验结果。初轮 skills.sh 正文核验窗口从 4 扩到 12，明确去 AI 味的需求不再混入宽泛写作词。
- ✅ 排序与最终界面同步收口：中文改写需求优先支持中文的候选，学术论文专用和芬兰语专用结果不再进入该场景推荐；右侧详情只能选中主候选区真实可见的推荐。空推荐、未完成和降级状态不再显示绿色成功对勾。
- ✅ 用修复后的实际查询规划重放同一用户原句，真实免费公开来源在 10.98 秒内召回 219 个候选，在线读取 12 份 `SKILL.md`，最终推荐 6 项。前三项为 `remove-ai-style`、`humanizer-zh` 和 `story-deslop`，后者当时有 13,432 次 Skill 级安装。`dbs-ai-check` 因只诊断不改写被排除，`humanize-academic-writing` 因领域过窄被排除，`finnish-humanizer` 不再进入该场景。
- ✅ 新增并发、真超时、核验窗口、精准补充词、中文与窄领域错配、空详情、状态色和完整应用交付流程回归。`app/Scripts/test-all.sh` 整体退出码为 0，主测试组 373 项全部通过，GitHub 来源的隔离用例也逐项通过。Release 生产构建、Info.plist、严格签名和 Designated Requirement 验证通过。
- 💾 新应用占用 9,308 KiB，比旧应用增加 20 KiB，新可执行文件 SHA-256 为 `b0707c4ef3213338d793e92ac993f6b745c0040959a96edd4045c7e42e3a5082`。每轮比旧版最多多保留 8 份、每份最多 8,000 字符的核验摘要，通常新增约 64 KiB，大量中文或多字节内容的极端估算不超过约 0.2 MiB。没有新数据库、向量索引、网页正文缓存或付费搜索服务，全部寻找记录仍共用原有 64 MiB 硬上限。
- ✅ 已可恢复替换到 `~/Applications/SkillBox.app` 并重新打开，安装后指纹与新构建完全一致，当前 PID 为 70904。旧应用完整保留在 `~/Documents/SkillBox Backups/2026-09-05/SkillBox-before-discovery-final-fix-20260905-003715.app.backup`；本轮没有发布 GitHub。安装后的最终前台点击验收因 Mac 当前锁屏尚未执行，解锁后必须用已安装应用输入用户原句，并以真实候选列表为最终验收出口。

## 2026-09-04

- ✅ 查清截图中“公开来源未完成、最终却一条候选都没有”的真实原因：同一轮里 Agnes 需求整理只用了 2.256 秒，B 站和公众号也确实找到了“去 AI 味”相关内容；结果被清空发生在后半程。旧逻辑要求媒体内容必须直接写出可唯一定位的 Skill／仓库名，否则会把其他免费目录已经核验通过的候选一起删掉；界面层又重复执行了一次相同过滤。与此同时，媒体支路外层只等 55 秒，内层却可能先等媒体 45 秒、再等名称核验，形成必然存在的提前截断窗口；能力短语还会被误判为精确 Skill 名，挤占正文核验名额。
- ✅ 最终出口现改为“媒体口碑增强可信度，免费目录的已核验强候选负责兜底”：媒体、skills.sh、GitHub 与可信目录并行，B 站和公众号也会收到跨语言补充查询；媒体内嵌的 `Humanizer-zh`、`1. humanize - ...` 等名字可以正确提取；只有看起来确实像单个 Skill 名的查询才享受精确核验优先级；普通 API 文档仅偶然出现 `rewrite` 不会再进入“去文案 AI 味”的结果。媒体没有写仓库名或某个平台超时，都不能再让最终结果区归零。
- ✅ 用用户原句重新跑真实免费公开来源，38.1 秒取回 18 个候选，最终稳定保留 `zenstory-ai/oh-story-claudecode/story-deslop`（13,421 次安装）和 `zc277584121/marketing-skills/remove-ai-style`（3,584 次安装）；二者真实 `SKILL.md` 都直接说明去除 AI 写作痕迹／机械感。一次公开来源失败仍能返回这两项；偶然命中 `rewrite` 的 Claude API 候选已由相关性门排除。新增回归均按先失败再修复完成，完整 `app/Scripts/test-all.sh` 401/401 通过，`git diff --check`、Release 构建、严格签名与 Designated Requirement 均通过；新可执行文件 SHA-256 为 `0cd5af1f15424d1f29f4e195b3f1d2f60a700fc9b5d019d7fb5c906bba4bdd5d`。
- 💾 新应用占用 9,288 KiB，比替换前 9,272 KiB 增加 16 KiB，约 0.016 MiB；没有新增数据库、向量索引、网页正文缓存或付费搜索服务，也没有提高寻找记录原有的 64 MiB 总上限。已按可恢复流程替换到 `~/Applications/SkillBox.app` 并从精确路径打开，当前 PID 为 64519，版本仍为 `0.2.2 (Build 5)`；旧版完整保留在 `~/Documents/SkillBox Backups/2026-09-04/SkillBox-before-final-result-fix-20260904-231633.app.backup`。替换前后 Skill 安装、分配、分类、来源、目标、事务文件摘要和 2 份寻找记录数量均一致，本轮没有发布 GitHub。
- ✅ 修复用户真实输入“我需要找去文案 AI 味的 skill”返回空结果的问题。根因是媒体标题明明同时列出 Humanizer、Stop-Slop、Taste 与 Nuwa，旧提取却只保留两个名字；随后前面的噪声候选耗尽正文核验名额，GitHub 匿名额度受限时又缺少免费直读兜底，最后旧语义门还没有识别“去文案 AI 味”这一说法。现在会完整保留媒体明确列出的 Skill 名，每个精确名字公平获得核验机会，并可直接读取 GitHub 公开原始 `SKILL.md` 核对真实名称。
- ✅ 对同一句需求完成真实公开网络回读，最终只保留与改写任务直接相符、同时有社区推荐和真实正文的 `blader/humanizer/humanizer` 与 `hardikpandya/stop-slop/stop-slop`；偏向视频创意方向的 Taste 和未证明能完成该任务的 Nuwa 会在相关性门槛被排除。新增 4 条公开边界回归，完整 `app/Scripts/test-all.sh` 门禁 397/397 通过，`git diff --check`、Release 构建、包内隐私、严格签名和 Designated Requirement 均通过；新可执行文件 SHA-256 为 `193a574188ffaf1583e8c4b558fb5330a9d4773972ba4d6f0306d538969631e0`。
- 💾 新应用占用 9,272 KiB，比替换前 9,256 KiB 增加 16 KiB，约 0.016 MiB；没有新增数据库、向量索引、媒体缓存、外部付费搜索或额外工具。极端情况下，一轮出现 8 个不同精确名字时会比旧版多保存至多 4 份、每份最多 8,000 字符的已核验正文，连同元数据估算额外小于约 40 KiB，并继续受全部寻找记录累计 64 MiB 的既有硬上限约束；只有用户自行选择的模型 API 可能产生费用。
- ✅ 已按可恢复流程把新版替换到 `~/Applications/SkillBox.app` 并从精确路径重新打开，当前进程 PID 为 43064，版本仍为 `0.2.2 (Build 5)`；旧版完整保留在 `~/Documents/SkillBox Backups/2026-09-04/SkillBox-before-real-search-fix-20260904-1737.app.backup`。替换前后 AI 设置、来源、组织、分配、安装、目标和事务文件摘要一致，寻找记录数量也保持 2 份；真实窗口回读仍显示 7 份全局 Skill。本轮诊断使用临时入口，没有代用户新增搜索记录、调用模型 API、改写 Agent Skill 目录或发布 GitHub。
- ✅ 按用户要求由独立子 Agent 从第一性原理完成语义路由盲审，并按审查结论落地三条自动链路：明确 Skill 名、GitHub 链接或仓库进入精确查找；只描述要解决的问题进入社区场景发现；作者加能力或询问已知 Skill 的口碑进入混合查找。界面仍只有一个输入框，链接、仓库、路径、名称和作者等用户明确事实优先于模型推测，模型不能改写已经确定的目标。
- ✅ 精确查找现在跳过媒体搜索、模型需求规划和候选比较；GitHub Skill 路径直接读取对应 `SKILL.md`，并同步确认仓库仍有效。结果只保留同名、同仓或同路径的真实 Skill；没有命中时如实说明，不用相似结果填补，同名多个仓库则全部列出。用户点名的真实 Skill 不再因安装量或热度低而消失。
- ✅ 场景发现只展示已经绑定 YouTube、B 站、公众号或 Hacker News 公开推荐证据、并读到真实 `SKILL.md` 的候选；可信目录、skills.sh 与 GitHub 只负责后续定位和核验，不能单独冒充社区推荐。媒体只提 Skill 名且存在多个同名仓库时保留为未解析线索，不把同一条口碑复制给多个项目。排序先看任务和交付物是否直接匹配，再比较社区作者数、平台覆盖、热度与安装量，热门但偏题的 Skill 不能再排在前面。
- ✅ 修复审查和全量回归中暴露的两处语义误判：“我想找一个能给公众号文章排版的 Skill”不会再把整句话当作者；`帮我找 humanizer Skill`、`Humanizer Skill`、`人群分析这个 Skill`、带引号名称、`owner/repo` 与 GitHub 路径均能进入精确链路。新增精确未命中不替换、场景证据门、同名歧义、低热度精确结果、相关性优先、直读 GitHub 路径和旧记录兼容等回归。
- ✅ 安装前 `app/Scripts/test-all.sh` 最终静默门禁 393/393 通过；Release arm64 构建通过，包内隐私、ad-hoc Hardened Runtime 严格签名、Designated Requirement 与 `git diff --check` 均通过。可执行文件 SHA-256 为 `4c1dbb47ba621856656032b68e7add939f1f327fe5bc4969929ae93b2bc6668b`。
- 💾 新应用占用 9,256 KiB，比原安装版 9,180 KiB 增加 76 KiB，约 0.07 MiB。路由记录每轮新增小于 2 KiB，不新增数据库、向量索引、网页正文缓存或付费搜索服务，全部寻找记录继续共用原有 64 MiB 总上限；只有用户选择的模型 API 可能产生费用，精确查找的模型 Token 消耗为零。
- ✅ 已按可恢复流程替换到 `~/Applications/SkillBox.app` 并从精确路径启动，当前进程 PID 为 25105，版本仍为 `0.2.2 (Build 5)`。替换前版本保留在 `~/Documents/SkillBox Backups/2026-09-04/SkillBox-before-semantic-routing-20260904-153644.app.backup`，旧可执行文件 SHA-256 为 `339b679c5ac91b4ded9740ad4cd9f95de2a91f572a592e7c6fa829ed9bf7b6e0`。启动后仍为 7 份 Skill、4 条分配、4 条安装、12 个应用目标和 10 条事务；AI 设置、安装关系、组织、来源和事务文件摘要未变，`catalog.json` 与聚合状态仅因正常启动重扫而更新。本轮没有代用户提交真实搜索、调用模型 API、改写 Agent Skill 目录或发布 GitHub。
- ✅ 按独立盲审结果完成发现质量收口：用户只补充“更热门、活人感”等条件时，上一轮作者和精确 Skill 名继续参与查询；媒体提取不再把 `http`、`https`、`www` 或域名后缀当成 Skill 名；“去 AI 文案味”会排除只检测、只打分而不负责改写的候选。无法立刻绑定真实 `SKILL.md` 的社区讨论不会混入推荐，但会以每轮最多 8 条的有界元数据保留并明确提示，避免真实口碑线索被静默丢弃。
- ✅ 可信目录、skills.sh、GitHub 和媒体平台已经改为并发、固定顺序合并：单个候选来源最长等待 55 秒，单个媒体平台最长等待 45 秒，超时只降级该来源并保留其他已核验结果。skills.sh 默认改用无需登录的 `/api/search`，真实匿名回读 `humanizer` 返回了 46,951、9,153 与 5,640 次安装的前三项；精确媒体名称会先查内置可信目录，再查 skills.sh，最后才消耗 GitHub 匿名搜索额度。
- ✅ 模型上下文由“最多 30 个候选、多组晋级和最终比较”收成“每轮只比较本地排序最高且尚未送评的 6 个候选一次”；输入只保留必要 Skill 证据和本地聚合的社区作者数、平台数、归一热度，不再发送媒体标题与作者原文。单轮比较输出仍限 1,500 Token，服务商结构化输出失败时仍可能按既有安全边界重试，未送评候选继续留到下一次深挖。
- ✅ `yt-dlp` 现在显式忽略本机配置、禁止读取浏览器 Cookie、禁止读取其他 Cookie 并关闭磁盘缓存。B 站与公众号真实只读网络回归在 0.689 秒内通过，全程未打开浏览器；项目完整 `Scripts/test-all.sh` 门禁通过当前 379 项。另行尝试的 Xcode Scheme 测试仍在启动测试前被项目既有的“测试目标无法导入可执行模块 `SkillBoxApp`”问题挡住，因此不把它写成通过；SwiftPM 完整门禁不受影响。
- 💾 新 Release 应用占用 9,180 KiB，比替换前 9,128 KiB 增加 52 KiB，约 0.05 MiB；未绑定社区线索典型每轮新增小于 8 KiB，全部寻找记录仍共用原有 64 MiB 总上限。没有新增数据库、向量库、全文镜像或付费搜索服务；唯一可选外部工具 `yt-dlp` 仍约 35.4 MiB，OpenCLI 已退出运行依赖且本轮未删除其旧副本。
- ✅ Release 候选通过包内隐私检查、ad-hoc Hardened Runtime 严格签名和 Designated Requirement 校验，可执行文件 SHA-256 为 `339b679c5ac91b4ded9740ad4cd9f95de2a91f572a592e7c6fa829ed9bf7b6e0`。已按可恢复流程替换到 `~/Applications/SkillBox.app` 并从精确路径启动，当前进程 PID 为 2136，版本仍为 `0.2.2 (Build 5)`；替换前版本保留在 `~/Documents/SkillBox Backups/2026-09-04/SkillBox-before-quality-hardening-20260904-131603.app.backup`，其可执行文件摘要为 `ee58abb23731d68f510b62d548e6ff02c6001b62fd5c942ed362ca501bda6d9e`。
- ✅ 替换后 `assignments.json`、`installations.json`、`organization.json`、`local-source-state.json`、`source-state.json`、`targets.json`、`transactions.json` 与 AI 设置摘要保持不变。应用正常启动时重扫并重写了 `catalog.json` 与聚合的 `library-state.json`；复核后仍为 7 份 Skill、4 条分配关系、4 条安装关系、12 个目标和 10 条事务，没有发现 Skill 增删或 Agent 安装关系变化。本轮没有发布 GitHub，也没有调用用户模型 API。
- ✅ 已将媒体发现链路改成全后台静默运行：公众号直接解析搜狗微信公开搜索页，B 站使用匿名公开接口与 WBI 签名，YouTube 继续只通过 `yt-dlp` 读取公开元数据，Hacker News 保持公开接口；所有网络请求都禁用 Cookie 和磁盘缓存，不读取 Chrome 标签页、登录态或浏览器数据，也不会自动打开浏览器。小红书与抖音当前完全退出默认和显式搜索链路，用户点名时会明确显示本轮未参与，不能偷偷改用浏览器兜底。
- ✅ 新增公众号公开 HTML、B 站固定签名向量、匿名接口无 Cookie、默认来源清单、禁用平台透明降级等回归。当前测试清单共 369 项，完整 `Scripts/test-all.sh` 门禁通过；需要真实网络的可选回读也已单独通过，公众号和 B 站都返回了真实公开结果。Release 候选通过包内隐私检查、严格签名和 `git diff --check`，可执行文件 SHA-256 为 `ee58abb23731d68f510b62d548e6ff02c6001b62fd5c942ed362ca501bda6d9e`。
- 💾 新版应用实际占用 9,128 KiB，比替换前的 8,892 KiB 增加 236 KiB，约 0.23 MiB；B 站和公众号不新增本地索引、数据库或常驻磁盘缓存。若从零启用 YouTube 公开元数据搜索，唯一仍需的免费外部工具 `yt-dlp` 当前占 36,276 KiB，约 35.4 MiB。OpenCLI 已完全退出 SkillBox 运行依赖；本机旧安装仍占 31,736 KiB，约 31.0 MiB，因本轮没有获得删除授权而保留，后续可单独清理。
- ✅ 已按可恢复流程把候选替换到 `~/Applications/SkillBox.app` 并从这个精确路径启动，当前进程 PID 为 60973，版本仍为 `0.2.2 (Build 5)`。旧应用完整保留在 `~/Documents/SkillBox Backups/2026-09-04/SkillBox-before-silent-discovery-20260904-0348.app.backup`；替换前后 `assignments.json`、`catalog.json`、`installations.json`、`organization.json` 和 `local-source-state.json` 摘要一致，没有改写用户 Skill、分类或 Agent 安装关系。
- ✅ 将“热门 Skill”发现从单一 Hacker News 线索扩展为媒体口碑优先：默认先读 YouTube、B 站、公众号和 Hacker News，当用户在需求中明确写出小红书或抖音时才读取对应登录态平台；媒体只提供带作者、标题、链接和单平台互动数的短线索，随后由 skills.sh 与 GitHub 共同定位原作并在线核验真实 `SKILL.md`。媒体普通单词、无法精确对名／仓库的内容和未经 GitHub 核验的模型猜测都不能形成候选。
- ✅ 热门排序改为先守住任务匹配，再比较独立提及者、跨平台覆盖、按平台对数缩放的互动信号和 Skill 级安装量；同一作者跨平台重复发布只计一人，不同平台数字不直接相加。公众号等拿不到作者身份的帖子仍可证明该平台出现过，但不会冒充新的独立推荐者。每个候选最多保留 5 条媒体证据元数据，不保存视频、字幕、文章正文、搜索页或平台 Cookie。
- ✅ 当前 Mac 已安装免费 OpenCLI 1.8.7 与官方 `yt-dlp` 2026.08.19；实际新增占用合计 68,016 KiB，约 66.4 MiB，其中 `yt-dlp` 36,276 KiB，OpenCLI、依赖与配置／补全骨架 31,740 KiB。YouTube 真实只读搜索已命中 “Humanizer: The FREE Claude Skill…” 并能抽取 Humanizer；输出已收窄到标题、作者、链接、播放量、简介和日期，不下载视频。OpenCLI 浏览器桥接尚未安装，公众号真实探测明确返回 `Browser Bridge extension not connected`，所以 B 站、公众号、小红书和抖音的真实平台闭环仍待用户安装并启用 Chrome 扩展后验收；应用会逐项标为未参与，不用其他来源冒充成功。
- ✅ 先失败再修复的新增回归覆盖精确名称提取、真实 Humanizer 标题、GitHub 验真、同作者跨平台去重、无作者不计人数、来源失败保留结果、公众号 `weixin` 映射、YouTube 精简只读输出和小红书／抖音当次许可门。当前完整门禁 366/366 通过；最终 Release 候选通过 `Info.plist`、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私检查与 `git diff --check`，可执行文件 SHA-256 为 `874876369bff726a1aa3d40516b35e5a2cbf90fb83b477c81cd62545760303f6`。
- 💾 最终候选应用包为 9,076 KiB，比当前安装版 8,892 KiB 增加 184 KiB，约 0.18 MiB；加上两项可选免费媒体工具后，本机本轮固定新增约 66.6 MiB。寻找记录仍受原有 64 MiB 累计硬上限约束，这个上限没有上调。下载时使用的两份临时校验文件已删除，这次探测启动的 OpenCLI 后台进程也已停止。
- 📌 新包仍只位于项目内 `app/.build/release/SkillBox.app`，没有替换或启动当前 `~/Applications/SkillBox.app`，当前运行版指纹仍为 `ecbdec4fde0d136a62a0b94b50e69447314cd0254a7cd73b922adb8dada36227`；没有改写寻找记录、Skill 库或任何 Agent Skill 目录。完整中文媒体体验还差 Chrome 浏览器桥接这一步，不能把 YouTube 命中和自动测试写成六个平台均已真实通过。
- ✅ 根据两次真实搜索记录复盘“热门感很差”的根因：“卡兹克”被模型自行改写成《英雄联盟》角色写作，原始作者身份和精确 Skill 名一起丢失；“去文案 AI 味”已经召回数千至数万次安装的候选，但旧排序把更新时间和说明长度放在真实使用与社区口碑之前。现在初次搜索始终保留用户原句，并保护“某人的某类 Skill”这类作者／品牌意图；“卡兹克的写作 Skill”会补充 `KKKKhazix writer` 和 `khazix-writer`，模型误补的游戏角色条件不能再覆盖用户原意。
- ✅ 发现链路新增免费、可失败的社区证据层：先在 Hacker News 的公开 Algolia 搜索中寻找明确链接到 GitHub 的讨论，再用原有 GitHub Provider 核验真实 `SKILL.md`、仓库身份和当前资料；没有明确 GitHub 链接、无法核验正文或只来自同一作者重复发布的线索不能伪装成“多人推荐”。社区源失败时，可信目录、skills.sh 与 GitHub 搜索继续工作。
- ✅ 排序先通过任务能力门槛，再按独立社区提及人数、社区互动和真实安装量比较，最后才看来源、维护、说明完整度和可归属于单个 Skill 的 Star。只检测 AI 痕迹、明确不负责改写的 Skill 不再进入“去 AI 味”推荐。没有接入付费网页搜索、Embedding、向量库、本地模型或完整网页镜像；Bing RSS 的许可边界和 Reddit 新应用审批条件不适合作为产品依赖，因此没有采用。
- ✅ 本轮新增作者身份保护、社区 GitHub 明链、同作者去重、社区证据排序、自然语言“AI 味”能力错配等回归；项目正式拆分门禁 355/355 通过（普通功能 319 项、GitHub 来源隔离 36 项），`git diff --check` 通过。Release arm64 候选已构建并通过 Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement 与包内隐私检查；可执行文件 SHA-256 为 `3d931b1a88519831fbeb28cf57ff492cb16f686d838d752b63a98ec776168713`。
- 💾 新逻辑不新增本地索引、数据库或常驻媒体缓存，固定数据存储新增为 0；Release 应用包为 8,964 KiB，比当前安装版 8,892 KiB 增加 72 KiB，约 0.07 MiB。每个候选最多保存 5 条社区元数据，不保存帖子正文；按 96 个详细候选的极端情况估算，一次寻找记录最多额外约 0.47 MiB，并继续包含在原有全部寻找记录累计 64 MiB 的硬上限内。
- 📌 当前新包只位于项目内 `app/.build/release/SkillBox.app`，没有替换正在使用的 `~/Applications/SkillBox.app`，没有启动候选、写入用户寻找记录或任何 Agent Skill 目录。代码门禁通过不能代替用户对真实搜索结果的前台体验，需获得明确替换授权后再进入体验验收。

## 2026-09-03

- ✅ 对发现搜索做了第一性原理盲审，并逐项修复审出的断点：搜索读到上限、单个查询失败和正文核验失败都会留下独立事实，不再把截断结果写成完整；30 个候选按轮转方式分组，最终比较只调整各组优胜者顺序，不再误杀已经成立的强候选；用户点“继续深挖”会沿用模型补出的跨语言搜索词，并从尚未核验、尚未比较的位置继续。
- ✅ Skill 正文现在始终按不可信资料处理。盲审真实复现了“改写诱导语 + 不支持目标的否定句”仍可骗过旧模型校验；修复后，候选必须先通过本地相关性与品质门槛才会送进模型，模型结果不能再新增、隐藏或降级推荐资格。否定识别已绑定到具体能力：`No`、`lacks`、`is absent`、`没有`、`缺少`和`未实现`等否定词族不再当成命中，而“支持邮件，但不支持日历”仍能正确保留邮件能力。候选名称只帮助被找到，必须由已核验的 Skill 摘要建立推荐资格。目录拆成作者官方维护和平台收录两档，平台收录只帮助找到和排序，不能形成品质证明；推荐还必须满足作者官方、至少 500 次 Skill 级安装，或“近两年维护 + 有实际使用结构的正文”之一。
- ✅ 可信目录已重新生成并排除仓库内部 `.github/`、`.agents/` 配置：共 921 项、432,373 字节，其中 19 项作者官方维护、902 项平台收录。补齐安全审查、无障碍、代码评审、测试、排错和发布等中文目标的免费英文扩词；真实目录基准要求安全、无障碍、演示文稿、Excel、PDF 和深度研究六类目标的对应 Skill 都进入前 5。
- ✅ 缓存与存储改为有界：内置目录正文缓存 6 小时过期，最多 128 份且合计不超过 8 MiB；skills.sh 的仓库资料和正文缓存分别最多 128、256 项。寻找记录保留所有候选元数据，只让最需要的 96 个候选保存每份最多 8,000 字符正文；150 个满长候选的压力样例小于 1.5 MB，所有寻找记录累计达到 64 MiB 后明确停止新增，不静默删除旧记录。
- ✅ 按先失败、再修复、再完整复算的方式新增召回触顶、提示注入、否定能力引用、平台收录越权、分组位置偏差、跨语言继续深挖、缓存过期、中文真实目录命中和累计存储上限等回归。发现与 AI 专项 109/109、完整门禁 350/350 均通过。最终 Release 候选重新构建并通过 `Info.plist`、严格签名、Designated Requirement 与包内隐私检查；可执行文件 SHA-256 为 `ecbdec4fde0d136a62a0b94b50e69447314cd0254a7cd73b922adb8dada36227`，候选占用 8,892 KiB，比当前已安装版 8,148 KiB 增加 744 KiB，约 0.73 MiB。
- 📌 本轮没有替换 `~/Applications/SkillBox.app`，没有写入任何 Agent Skill 目录，也没有发送真实用户需求或调用真实模型完成前台搜索。因此当前结论是代码、固定目录、自动回归和候选包通过，不能冒充真实用户搜索体验已经通过或已经发布。
- ✅ 按“质量优先、模型调用之外零费用”的边界重做发现链路：初次／深挖单查询召回从 24／96 提升到 64／160；取消 Star 和安装量的送评门槛；中文常见任务在 AI 不可用时也会补充英文能力词。排序改为先看任务与交付物匹配，再看可信来源、维护和说明完整度，热度只作后置参考。
- ✅ 新增 924 项版本化可信目录，来源为 OpenAI、Anthropic 与 GitHub 公开 Skill 集合，资源文件为 434,189 字节。目录只用于召回，展示前仍实时读取并验证当前 `SKILL.md`；没有引入付费搜索、向量数据库、Embedding、本地模型或完整仓库镜像。搜索正文只作内存核验，寻找记录的单候选正文上限由 60,000 字符降为 12,000 字符。
- ✅ AI 比较从单次前 8 个升级为最多 30 个候选、每组 6 个、各组第一名再做最终比较；每组必须完整返回全部真实候选，未知、重复、漏评或损坏结果整组回退。新增低 Star 送评、跨语言保留、维护优先、分组晋级、YAML 多行说明与实时目录核验等回归；搜索专项 46/46、AI 接入专项 31/31、完整门禁 320/320 均已通过。
- ✅ Release 候选已构建并通过 Info.plist、ad-hoc Hardened Runtime、严格签名、Designated Requirement 与包内隐私检查，可执行文件 SHA-256 为 `060d736dbc92f340b65c3ed1c423bc4cb7b1dc906a14d5386323a543cb0034b4`。可信目录为 434,189 字节；候选应用实际占用 8,716 KiB，比当前已安装版 8,148 KiB 增加 568 KiB，约 0.55 MiB。本轮没有替换已安装版，也没有用真实用户需求完成前台搜索体验验收，因此还不能把自动测试通过写成搜索体验已经通过。
- ✅ 用户确认清理建议后，保留当前正式版、用户数据和最近一份 `SkillBox-before-import-fix-20260902-224800.app.backup` 回退包；其余 6 份过期 App 备份与约 719 MB 的可重建 `app/.build` 已一起移入废纸篓目录 `SkillBox-cleanup-20260903-035047`。其中也包含封装旧程序的本地 `0.2.2` DMG，避免后续误将它当成当前上线包。所有目标均未永久删除，清空废纸篓前仍可恢复。
- ✅ 清理后 `~/Applications` 仍只保留并运行正式 `SkillBox.app`，版本 `0.2.2 (Build 5)`，可执行文件 SHA-256 继续为 `a5d11e207fa9036a170faf5bc9452b862c20b2fda16c54825dfe6af0f8b39758`，严格签名与 Designated Requirement 通过。`catalog.json`、`assignments.json`、`installations.json` 和 `organization.json` 清理前后摘要完全一致，确认没有误动用户 Skill、分类或 Agent 安装关系。废纸篓尚未清空，因此约 764 MB 目前只是移出工作位置，尚未真正释放磁盘空间。
- ✅ 用户反馈替换后仍看到旧界面，现场进程证据确认实际运行的是 `~/Applications/SkillBox-before-intro-v4-20260902-170802.app`（`0.2.1 Build 4`），正式路径的新应用当时没有运行。根因是 7 份历史备份继续以 `.app` 形式放在 `~/Applications`，且都使用 `com.zhaoji.skillbox`，macOS 会把它们一起识别成可启动的 SkillBox，导致应用搜索或启动入口可能命中旧备份。
- ✅ 在确认所有 SkillBox 已关闭后，只注销并移动 7 份明确命名为 `SkillBox-before-*.app` 的旧备份；它们完整保存在 `~/Documents/SkillBox Backups/2026-09-03/`，并改为可恢复但不可直接启动的 `.app.backup` 后缀，没有删除。`~/Applications` 现在只保留唯一的 `SkillBox.app`。
- ✅ 正式版已重新登记并从精确路径启动，当前进程为 `~/Applications/SkillBox.app/Contents/MacOS/SkillBox`，版本 `0.2.2 (Build 5)`，可执行文件 SHA-256 仍为 `a5d11e207fa9036a170faf5bc9452b862c20b2fda16c54825dfe6af0f8b39758`，严格签名与 Designated Requirement 通过。真实窗口已回读 6 份全局 Skill，包括刚加入的 `wechat-layout-publisher`；本轮没有改写 Skill 数据或任何 Agent 目录。

## 2026-09-02

- ✅ 将提交 `0f9cad7` 对应的 Release arm64 候选按可恢复流程替换到 `~/Applications/SkillBox.app`。候选和正式安装版均为 `0.2.2 (Build 5)`，可执行文件 SHA-256 为 `a5d11e207fa9036a170faf5bc9452b862c20b2fda16c54825dfe6af0f8b39758`；包内隐私检查、严格签名与 Designated Requirement 均通过。被替换的旧安装版完整保留为 `~/Applications/SkillBox-before-import-fix-20260902-224800.app`，替换前后 `assignments.json`、`installations.json`、`catalog.json` 和 `local-source-state.json` 摘要一致，证明替换应用包没有改写用户 Skill 数据。
- ✅ 从正式安装路径真实启动新版，并用用户提供的开发目录 `wechat-layout-publisher` 完成本地添加闭环。文件夹选择器能够直接识别 Skill 根目录，核对页显示 `6 / 7` 项进入 SkillBox：必需的 `SKILL.md`、`assets`、`references`、`scripts` 保留，用户选择的 `.env.example` 与 `agents` 保留，`.gitignore` 留在开发项目中；后续同名确认和风险确认均正常完成，不再出现“没有找到 Skill”或“无法组成完整 Skill”。
- ✅ 添加完成后「我的 Skills」从 5 份变为 6 份，详情明确显示来源为“本地开发文件夹”、状态为“与开发文件夹一致”，并显示“还没有安装到任何 Agent”。目录来源精确记录为用户选择的 `公众号排版发布/wechat-layout-publisher` 开发目录；添加前后开发目录全量文件指纹同为 `027f084554f89e3325a0565f7fb311f1b1077847638c5ae0d20e00f9d0f3c767`，`assignments.json` 与 `installations.json` 摘要也完全不变，证明本轮只建立中央快照与更新来源，没有修改开发项目或向任何 Agent 安装 Skill。
- ✅ 严查“添加 Skill”后确认本地失败的根因：选择文件夹预览使用 128 MiB 手动上限，确认可用内容和后续更新检查却又退回 50 MiB，导致 `wechat-layout-publisher` 在第一步能识别、第二步却被判定为无法组成 Skill。现已用同一条 `userSelectedLocalSource` 规则贯穿预览、确认、导入、检查和重新关联；中途失败会保留扫描器的具体原因，不再统一抹成“没有 Skill”。
- ✅ GitHub 添加补齐网络恢复闭环：区分 TLS 安全连接失败、断网与超时；短暂 TLS 和超时在保持系统证书验证的前提下只自动重试一次。仍失败时保留仓库地址与跟踪方式，弹窗明确说明没有改动 Skill，并可直接点“重试”。TLS、断网、超时和不重填仓库的一键重试四条定向回归均已通过。
- ✅ 清理两处与当前产品定位冲突的旧逻辑：本地开发源不再出现“只导入这一次”，添加后必然建立来源关联；扫描本机安装情况时不再读取 `~/.agents/skills` 或 `~/.config/agents/skills`，只看已配置 Agent 自己的 Skill 目录。失败也不再发送“操作未完成”的绿色对勾提示，新错误会立即清掉上一条成功轻提示。
- ✅ 本轮完整门禁通过 310/310 项（主测试 274 项、GitHub 来源隔离执行 36 项），`git diff --check` 通过。Release arm64 候选 `0.2.2 (Build 5)` 已重新构建，`Info.plist`、ad-hoc Hardened Runtime 签名、Designated Requirement 和包内隐私检查均通过；可执行文件 SHA-256 为 `a5d11e207fa9036a170faf5bc9452b862c20b2fda16c54825dfe6af0f8b39758`。隔离的 QA 资料库已真实打开“从电脑添加”，确认页只保留内容核对与“添加并跟踪更新”，没有点击最终添加，也没有替换已安装应用或改写用户 Skill 数据。
- ✅ 修复手动添加本地开发 Skill 时的误报。用户选择的 `wechat-layout-publisher` 根目录本身有可读的 `SKILL.md`，但整份 Skill 的普通文件总量约 66.6 MB，超过旧实现复用的 50 MiB 自动扫描上限；扫描器已经给出“文件总体积超过上限”，本地来源入口却丢掉了这条诊断，最终错误显示成“没有找到 Skill”。
- ✅ 按公开添加入口先写两条失败回归，再完成修复：用户主动选择的本地开发源允许单份 Skill 最多 128 MiB，启动时的自动扫描仍保持 50 MiB；选中项本身是 Skill 根目录时保留空相对路径，读取失败则显示具体原因并去掉绝对路径和不适用于手动操作的启动提示。64 MiB Skill 根目录确认和错误透传两项定向测试均已转绿，`app/Scripts/test-all.sh` 完整门禁通过 302/302 项。
- ✅ 用户确认当前体验通过并授权提交、检查上线准备。已完成覆盖本次功能变更、凭据、网络、文件写入、安装事务和发行包边界的安全差异审查，7 类风险面全部检查完毕，没有发现可报告的安全漏洞；审查编号为 `1abad84e-241d-4cb9-a2a3-e0e8843d1ca9`。该结论只覆盖当前变更，不冒充第三方 AI 服务、GitHub 账号安全或另一台干净 Mac 的真实安装证明。
- ✅ 提交前补抓到一处失败提示插值错误：普通 Agnes 错误原本会把 `failure.message` 当成字面文字展示。现已改为显示真实错误原因，同时继续说明已有介绍会保留；回归覆盖真实错误、旧介绍保留和后续重试，定向测试 7/7、完整发布测试清单 300/300 均通过。
- ✅ GitHub 已存在正式版 `0.2.1 (Build 4)`，本次大改不能继续复用同一发行编号。新的待发布候选已提升为 `0.2.2 (Build 5)`，用于生成独立 DMG 和更新身份；当前步骤只准备并提交候选，不上传 GitHub、不创建标签或 Release。
- ✅ 经用户明确授权，对 `zhaoji-writing` 执行一次与真实「获取 Skill 介绍」相同的受控诊断。Agnes 两次均返回 HTTP 200，鉴权、接口和模型可用；真实失败为 `finish_reason=length`，原逻辑先给 900 Token，截断后又用剩余 300 Token 从头重试，最终两轮累计输入 15,226 Token、输出 1,200 Token 仍无完整 JSON。诊断未输出 API Key 或 Skill 正文，临时诊断入口已清理。
- ✅ 按根因修复 Skill 介绍请求：每次用户点击只发一轮，直接使用 2,400 Token 完整输出上限，去掉被截断后用更小额度重复发送整份资料的无效重试。上限只是允许模型生成的最大长度，模型提前完成时不会固定消耗满额。
- ✅ 介绍被截断时会直接提示「Agnes 返回的介绍不完整」，并保留已有介绍；其他安全的领域错误也不再被统一抹成同一句泛化失败。两条针对性回归已先在旧逻辑上失败、修复后转绿；`app/Scripts/test-all.sh` 完整门禁通过 300/300 项。
- ✅ Release arm64 候选已重新构建，`Info.plist`、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私与仓库本机路径检查、`git diff --check` 通过；可执行文件 SHA-256 为 `7a3711d42667cfc0d28c8e06b4a103c09cc59f61c44d302d5636338a490d7516`。已按同卷可恢复流程替换到 `~/Applications/SkillBox.app`，安装后摘要与候选完全一致、签名复核通过，并已从正式路径启动。替换前的安装版保留为 `~/Applications/SkillBox-before-guide-fix-20260902-175958.app`，其可执行文件 SHA-256 为 `2d0c2acf543b92c9eec348089c6476ff71c326f66f2b0adcf808115171bf2fa2`。本次替换没有改写 Skill 库或 Agent 目录，修复后也尚未再调用用户真实 Agnes API。
- ✅ 经用户明确授权，已将包含 `usage-guide-v4` 的 Release 候选替换到 `~/Applications/SkillBox.app`。替换前先在同目录完成候选整包暂存、严格签名和摘要校验，再把原安装版保留为 `~/Applications/SkillBox-before-intro-v4-20260902-170802.app`。新安装版为 `0.2.1 (4)`，可执行文件 SHA-256 为 `2d0c2acf543b92c9eec348089c6476ff71c326f66f2b0adcf808115171bf2fa2`；旧备份摘要为 `c4a8549bec69d4437c65f4e87c765f18d2969d0b8ea47f61719b872fd50cbafe`。新应用已从正式安装路径启动，运行进程路径为 `~/Applications/SkillBox.app/Contents/MacOS/SkillBox`。本次只替换应用包，没有点击「获取 Skill 介绍」、没有调用用户 AI API，也没有改写 Agent Skill 目录。
- ✅ Skill 介绍生成合同升级为 `usage-guide-v4`：「作用」改为直接说用户提供什么、Skill 做什么和最后交付什么；「适用场景」只写具体开始时机或眼前麻烦；「使用流程」提升为介绍主体，可写 1 至 7 步，并在作者材料已明确时保留提问内容、选择项原名、可选值、回答格式和交付物。
- ✅ 启动提示词改为只负责触发 Skill 和表达大致目标，默认不替用户预选答案，不锁定流程、方法、语气、数量或输出格式。本地作者材料保底现在能识别「使用流程」「交互流程」和 `workflow`，最多保留 7 步及其中的具体回答格式。旧提示词版本下的完整介绍继续作为生成失败时的同内容保底，不会被突然删掉。
- ✅ 发现详情中的旧标题已与「我的 Skills」统一为「作用、适用场景、使用流程、启动提示词」；主界面的空状态、生成中状态和按钮悬停说明统一使用「AI／AI 服务」，不再在主功能入口绑定 Agnes 名称。
- ✅ 新增的写作合同断言和“固定选择格式不丢失”回归已通过；`app/Scripts/test-all.sh` 完整门禁通过 299/299 项（主流程 267 项、GitHub 来源隔离组 32 项）。Release arm64 候选已重新构建，Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私和 `git diff --check` 通过，可执行文件 SHA-256 为 `2d0c2acf543b92c9eec348089c6476ff71c326f66f2b0adcf808115171bf2fa2`。构建仍保留原有的一条钥匙串非交互保护 API 弃用警告。
- 📌 最终 Release 候选已打开到「我的 Skills」完成前台结构与中性 AI 入口检查。当前屏幕上仍可能显示同一 Skill 以前保存或作者材料保底的旧文字；只有用户主动点击「获取 Skill 介绍／重新获取」才会按 v4 新标准请求一次。本轮为了不产生用户 API 费用，没有点击该按钮、没有发送真实 Skill 文字；也没有替换已安装版、提交或发布 GitHub。
- ✅ 设置目录与详情标题的“AI 服务”图标由含义偏泛的闪光改为紫色 AI 大脑；“GitHub”由代码括号改为项目内已有、可追溯的 GitHub 官方章鱼猫矢量标志。两处统一复用 `SettingsPageIcon`，选中态自动使用白色图标，普通态随系统明暗模式保持清晰；新增图标合同回归，避免以后退回字母或近似品牌占位。
- ✅ `app/Scripts/test-all.sh` 完整门禁再次通过 299/299 项；Release arm64 候选重新构建并通过 Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私与 `git diff --check`，新可执行文件 SHA-256 为 `9ff8f79ce2fa7b7d187f5cd4940b284735624a5f22a5a54eddf48e3ea2ea1f7d`。真实 Release 应用已重启，AI 服务与 GitHub 两页均完成前台视觉核对；本轮未替换已安装版、提交或发布 GitHub。
- ✅ 修复正式设置页中间目录栏过暗的问题：移除会在当前 macOS 材质下落成深灰遮罩感的 `underPageBackgroundColor`，改用与窗口一致的明亮背景；五个设置入口补齐稳定浅色卡片、细边界和轻微层次，选中态收成轻蓝底与清晰蓝色描边。设置结构、真实状态摘要和右侧功能均未改变。
- ✅ `app/Scripts/test-all.sh` 完整门禁通过 299/299 项（主流程 267 项、GitHub 来源隔离组 32 项）；Release arm64 候选重新构建，Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私与 `git diff --check` 均通过，新可执行文件 SHA-256 为 `862a84e377d04caae77597c3d18068e394e0a9bb56e8f9048d55255f3e195615`。真实 Release 应用已重启并打开设置页完成前台视觉核对，中间目录栏为明亮浅底、选中和未选中入口均清楚可辨；本轮未替换已安装版、提交或发布 GitHub。
- ✅ 用户确认 v31 后，正式设置页已从“顶部分段按钮＋长 Form”替换为“设置目录＋当前详情”双栏。目录固定为「AI 服务、GitHub、存储与记录、操作记录与恢复、隐私与安全」，每行显示连接数、仓库数、寻找记录、最近操作或安全范围等真实摘要；原「通用」「数据」含糊分类已删除，操作记录保留完整恢复能力并纳入同一套视觉层级。
- ✅ AI 页先说清「Skill 介绍」固定使用 Agnes、「发现 Skills」当前使用哪家服务，再进入各服务连接管理；点击某家服务现在只选择“正在管理谁”，不会暗中改掉发现功能的服务。正式界面不再使用服务商首字母伪装品牌图标，改用中性的系统功能图标，Agent 产品列原有的可追溯官方图标保持不变。Spec 升级为 v2.20，并新增 v31 五项目录、热区和尺寸合同回归。
- ✅ 设置页专项编译和 `NavigationAndAgentCatalogTests` 10/10 项通过，`app/Scripts/test-all.sh` 完整门禁通过 299/299 项；项目内 Debug App 完成真实窗口验收，五个设置页均可进入，文字换行、滚动、连接状态、寻找记录和恢复入口正常。本轮没有测试真实 API、删除记录、断开 GitHub 或执行恢复，也没有替换已安装版。
- ✅ Release arm64 候选构建、Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私扫描和 `git diff --check` 通过；新可执行文件 SHA-256 为 `68ad4ee1629fd0edc1307ec0a2d441f550086be64a672120c7193873c45de430`。候选只保存在项目内，没有安装、提交或发布。
- ✅ “我的 Skills”详情中的服务商化入口已收口为中性产品语言：原“Agnes／连接 Agnes”改为“AI 介绍／连接 AI”，未连接提示和悬停说明同步改为“连接 AI 服务”。设置页继续展示各家真实服务商名称；本轮只调整入口表达，不擅自改变已确认的 Skill 介绍服务选择逻辑。Spec 升级为 v2.19，并新增界面文案回归。`app/Scripts/test-all.sh` 完整门禁再次通过 298/298 项；Release 候选构建、严格签名与隐私检查通过，新可执行文件 SHA-256 为 `711dd04533ee08ccb2f540c0101408e7fd0592e086a82346fafc638f270703cf`。
- ✅ 钥匙串读取已拆成两条明确路径：启动、状态恢复、后台检查和自动发现继续使用“禁止弹窗”的静默读取；只有用户点击“保存并测试”或“获取 Skill 介绍”时，才使用允许 macOS 显示授权窗口的主动读取。主动确认成功后直接复用钥匙串中的原 API Key，不要求重新粘贴；取消或拒绝时不调用 Agnes，并提示用户重试时选择“始终允许”。
- ✅ 新增“主动读取可以授权”和“取消钥匙串确认零发送”两项回归，并在连接测试中断言主动读取入口；冷启动零 Key 读取回归继续通过。`app/Scripts/test-all.sh` 完整门禁通过 298/298 项（主流程 266 项、GitHub 来源隔离组 32 项），`git diff --check` 通过。Spec 升级为 v2.18，ADR-0014 同步钥匙串合同。
- ✅ Release arm64 候选重新构建，Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement 与包内隐私检查通过；新可执行文件 SHA-256 为 `03247c7f648e4473e71d0d222acd9713b239540cebfb2c85c6f0424611ccce0c`。构建仍保留原有的一条钥匙串非交互兜底 API 弃用警告。
- 📌 本轮没有读取或改写真实 API Key，没有启动候选、替换本地使用版或发布 GitHub。真实“旧版本已授权 → 安装新 ad-hoc 版本 → 首次主动使用确认一次 → 后续不再询问”仍需要安装边界获准后，由用户在 macOS 系统授权窗中完成前台验收。
- ✅ 用户明确纠正 Agnes 产品模型后，已将 Skill 介绍改为“用户自有 API + 逐份主动获取”：用户在“设置 → AI”中填写并测试自己的 Agnes API Key，只有在当前 Skill 详情中点击“获取 Skill 介绍”才读取脱敏说明文字并发起请求。打开、切换、新增和内容变化均不再自动请求。
- ✅ 删除上一方案的首次授权弹窗、自动批量整理、`BuiltInUsageGuideService`、SkillBox 中转端点与发布配置。Skill 介绍固定读取 Agnes 配置和用户钥匙串 Key，不跟随“发现 Skills”中选择的 DeepSeek 或其他服务商；失败保留旧介绍、不静默切换，用户可再次点击重试。
- ✅ 正式 SwiftUI 现在按连接状态显示“连接 Agnes”、“获取 Skill 介绍”或“重新获取”；未连接时直达 AI 设置，请求中立即显示进度且保留旧介绍。Spec 升级为 v2.17，ADR-0014 取代 ADR-0013。
- ✅ 新增 5 项手动 Agnes 回归，覆盖打开零发送、显式点击、固定 Agnes Key、未连接零发送、失败保留与主动重试、并发去重。`app/Scripts/test-all.sh` 完整门禁通过 296/296 项；Release arm64 构建、Info.plist、ad-hoc Hardened Runtime 严格签名、包内隐私检查和 `git diff --check` 通过，新可执行文件 SHA-256 为 `d73138e9cbac767caf0c4255a7c449d948bb6361d35011d9a062aa4503cea52f`。仍保留一条钥匙串非交互保护 API 的弃用警告。
- 📌 本轮只生成项目内 Release 候选，没有启动、没有替换本地使用版、没有调用真实 Agnes API，也没有提交、发布 GitHub。用户之后在设置中填写的 Agnes Key 将直接用于本人账号的请求和额度。
- ✅ 用户确认 v30 后，已将 Skill 介绍从“用户自配 AI、打开时才整理”切换为内置 Agnes 2.5 Flash 基础能力。首次只展示一次精简数据去向提示；同意后自动补齐现有、以后新增及内容变化的已管理 Skill，“暂不用”不反复弹出，关闭后保留已有介绍。
- ✅ 新增可替换的 `BuiltInUsageGuideService`、版本化无 Key 请求合同、HTTPS 限制、512 KiB 流式响应上限、一次同意持久化、按 Skill ID／指纹／提示词版本缓存、同会话失败不反复请求和不静默切换 DeepSeek 边界。正式 SwiftUI 增加 v30 授权卡、顶部整理进度、详情原地整理中状态，设置中将“Skill 介绍服务”与“发现 Skills 的 AI 助手”分开。
- ✅ 新增 7 项 Agnes 行为与安全回归：现有／新增 Skill 只生成一次、拒绝零发送、关闭保留缓存、失败保留完整旧介绍、请求结构不含 API Key、HTTP 端点不可用、HTTPS 客户端不发送 Authorization 头。测试清单现为 298 项，全部通过分组或逐条验证；其中旧 `SourceProviderTests` 32 项合并在同一测试进程时会长时间不退出，32 项逐条均通过。因此当前可确认全部断言通过，但不将一键聚合运行器写成已通过。
- ✅ Spec 升级为 v2.16，新增 ADR-0013，并标明 ADR-0008 中被取代的部分。Release arm64 生产构建、Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私检查和 `git diff --check` 通过；可执行文件 SHA-256 为 `0e757d822e156b055eddbf690d35b902ee9b0b00418a5961e6f5d4a04b2d5bd4`。构建仍保留一条钥匙串非交互保护 API 的弃用警告。
- 📌 当前 Release 候选的 `SkillBoxUsageGuideServiceURL` 仍为空，所以客户端会如实判定内置介绍服务尚未可用，不会上传任何 Skill、直连 Agnes 或伪造生成成功。要对真实用户开启，还需建设并独立验收 SkillBox 转发服务的鉴权、限流、费用上限、日志脱敏和原文不保留。本轮没有启动候选、没有替换本地使用版、没有提交、发布或部署服务。
- ✅ 修复「Skill 介绍」实际只剩“作用”的根因：同一内容指纹下已保存的 v2 四段说明原本被 v3 提示词版本门整份拒绝，界面只能退回不完整的本地提取。现在优先使用当前提示词版本；需要升级或 AI 不可用时，保留同一 Skill 内容中更完整的旧版说明作为保底，不再丢掉“适用场景、使用流程、启动提示词”。
- ✅ 风险确认状态现在主动通知 SwiftUI 刷新，用户点击「我已了解」后，当前 Skill 不用切走再切回就会立即从橙色待确认变为绿色已确认。新增了一项直接监听界面变化通知的回归，避免只验证数据而漏掉实际刷新。
- ✅ 「Skill 介绍／文件详情」改为各占一半宽度、整块 46 pt 高区域可点，不再只命中文字。一级侧边栏改为完整导航容器：四个入口都有整行热区、非选中边界、悬停反馈和清晰选中态，顶部品牌区与底部范围卡收住原先没有目的的留白。设计说明已同步到 `design/ui/skillbox-global-library-v28.md`，新视觉预览为 `design/ui/skillbox-global-library-v28-sidebar-tabs.png`。
- ✅ 新增 2 项功能回归并扩充交互尺寸验收；`app/Scripts/test-all.sh` 完整门禁通过 291/291 项（主流程 259 项，GitHub 来源拆分组 32 项）。Release arm64 生产构建 36.76 秒通过，可执行文件 SHA-256 为 `3ba4bbd32a70638bfc0de9b06613b3e2434ea672ea8ce1cebc294e042d0cbba2`；Info.plist、ad-hoc Hardened Runtime 严格签名、Designated Requirement、包内隐私和 `git diff --check` 均通过。构建仍保留一条钥匙串非交互保护 API 的弃用警告。
- 📌 本轮只生成项目内 Release 候选 `app/.build/release/SkillBox.app`，未启动候选、未替换 `~/Applications/SkillBox.app`、未改写真实 SkillBox 数据或任何 Agent 目录，也未提交或发布 GitHub。当前结论是代码、回归、视觉预览和生产包校验通过，不冒充已安装版的真实前台体验。
- ✅ 根据用户对正式版的直接体验纠正了两处“原型已确认、正式实现却没有按标准落地”的偏差。安装表不再使用系统拖放的松手后跳位：表头超过 7 pt 后生成独立材质浮层并一比一跟手，原列留下蓝色占位，整列随目标槽位实时让位；靠近左右边缘自动滚动，跨槽位有轻触觉反馈，拖出当前可见表格、按 Esc、窗口／应用失焦或目标消失都会取消，只有有效松手才一次性保存最终顺序。
- ✅ 「我的 Skills」去掉压在左右分栏上方的旧整页标题，左侧收成独立 Skill 库与搜索／分类入口；右侧从顶部依次呈现名称与管理安装、来源／当前安装版本／版本情况三张摘要、横向安装情况、下划线式「Skill 介绍／文件详情」。介绍严格使用「作用、适用场景、使用流程、启动提示词」四种中性浅灰卡，正常来源不再占据大块维护卡，只有可更新或来源异常时才展开操作提示。
- ✅ 新增 3 项 Agent 拖动会话、取消和 v28 详情层级回归；完整门禁自动发现并通过 289/289 项测试（普通组 257 项，GitHub 来源拆分组 32 项）。Release arm64 候选重新构建，Info.plist、ad-hoc Hardened Runtime 严格签名、包内隐私和 git diff --check 均通过。
- 📌 新候选仍只位于项目内 app/.build/release/SkillBox.app。为了不让开发候选读取或改写真实使用数据，本轮没有启动它，也没有替换 ~/Applications/SkillBox.app、写入任何 Agent 目录、提交或发布 GitHub；正式前台手感仍需在隔离数据或用户明确授权替换后验收。
- ✅ 用户确认 v28 后已将正式 SwiftUI 一级导航收口为「我的 Skills → 安装到应用 → 发现 Skills → 设置」；应用打开后直接进入「我的 Skills」。旧的总览看板与冲突中心已从正式代码删除，操作记录改从「设置 → 操作记录与恢复」进入。
- ✅ 正式「我的 Skills」现在突出 GitHub／本地唯一来源、版本状态、官方 Agent 图标与安装关系；右侧详情拆为「Skill 介绍」和「文件详情」，介绍顺序统一为「作用、适用场景、使用流程、启动提示词」；单个 Skill 可快速全部安装、全部卸载或逐应用管理。
- ✅ 正式「安装到应用」只保留「安装、已安装、可更新、已有同名、应用不可用」五种状态；Skill 列可直接按全部、真实文件夹或未分类筛选，Agent 列支持拖动与恢复默认顺序。「管理应用」支持移出、加回、恢复全部预设应用，以及添加、编辑和删除自定义应用；存在受管理副本时会明确说明「保留文件并停止管理」的后果。
- ✅ 新用户默认 Agent 收口为 GPT、Claude、WorkBuddy、ZCode、Kimi Code、Cursor、HanaAgent、Pi、DeepSeek Harness 和 Trae，全部按自身专属 Skills 目录判断。界面显示 GPT，底层继续使用稳定的 `codex` 类型、UUID 和 `~/.codex/skills`；Kimi Code 改为 `~/.kimi-code/skills`，DeepSeek Harness 支持 `DSH_HOME/skills` 并以 `~/.dsh/skills` 作默认值。老用户已有 Gemini CLI、OpenCode 和自定义目标保留，新用户不再默认出现它们。
- ✅ 新增 7 项 v28 导航、Agent 目录、老数据兼容、排序恢复与移出保护回归；`app/Scripts/test-all.sh` 完整门禁自动发现并通过 286/286 项测试（普通组 254 项，GitHub 来源拆分组 32 项）。Release arm64 生产构建 39.94 秒通过，十一份可追溯 Agent 官方图标已进入应用包，`Info.plist`、ad-hoc Hardened Runtime 严格签名、包内隐私检查与 `git diff --check` 通过。构建仍保留一条钥匙串非交互保护 API 的弃用警告，本轮未移除这层防密码弹窗保护。
- 📌 当前仅生成项目内 Release 候选 `app/.build/release/SkillBox.app`；未启动候选、未替换 `~/Applications/SkillBox.app`、未写入真实 Agent 目录、未提交或发布 GitHub。真实前台体验、已安装版替换与公开发行仍是后续独立门。
- 🎨 按用户真实目标重排 v28 一级导航为「我的 Skills、安装到应用、发现 Skills、设置」；核心管理任务连续放在前面，附加的 Skill 发现与设置轻微分隔。原型、设计说明和 Spec v2.13 已同步。
- 🧭 「最近操作」退出一级导航，但没有删除这项安全能力：每次安装、更新、卸载或删除仍就地给出结果和即时撤销，更早记录、备份状态与恢复入口统一进入「设置 → 操作记录与恢复」。正式 SwiftUI 尚未替换，继续等待当前母版视觉确认。

## 2026-09-01

- 🎨 用户确认后补齐预设应用的批量恢复出口：“管理应用”新增「恢复全部预设应用」，可以一次找回所有被移出的十个默认 Agent，只改变 SkillBox 显示列表；自定义应用、安装关系、当前列顺序和磁盘文件保持原样。安装表原「恢复默认」改为「恢复默认顺序」，Spec 升级至 v2.12，避免用户把恢复排序误解为恢复应用。
- 🎨 v28 安装矩阵扩充为 10 个默认 Agent 产品：GPT、Claude、WorkBuddy、ZCode、Kimi Code、Cursor、HanaAgent、Pi、DeepSeek Harness、Trae；全部继续使用官方图标和产品名。新增“管理应用”、移出／加回应用、自定义应用名称与已有 Skills 文件夹选择，以及存在受管理副本时的安全提示原型。
- ✅ 核对官方资料与本机状态后拆开品牌名称、稳定适配器和真实路径：Codex App 合并进 ChatGPT 桌面应用属实，但 Codex 仍是官方独立视图；界面按需求显示 GPT，底层保留 `codex`、固定 UUID 与 `~/.codex/skills`。同时发现现有 Kimi Code 路径 `~/.kimi/skills` 已落后于官方当前 `~/.kimi-code/skills`，已在 Spec v2.11 与 ADR-0012 写入显式迁移规则，尚未改动正式 SwiftUI 和用户文件。
- 🎨 v28 视觉母版的 Agent 识别全部换成官方图标：Codex、WorkBuddy 和 HanaAgent 来自本机官方 App 包，Claude Code、Cursor、Gemini CLI 和 ZCode 来自官方品牌资源或官网；已记录每个文件的可追溯来源，删除字母、自制 SVG 和近似品牌占位。
- 🎨 「我的 Skills」详情标题收口为「作用」和「适用场景」。依据 `vibe-project-foundation` 真实 `SKILL.md` 重写作用、适用时机和用户流程，直接写清项目说明、AI 协作规则、基础目录和 Git 备份等交付，删除「想清楚」「不容易乱」等空泛效果语。Spec 升级至 v2.10。
- ✅ 浏览器自动检查与原图回看通过：7 个 Agent 列头均为可正常加载的官方图标且同时显示产品名；「作用／适用场景」标题和具体交付文案存在，旧标题、空泛文案与字母占位不存在；全部 5 行、内容创作 2 行筛选、列拖动持久化和页面脚本零错误仍通过。正式 SwiftUI 仍等待本轮视觉确认。
- 🎨 安装矩阵进一步按真实目录收口：删除“已可用／间接可见”及 `.agents` 共用目录判断，内置 Agent 只检查自己在路径合同中的标准 Skill 目录；WorkBuddy、ZCode 在自身目录未安装时直接显示“安装”。Agent 列头增加产品图标、产品名称与“已找到／未找到”状态，单 Skill 管理弹窗同步使用同一套表达。Spec 升级至 v2.9。
- ✅ 重新渲染集中安装表、分类菜单与单 Skill 安装弹窗。浏览器自动检查确认：界面不存在“已可用、间接可用、间接可见、共用目录、通用目录”，五种图例与 7 个 Agent 产品图标完整显示；内容创作筛选 2 行、全部筛选 5 行，列拖动及顺序保存通过，页面脚本零错误。正式 SwiftUI 仍等待本轮视觉确认后再替换旧的共用目录模型。
- 🎨 安装矩阵按用户反馈继续收口：删除独立文件夹侧栏，分类选择器直接进入 Skill 列标题，可选择全部、真实文件夹或未分类；单元格由文字胶囊改为六种图标状态，并补回蓝色“可更新”。“间接可用”改为“已可用”，“比较内容”改为“已有同名”，矩阵不再出现没有具体原因的“需要确认”。Spec 升级至 v2.8。
- ✅ 浏览器验证确认：内容创作筛选显示 2 行，切回全部显示 5 行；六种图例与“可更新”交叉点均可见，单元格没有重复长文字，旧的三个含糊词在矩阵中完全消失；Agent 列拖动及刷新后排序保持继续通过，页面脚本零错误。新增分类菜单展开态 PNG，并重新生成集中安装表和单 Skill 安装弹窗。
- 🎨 用户认可 v28“我的 Skills”整体方向后，进一步明确两类工作区：“我的 Skills”用于查看 Skill、来源、版本、文件结构和 AI 整理的用户说明，并提供全部安装、全部卸载与逐应用管理；“安装到应用”用于跨 Skill 集中管理安装关系。Spec 升级至 v2.7，两处共用同一份安装关系，不制造第二套状态。
- 🎨 v28 交互母版已增加醒目的 GitHub／本地来源图标、用户文件夹筛选、AI 大白话介绍、用户视角使用流程、可复制启动提示词和独立文件树；安装表格按“我的 Skills”文件夹筛选行，Agent 列可整列拖动并持久保存顺序，新 Agent 默认放在末尾。
- ✅ 浏览器交互检查通过：来源图标、介绍／文件切换、文件夹与搜索组合筛选、Agent 列拖动、刷新后顺序保持均正常，页面脚本零错误；主页面、文件详情、单 Skill 安装弹窗和集中安装表均已生成 PNG。正式 SwiftUI 仍等待这轮视觉确认。
- ✅ 产品模型按第一性原理收口：SkillBox 只管理跨项目使用的全局 Skill，每份 Skill 只允许“本地开发文件夹”或“GitHub 仓库”一个来源；项目级 Skill 完全退出范围，Agent 目录只作为安装目标。规格升级到 v2.6，并新增 ADR-0011；内部保存内容统一定义为一次安装使用的不可变快照，不再作为用户原件。
- ✅ 先新增公开行为回归，证明旧实现仍会把 Agent 目录导入为受管理来源；随后在 `LibraryStore.importCandidate` 增加来源边界，仅允许本地开发文件夹与 GitHub，目标用例转绿。历史 `agentDirectory` 记录暂时只读保留，避免无提示丢失用户数据，后续通过明确迁移流程重新关联来源。
- 🎨 完成消费级 v28 视觉母版：打开应用直接进入“我的 Skills”，一级导航只保留“我的 Skills、发现 Skills、最近操作、设置”；主页面只围绕 Skill 列表、唯一来源和多应用安装关系，删除总览看板、冲突中心与 Agent 检测矩阵。添加入口只提供两个来源，安装弹窗只处理当前 Skill 的应用选择。正式 SwiftUI 尚未替换，等待用户确认母版。
- ✅ 当前 Swift 包完整测试 20 个测试组、279 项全部通过，`git diff --check` 通过。现阶段只落地来源入口的底层硬边界和设计依据；旧版“停止跟踪／清除来源”迁移、正式导航与页面替换仍须在母版确认后作为下一实现切片完成。
- ✅ `v0.2.1 (Build 4)` 已完成正式发布。产品提交与标签均指向 `d59f82bc691f096ea49ca5a6b25e2cf0a7e30088`；GitHub Latest Release 为公开、非草稿、非预发布的 `v0.2.1`，包含 `SkillBox-0.2.1.dmg`、`.sha256` 与 `-release.json`。完整发行门自动发现并通过 278 项测试；Release arm64 构建、ad-hoc Hardened Runtime 严格签名、包内隐私、图标系统回读、DMG 完整性与来源清单均通过。
- ✅ GitHub 附件已重新下载并只读挂载复核。DMG SHA-256 为 `f96310a2d3b059a56758a95e3a28432524ff2a4626ed744ec9993604a338ca42`，发布清单 SHA-256 为 `7ee485859a1f9607ccc5b5a7d99c4ba9a3bc44c1407cb84aadd9462e3cbd8138`；下载文件与本地产物逐字节一致。包内 App 为 `0.2.1 (4)`，可执行文件 SHA-256 为 `c4a8549bec69d4437c65f4e87c765f18d2969d0b8ea47f61719b872fd50cbafe`，严格签名与发布隐私门再次通过。
- ✅ GitHub 下载副本已按同卷暂存、复核、可恢复切换的方式安装到 `~/Applications/SkillBox.app`；被替换的 `0.2.0 (3)` 整包保留为 `~/Applications/SkillBox-before-v0.2.1-20260901-101212.app`。新正式版已从准确安装路径启动，运行版本、构建号、签名和可执行摘要均与 GitHub DMG 一致。
- ✅ 安装前后用户数据边界已复核：5 个 Skill、3 份安装关系和 3 份分配关系均保留；组织状态继续为 `folders=0`、5 个 placement 全部没有 `folderID`，“操盘”没有恢复。启动只刷新目录快照类文件，没有改变组织、安装或分配事实。
- 📌 GitHub 正式版、本地开发版与本地使用版的产品运行内容现已对齐到 v0.2.1。另一台干净 Mac 的浏览器下载、隔离属性与“仍要打开”仍是独立的陌生用户安装体验门；本轮没有把本机验收冒充为该门已通过。
- ✅ 修复“幽灵分类”：真实数据中存在一个名称为“操盘”、数量为 0 的历史分类；旧界面主列表会过滤空分类，右键“移动到”却列出全部分类，造成用户从未见过或管理过该分类却在菜单中突然看到。展示策略现保留全部已保存分类，空分类显示 `0` 并继续提供重命名、删除入口；新增回归先因缺少空分类展示策略编译失败，最小实现后转绿。
- ✅ 经用户授权，已精确清理真实使用数据中唯一的空“操盘”分类 `2A9F06F3-0BFE-47EA-BABD-189215C14DC8`。操作前保存 `organization.json` 与完整 `library-state.json` 的两代可恢复备份；候选按最新状态重新生成并逐字段对比，5 个 Skill 的顺序、来源、安装和其余状态未变化。正式 `~/Applications/SkillBox.app` 重新启动后，两份状态均保持 `folders=0` 且没有非空 `folderID`，没有恢复该分类。
- ✅ `app/Scripts/test-all.sh` 完整门禁通过，新增“已保存空分类仍可见”用例；Release arm64 构建、ad-hoc Hardened Runtime 与 `codesign --verify --deep --strict` 通过。当前修复版仅形成项目内 Release 候选，尚未替换正式安装版、未发布 GitHub；正式安装版的数据异常已经清理，但以后新建空分类保持可见的程序修复仍须独立安装与发布。
- ✅ 将已经完成真实拖动验收的本地工作区冻结为 `v0.2.0 (Build 3)`，发布提交为 `1b43cad89933181cf1b8cba39cc9ef697ca79faa`。提交包含「我的 Skills」单行来源与大小、同组拖动占位／让位、独立落点线、右键移动与可恢复删除，以及相应测试和 v22–v26 视觉依据；仓库内的个人绝对路径已改为通用 `~/Applications` 表述。
- ✅ 最终发布门重新发现并执行 277 项自动化测试，全部通过；Release arm64 构建、ad-hoc Hardened Runtime 严格签名、包内隐私、应用图标系统回读、DMG 系统完整性、来源清单和隔离属性模拟均通过。隔离副本仍按正式策略得到 `manual-approval-required`，没有冒充 Developer ID 或 Apple 公证包。
- ✅ GitHub `master`、`v0.2.0` 标签和 Latest Release 均回读到提交 `1b43cad…`；Release 为公开、非草稿、非预发布，包含 `SkillBox-0.2.0.dmg`、`.sha256` 与 `-release.json`。从 GitHub 重新下载后的 DMG SHA-256 为 `7b9a2ea76c62c1828f338716217853a3061f3cbd0cd5d4f5f1a75375e97e8bed`，清单 SHA-256 为 `508e2124f0394e46c8acc3d08ed246f9d23d39fe4d3ce3bedb67f91ebed8dc97`，二者均与线上摘要和校验文件一致。
- ✅ 只读挂载重新下载的 GitHub DMG 后，包内 App 为 `0.2.0 (3)`，可执行文件 SHA-256 为 `7392dea51b3e2f2a32fbb103ba1858c5cf1d4faebe5a10e7fdd7ef1ba7522def`，严格签名和发布隐私门再次通过。该 GitHub 下载副本已复制到暂存路径并二次校验，再可恢复切换到 `~/Applications/SkillBox.app`；旧 `0.1.0 (2)` 整包保留为 `~/Applications/SkillBox-before-v0.2.0-20260901-004221.app`。
- ✅ 新正式版从 `~/Applications/SkillBox.app` 启动成功，进程路径准确；真实窗口回读为「我的 Skills」，显示 5 个 Skill、GitHub／本地来源、中央库大小与拖动整理提示。安装没有改动 `Application Support` 中的中央 Skill、排序数据或任何 AI 应用目录。
- 📌 三端产品运行内容已经对齐到 v0.2.0。仍未把“另一台干净 Mac 通过浏览器下载并走一次隔离后的‘仍要打开’”冒充已验收；这属于面向陌生用户安装体验的独立门槛，不影响本机正式版、GitHub 附件和来源提交的一致性。

## 2026-08-30

- ✅ 用户明确要求清理过期版本并强调不得误删后，先对个人 Applications 目录中所有 `SkillBox*.app` 逐个核对名称、容量、修改时间、版本号、可执行文件 SHA-256 与签名；同时检查 `/Applications` 和正在运行的进程路径。正式版只有 `~/Applications/SkillBox.app`，未发现系统 Applications 中的另一份或指向历史版的链接。
- ✅ 只将 6 个精确命名的历史包移入 macOS 废纸篓：`SkillBox-backup-iconfix-20260821-115857.app`、`SkillBox-before-placeholder-20260829-073300.app`、`SkillBox-before-reorder-runtime-20260829-082414.app`、`SkillBox-before-v26-20260829-001559.app`、`SkillBox.backup-20260828-014342.app`、`SkillBox.backup-20260828-224135.app`。未清空废纸篓，6 个名称均已通过 Finder 回读，仍可恢复。
- ✅ 清理后 `~/Applications` 中只剩正式 `SkillBox.app`；其可执行文件 SHA-256 仍为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，严格签名和 Designated Requirement 复核通过。项目源码、构建目录、Skill 内容、排序数据与 AI 应用目录都未纳入本次清理。

## 2026-08-29

- ✅ 用户完成候选版真实拖动验收并明确要求“替换”后，已将修复拖动命中边界的 Release 候选可恢复安装到 `~/Applications/SkillBox.app`。正式安装版可执行文件 SHA-256 为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，与验收候选一致；严格签名与 Designated Requirement 复核通过。
- ✅ 被替换的 `f143d037…` 版本已整包保留为 `~/Applications/SkillBox-before-reorder-runtime-20260829-082414.app`，可执行文件 SHA-256 为 `f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`，签名仍有效。新版已从正式路径启动，验收进程 PID 为 `97344`，窗口与侧边栏状态回读正常。
- ✅ 用户刚才在候选版中进行真实拖动后，`organization.json` 在 `08:23:06` 已保存新顺序，时间早于 `08:24:14` 开始的安装替换；安装过程保留了该顺序，没有用旧数据覆盖。本次只替换 SkillBox 应用包，没有写入任何 AI 应用目录。
- ✅ 用户对已安装的等高占位版本实测后再次发现“拖动时卡片不让位”。运行时轨迹确认：每张 Skill 卡片的真实坐标都正确，但外层 `ScrollView` 的尺寸偏好始终回报 `0 × 0`，导致所有指针位置都被误判为“列表外”，`previewOrder` 从未更新。前一轮仅验证了让位状态计算，没有覆盖这个真实命中边界，因此当时的“已修复”结论不成立。
- ✅ 拖动命中区现改为由当前分组内已测量的真实 Skill 卡片边界合并得出，不再依赖会回报零值的滚动区尺寸；插入线的宽度和起点也改用同一份真实边界。新增回归先因缺少实卡片边界策略编译失败，实现后转绿。
- ✅ 已对清除诊断日志后的 Release 候选执行真实 macOS 鼠标拖动：`html-deck-studio` 跨过 `zhaoji-writing` 中线后，后者立即上移占据原位，目标槽位同步腾出；鼠标移出列表并松手后，浮层、蓝线和临时顺序全部清除。`organization.json` 验收前后 SHA-256 同为 `92c610b4abed80bdd00d45c3fc015d8876eb6b447d9f10b4f06307cdd4b274f3`，未落地改变用户排序。
- ✅ `app/Scripts/test-all.sh` 完整门禁 276/276 通过，`git diff --check` 通过；最终 Release 候选可执行文件 SHA-256 为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，严格签名与 Designated Requirement 验证通过。该候选已打开供用户直接试拖，尚未替换 `~/Applications/SkillBox.app`；已安装版仍是用户刚才验出问题的 `f143d037…` 版本。
- ✅ 用户确认后，已把“等高占位槽＋独立跟手卡片”修复版按同卷可恢复方式安装到 `~/Applications/SkillBox.app`。正式安装版 SHA-256 与 Release 候选一致（`f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`），严格签名与 Designated Requirement 复核通过，并已从正式路径成功启动。
- ✅ 被替换的上一版完整保留为 `~/Applications/SkillBox-before-placeholder-20260829-073300.app`，SHA-256 为 `afd7e97772eaaadc752758a831632285175fc3b87242c84aa7578ac09929531a`。启动复核发现一条自 00:17 起残留的旧进程，已精确结束；当前只运行新安装实例 PID `72258`。本次没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 用户真实拖动已安装 v26 后发现卡片没有形成“互相让位”的松手结果预览，被拖卡片会与相邻卡片重影、压叠。根因是 SwiftUI 中同一张卡片同时参加 `LazyVStack` 排位动画和鼠标位移，布局层与跟手层没有分离；HTML 原型其实已经使用独立 ghost 与 placeholder，正式实现没有完整复现这层结构。
- ✅ 正式 SwiftUI 已改为“等高空占位槽＋独立跟手卡片”：被拖卡片在列表中只保留透明占位，目标变化时占位槽随 `previewOrder` 移动并推动其他卡片让位；视觉卡片单独跟随鼠标，蓝线固定在占位槽底部。新增回归先因缺少占位／浮层分离接口失败，修复后转绿；`app/Scripts/test-all.sh` 自动发现的 276 项测试全部通过，`git diff --check` 通过。新 Release 候选的 SHA-256 为 `f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`，严格签名与 Designated Requirement 通过；正式安装版尚未再次替换。
- ✅ 用户明确确认替换后，已将 v26 Release 候选按同卷可恢复方式安装到 `~/Applications/SkillBox.app`。安装版可执行文件 SHA-256 与候选一致（`afd7e97772eaaadc752758a831632285175fc3b87242c84aa7578ac09929531a`），严格签名与 Designated Requirement 复核通过，并已从正式路径成功启动。
- ✅ 原安装版完整保留为 `~/Applications/SkillBox-before-v26-20260829-001559.app`，其可执行文件 SHA-256 为 `049b750e39d85e27541e1d0e81ccbcca56440a970c80c8d7a3c4298f4347ebdb`。本次只替换应用包，没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 用户真实拖动 v26 时反馈“看不到落点线”。根因是蓝线被绑在被拖卡片或占位层的底边，与卡片阴影重合，截图中虽有 `2 px` 色带，用户感知上几乎等于没有。
- ✅ v26 HTML 原型和正式 SwiftUI 已同步改为独立落点层：蓝线固定在最终插入槽位、层级高于跟手卡片和阴影；拖出列表、取消或松手后仍统一清除。原型页面使用 `file://` 打开，已有页面需手动按 `⌘R` 重载才会读取新文件。
- ✅ 交互回归先在旧实现上因缺少“独立插入线位置”而失败，修复后 8/8 通过；`app/Scripts/test-all.sh` 自动发现的 275 项测试全部通过，`git diff --check` 通过。新 Release 候选的 `Info.plist`、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过；未替换已安装 SkillBox。
- 📌 重建后的 Release 候选已在真实 macOS 窗口回读，普通列表无蓝线残留，拖出列表后五个 Skill 顺序保持不变。辅助操作未能在鼠标按住的中间帧同时回传截图，因此“拖动中蓝线的实际像素观感”仍留给用户在已刷新原型中直接确认，不用静态测试代替这道真实体验门。

## 2026-08-28

- ✅ 用户确认 v26 后已接入正式 SwiftUI：Skill 卡片的来源图标移到尾部固定列，GitHub 使用可直接识别的章鱼猫标志，本地使用蓝色实心文件夹，存储大小固定在最右侧，不再因名称长短左右不齐。
- ✅ 原生逐行拖放已改为统一拖动会话：移动超过 `8 pt` 才启动，被拖行在列表轨道内跟手，相邻行实时让位，落点只在被拖行下方显示一根 `2 pt` 蓝线。松手、取消、离开列表、窗口或应用失焦、页面消失及对象被刷新移除都会统一清空临时状态，不再遗留蓝线、空位或浮层。
- ✅ 新增并更新 8 项列表交互回归，覆盖三类来源图标、上下落点、邻接顺序、完成后只产生一次移动且清空状态、取消后回到空闲。`app/Scripts/test-all.sh` 自动发现的 275 项测试全部通过，`git diff --check` 通过。
- ✅ 已生成 `app/.build/release/SkillBox.app` Release 候选；`Info.plist` 校验、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过。本轮只生成项目内候选，没有替换 `~/Applications/SkillBox.app`，没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 根据用户对 v22 真实拖动界面的反馈，完成 v23 克制版列表：拖动落点只保留一根 `2 pt` 系统蓝插入线，去掉端点和「放到某 Skill 上方／下方」文字胶囊，不再额外撑开列表空白。插入线只在拖动且存在有效落点时显示，离开、取消或放置后立即清除；精确落点文字仅保留给 VoiceOver。
- ✅ Skill 卡片改为单行「名称＋尾部来源图标＋中央库大小」：GitHub 使用远程网络图标，本地来源使用文件夹图标，应用导入使用导入图标；来源全称改放悬停说明与辅助功能名称。新增 2 项回归后完整门禁 274/274 通过，Debug App 的 `Info.plist`、ad-hoc Hardened Runtime 与严格签名通过。
- ✅ 真实 macOS 窗口已验收普通状态和取消式拖动：普通状态没有任何插入线，拖动中只出现系统蓝线，按 Esc 后线立即消失，html-deck-studio 与 agent-team 顺序保持不变。可视化产物保存为 `skillbox-library-reorder-v23.html`、`skillbox-library-reorder-v23-normal.png` 和 `skillbox-library-reorder-v23-drag.png`；本轮只验收 Debug 候选，没有再次替换已安装 SkillBox。
- ✅ 用户通过「我的 Skills」列表整理 v22 原型后，已接入正式 SwiftUI：列表行只保留 Skill 名称、GitHub／本地来源与中央库占用大小，移除原简介和状态徽标；存储大小递归计算中央主 Skill 内的普通文件，不计安装副本、来源缓存和备份。
- ✅ 同组拖动按目标行上下半区解析为上方／下方，拖动中显示蓝色插入线和「放到 <Skill> 上方／下方」文字；邻接行和向下移动会先排除被拖 Skill 再计算落点，避免索引偏移。减少动态效果时保留落点而取消过渡动画。
- ✅ Skill 行右键菜单现只提供「移动到」已有文件夹和「删除 Skill…」。删除复用现有系统废纸篓与即时撤销链路；有受管理安装副本时继续先选「保留副本」或「全部卸载并清理」，不会因为从右键入口进入就绕过确认和预览。
- ✅ 新增 5 项 TDD 回归，覆盖命名上下落点、邻接向下排序、起始卡片不冒充落点、中央库嵌套文件大小和三类来源文案。`app/Scripts/test-all.sh` 完整门禁 272/272 通过（主测试 240 项，GitHub 来源隔离测试 32 项），`git diff --check` 通过。
- ✅ 从当前源码生成独立 Debug App 并通过 `Info.plist`、ad-hoc Hardened Runtime 与严格签名校验。真实 macOS 窗口确认五份 Skill 正确显示来源与 34 KB／46 KB／97 KB／5 MB／600 KB 的中央库大小，右键菜单显示「移动到」和「删除 Skill…」，拖动前台真实显示「放到 agent-team 上方」的插入线。本轮只停留在拖动预览后关闭验收副本，未落地改变用户排序，未触发删除，也未替换已安装 SkillBox。
- ✅ 用户确认进入下一步后，重新执行 `app/Scripts/test-all.sh` 完整门禁并通过，再由当前工作区源码重新运行 `package-app.sh release`。新候选 `Info.plist` 校验通过，ad-hoc Hardened Runtime 签名在磁盘有效并满足 Designated Requirement；可执行文件 SHA-256 为 `6017e49fce3a2808cf6c3b62d33b459592f1e1ee8959838913ae9b341350856e`。
- ✅ 已完成本机安装版的可恢复更新：先把候选复制到 Applications 同卷暂存位置并复核摘要与签名，精确结束旧安装进程后，将原 `~/Applications/SkillBox.app` 整体保存为 `~/Applications/SkillBox.backup-20260828-014342.app`，再把候选原子改名到正式路径。旧版备份 SHA-256 为 `693681db29f41f47760e8dda8a013ffb1d64a2c2f9a9e48e4415b9ddf24ff6ca`，签名仍有效，可用于回退。
- ✅ 安装后从正式路径真实启动，运行进程为 `~/Applications/SkillBox.app/Contents/MacOS/SkillBox`；正式安装版可执行摘要与 Release 候选一致。系统截图与精确 PID 辅助操作确认总览正常加载，并在 `agent-team` 详情中真实显示「安装前，有 1 项内容需要你了解」「待了解」与显式「查看并确认」，没有裁切或遮挡。
- 📌 本次只替换 SkillBox 应用包，没有修改中央 Skill 数据、任何 AI 应用目录或 GitHub。应用元数据仍为 `0.1.0 (2)`，本轮更新以可执行摘要和真实前台内容识别；原生 Computer Use 管道仍返回 `native pipe closed before response`，按用户已授权的系统截图与辅助操作完成兜底验收。源码与本轮设计产物仍保留在未提交工作区，未擅自创建 Git 提交。
- ✅ 用户明确要求“替换”后，再次从当前 v22 源码构建 Release 候选，`Info.plist`、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过；候选可执行文件 SHA-256 为 `049b750e39d85e27541e1d0e81ccbcca56440a970c80c8d7a3c4298f4347ebdb`。
- ✅ 已完成第二次同卷可恢复替换：原安装版保留为 `~/Applications/SkillBox.backup-20260828-224135.app`，其可执行文件 SHA-256 为 `6017e49fce3a2808cf6c3b62d33b459592f1e1ee8959838913ae9b341350856e`，签名仍有效。新版已原子改名到 `~/Applications/SkillBox.app`，验收时进程 PID 为 `60474`，可执行路径与候选摘要完全一致。
- ✅ 从正式安装路径真实回读“我的 Skills”：五张卡片只显示名称、GitHub／本地来源和 34 KB／46 KB／97 KB／5 MB／600 KB 大小；右键菜单真实显示“移动到”和“删除 Skill…”。验收只展开菜单后按 Esc 关闭，没有移动、删除、改变排序或写入任何 AI 应用目录；正式 SkillBox 已留在“我的 Skills”页运行。

## 2026-08-27

- ✅ 用户通过消费级交互闭环 v21 设计后，已将高风险确认、批量安装/卸载预览和「恢复到操作前」三条闭环接入正式 SwiftUI；「安装到应用」现有矩阵布局与状态样式保持不变。
- ✅ 高风险卡片新增显式「查看并确认」；确认摘要绑定 Skill ID、当前内容指纹和稳定风险证据，重启后保留，内容或证据变化后自动失效。确认后列表橙色标记与详情大卡同步收起，`blocked` 内容仍不可安装；批量安装公共边界也无法绕过确认。
- ✅ 批量安装与卸载改为内存草稿预览；预览前不持久化安装选择，「取消，不保存」后不保留接管或替换授权，也不写入应用文件夹。用户最终确认后才持久化草稿，并基于最新状态重新计划再执行。
- ✅ 「恢复到操作前」改为先展示位置级恢复清单，并准确说明中央原件是否受影响；打开或取消预览不改文件，确认后才调用现有安全恢复引擎，操作后的外部改动仍会被保留。
- ✅ 按 TDD 新增 5 项消费级闭环回归，覆盖风险确认持久与失效、安装公共边界、取消预览不留选择、取消不留替换授权以及恢复预览。项目完整测试门自动发现并通过 267/267 项；最终 Release 生产构建 18.67 秒通过，`Info.plist`、本地签名和 Designated Requirement 校验通过，`git diff --check` 通过。
- ✅ 用户明确允许 macOS 系统截图与辅助操作后，已对精确路径 `app/.build/arm64-apple-macosx/release/SkillBox.app` 做真实前台验收。先用 PID 与可执行路径识别并退出会抢占同一 Bundle ID 的旧安装版，随后在最新 Release 窗口完成三条安全闭环：风险卡可见「查看并确认」，关闭后仍保持「待了解」；批量安装预览明确提示尚未保存或写入，点击「取消，不保存」后回执为未保存任何安装选择；「恢复到操作前」先列出 1 个具体位置，取消后回执为未修改任何文件。三条路径的按钮、弹窗、取消出口及返回状态均无裁切或遮挡。
- 📌 本次只验证上述三条核心闭环的真实鼠标/辅助操作与像素结果，没有点击「我已了解」「确认安装」或「确认恢复」，没有写入真实 Agent 目录、替换用户应用或发布 GitHub Release。验收后已关闭测试进程并重新打开原安装版。完整应用遍历、键盘、VoiceOver、干净账户安装和线上发行仍是独立验收门；原生 Computer Use 管道本轮仍不可用，系统截图与精确 PID 辅助操作由用户单独授权后作为前台证据。

## 2026-08-22

- ✅ 产品负责人明确授权取消 Developer ID Application 与 Apple 公证的强制上线条件。当前正式策略改为 GitHub 提供 ad-hoc Hardened Runtime DMG，用户首次安装或更新后按“系统设置 → 隐私与安全 → 仍要打开”放行；历史记录中的证书、公证待办已由 Spec v1.9 与 ADR-0010 取代。
- ✅ 上线前安全复核新增三项 TDD 修补：发现规划发送前统一清理用户输入与历史意图中的秘密和本机路径；脱敏扩展到 `/Volumes`、`/private`、`file://`、带引号字段和 YAML 块；高风险分析增加 `nc`、`scp`、`sftp`、`rsync` 等常见凭据外传通道。
- ✅ 发布链新增 Release 调试信息移除、本机路径映射、最小包内容白名单、凭据与个人路径扫描、来源快照、JSON 清单、SHA-256 和隔离属性模拟。脚本明确拒绝 Developer ID、公证凭据与测试 GitHub 身份覆盖，不会自动上传或安装。
- ✅ 最终门禁全量通过：主测试 230 项、GitHub 来源拆批 32 项，共 262 项；Release arm64 构建、ad-hoc Hardened Runtime、严格签名校验、最小包内容、隐私扫描、图标系统回读、DMG 系统完整性、隔离属性拒绝模拟、JSON 来源清单和 SHA-256 均通过。
- ✅ 只读挂载最终 DMG 后独立复核：包内只有 `SkillBox.app`、应用程序快捷方式和中文安装说明；挂载副本签名与隐私门再次通过，DMG 和 JSON 清单的 SHA-256 回读一致。
- 📌 Developer ID 与 Apple 公证不再是当前上线阻断项。产物已达到“可上传 GitHub 的发布候选”；尚未替用户安装、未上传 GitHub。上线宣称仍需由用户或干净账户真实完成一次“仍要打开”安装和核心鼠标流程。

- ✅ 关闭上线前剩余三项数据一致性债务：现有安装目录改为 macOS 同卷原子交换并在交换后校验真实旧内容；最终提交边界出现外部编辑时会交换回去并停止，不覆盖用户改动。卸载先原子移出并校验，再删除。
- ✅ 删除撤销新增写前恢复日志；内容从废纸篓移动后若应用退出，下次启动会把内容退回原废纸篓位置并把事务标记为已回滚，不会留下“文件已恢复、列表却没有”的半完成状态。
- ✅ 新增 `library-state.json` 完整状态提交点；原有八份分项 JSON 继续作为兼容镜像，但启动只采用最后原子提交的完整代际。旧版只有分项文件时自动无损迁移，单份镜像来自不同代际也不会污染当前状态。
- ✅ 三项问题均先建立失败用例再修复；中央仓库 38 项、事务相关筛选 58 项通过，`app/Scripts/test-all.sh` 全量通过（主测试 227 项，8 组 GitHub 来源拆批各 4 项，共 259 项）。
- ✅ Release 生产构建 36.68 秒通过；`Info.plist`、Hardened Runtime ad-hoc 签名、`codesign --verify --deep --strict` 与 `git diff --check` 通过。构建仍保留一条钥匙串非交互保护 API 的弃用警告；它不影响当前构建，暂不移除这层防止密码弹窗的兼容保护。
- 📌 本轮未安装或替换用户应用，未写入真实 Agent 目录，未上传 GitHub。当时记录的签名公证门已被本日 Spec v1.9 与 ADR-0010 取代；干净账户安装和真实前台验收仍保留。

## 2026-08-21

- ✅ 按上线前安全审计建议完成阻断项修补：大体积文本和被 `SKILL.md` 引用的说明文件继续执行高风险指令扫描；启动扫描增加文件、目录、深度、总量和时间预算；GitHub 元数据与目录树增加响应上限，并拒绝服务端标记为不完整的结果。
- ✅ 中央 Skill 更新在替换前核对实时内容，版本归档必须与目录名指纹一致，并通过独立临时目录完成校验后提交；本地导入的临时文件只按明确登记的精确目录清理，自定义安装位置同时拒绝中央库的上级目录。
- ✅ AI 验证结果绑定精确的服务地址、模型和授权代际；切换配置或删除密钥会立即撤销旧任务写回权限，避免迟到响应把新配置或已删除密钥重新标成可用。更新进入提交阶段后不再向用户显示虚假的“已取消”，GitHub 连接数量和凭据状态文案改为真实语义。
- ✅ 先用失败用例复现上述问题，再完成回归；`app/Scripts/test-all.sh` 全量通过（主测试 224 项，8 组 GitHub 来源拆批各 4 项），Release 生产构建、`codesign --verify --deep --strict` 与 `git diff --check` 通过。
- 📌 本轮修补未解决三个低风险架构债务：安装目标最终检查与替换之间仍有极短竞争窗口，删除/撤销恢复尚未全部纳入写前日志，多份 JSON 状态仍分文件提交。它们不再作为本轮高、中风险阻断项，但后续应继续事务化收口。
- 📌 当前产物仍是 ad-hoc 本地候选；完整鼠标前台回归、Developer ID 签名、Apple 公证、Gatekeeper、干净账户安装与 GitHub Release 下载哈希回读仍未通过，因此尚不能标记为正式上线。

- ✅ 修复 SkillBox 每次启动反复要求输入 Mac 登录密码：启动阶段不再读取 GitHub Token 或六类 AI API Key，只恢复不含秘密的连接提示；超过 24 小时的公开 GitHub 自动检查继续匿名执行，私人来源只在用户主动操作后使用凭据。自动钥匙串读取同时禁止 LocalAuthentication 与 Security 授权界面；用户明确“连接 GitHub”或“保存并测试”时，才允许为新应用身份重新绑定一次旧凭据。
- ✅ 真实 Release 冷启动复验先连续复现 `SecurityAgent` 的 `confirm-access-password` 窗口，再在最终实现中确认 SkillBox 进程保持运行且系统无 `SecurityAgent` / `authorizationhost`，统一日志也没有密码确认或钥匙串授权窗口记录。Computer Use 对开发构建的窗口树仍返回 `native pipe closed before response`，因此只将“启动密码弹窗消失”判为 PASS，完整鼠标前台门仍保持 BLOCK。
- ✅ 完成最新候选的九项安全修复：危险 `SKILL.md` 指令不再被文档规则降级；AI 密钥只会发往已验证的精确服务地址与模型；私人内容确认绑定接收方；候选比较与使用说明统一脱敏；未知或私人仓库证据默认不进入 AI；自动回滚、中央更新和中央删除均在崩溃或并发变化下保护较新内容；发现、GitHub 与 OAuth 响应增加硬上限；GitHub 和 AI 凭据改为仅本机且设备解锁时可访问。
- ✅ 新增 `Scripts/test-all.sh`，运行时自动发现全部测试，并把已知可能令 SwiftPM 助手停留的 GitHub 来源组拆批。当前 16 个普通测试组 214 项、GitHub 来源 30 项，共 244/244 通过；高风险、AI、发现、中心库、事务、删除中断、更新中断、网络上限、启动无秘密读取和钥匙串回归均在其中。
- ✅ 最终 Release 生产构建 17.33 秒通过；`Info.plist`、Hardened Runtime ad-hoc 签名与 `codesign --verify --deep --strict` 通过。生成包位于 `app/.build/release/SkillBox.app`，未替换用户安装版、未上传 GitHub Release。
- ✅ Release 生产构建 53.46 秒通过；`Info.plist`、品牌资源、Hardened Runtime ad-hoc 签名和 `codesign --verify --deep --strict` 通过。打包脚本现在只接受 `debug/release` 并在组包前清除精确旧包，避免陈旧文件混入。
- ✅ 图标集成回读在复用 `.build/release` 路径时复现 LaunchServices 旧路径缓存，特征距离 1.165；同一签名包复制到全新路径后为 0.592 并通过。正式发行脚本因此每次使用唯一暂存目录，并在制作 DMG 前调用 macOS `NSWorkspace` + Vision 复验真实系统图标，避免把缓存误判成包内资源故障，也避免漏掉真正的默认图标回归。
- ✅ 新增 `Scripts/release-distribution.sh`，公开发行强制要求干净 Git、全量测试、Developer ID、带时间戳签名、DMG、Apple 公证、票据装订、Gatekeeper 与 SHA-256；缺少真实发行证书时原子停止。本机 ad-hoc 包的 Gatekeeper 结果为 `rejected`，因此当前结论仍是“本地发布候选”，没有冒充已上线版本。
- 📌 仍待真实可恢复 Agent 闭环、当前候选完整鼠标前台回归、Developer ID/公证、干净账户安装和线上 Release 下载哈希校验；本轮未替换用户已安装应用，未写入真实 Agent 目录，未发布远端 Release。
- 📌 已尝试启动当前 Release 候选做只读前台烟雾验收，进程可启动，但 Computer Use 连续返回 `native pipe closed before response`，未取得窗口树或鼠标证据；本轮启动的测试进程随后已关闭。该门保持 BLOCK，不以进程启动或静态检查代替真实前台 PASS。
- ✅ 中央 Skill 删除位置由应用内 `Deleted/` 改为 macOS 系统废纸篓；使用系统返回的实际废纸篓 URL 支持一次即时撤销，清空废纸篓后明确无法恢复。SkillBox 不再为正常删除继续保留第二份中央内容。
- ✅ 内容早已缺失或历史数据与另一条同名记录共享中央路径时，只清理失效记录与管理关系，不移动新记录内容、不制造空备份；旧版 `Deleted/` 遗留内容会在新应用启动时迁入系统废纸篓。
- ✅ 新增系统废纸篓注入、失效记录零占用和旧删除目录迁移回归；中央库、同步事务、删除流程与本地开发源共 60 项相关测试通过。Release 生产构建 50.19 秒、Info.plist 与 ad-hoc Hardened Runtime 签名校验通过。新测试包真实启动后，应用内 `Deleted/` 已为空，已知旧备份出现在 `~/.Trash/1786718892-AD1EF41A-467A-4D32-AD52-CB75B5071419`；目录中只剩新的本地开发源记录。Apple 官方确认废纸篓未清空前仍占磁盘，只有用户清空后释放空间。
- ✅ 修复旧 Skill 点击清理后仍留在列表的问题。真实数据表明旧 `obsidian-feishu-sync` 的中央内容已经缺失，但目录记录仍占用 `Library/obsidian-feishu-sync/content`；新本地开发源导入只检查磁盘目录，错误复用了同一路径，导致两条记录共享中央内容位置。
- ✅ 中央目录分配现在同时检查磁盘和目录记录，内容缺失的旧记录也会继续保留其路径占用；残缺或历史共享路径记录可只清理元数据和管理关系，不再因中央内容缺失失败，也绝不会移动另一条同名记录的内容。本机已安装副本继续保留。
- ✅ 按 TDD 新增 3 项回归，覆盖缺失目录的路径保留、残缺同名记录清理不伤替代版本，以及 AppModel 完成清理后列表快照、安装副本和撤销入口的一致性。16 组共 228 项业务测试全部通过；`SourceProviderTests` 整组入口仍偶发停在 SwiftPM 助手，拆成 4 批后 30/30 通过。Release 生产构建 55.65 秒通过，Info.plist 与 ad-hoc Hardened Runtime 签名校验通过。
- ✅ 修复 Dock 仍显示 macOS 默认蓝图图标：根因是手工组装的 SwiftPM 应用同时声明 `CFBundleIconName=AppIcon`，LaunchServices 优先走了未正确关联的资产目录；同时构建号始终为 1，使错误解析继续被缓存。已删除该错误声明并将构建号递增为 2。
- ✅ 新增 `AppIconIntegrationCheck.swift`，直接通过 macOS `NSWorkspace` + Vision 特征距离比较“系统为 `.app` 解析的图标”与包内品牌图标；错误安装版稳定红灯 1.084，修复后正式安装版绿灯 0.593。构建号 2 已备份式替换、重新注册并从 `~/Applications/SkillBox.app` 真实启动；签名、运行路径和系统解析图标回读通过。
- ✅ 将用户确认的白色 `S` 角色正式定为 SkillBox IP 与应用 Logo；保留原始角色母版，并生成适合 macOS 浅、深色桌面的银白圆角图标母版。
- ✅ 接入 16–1024 px 完整 AppIcon 资源、`Assets.car` 与 `.icns` 兼容资源；打包脚本会将两者写入应用包，`Info.plist` 已声明 `SkillBox.icns` 品牌图标。Release 真实打包、资源回读、Hardened Runtime ad-hoc 签名与 `codesign --verify --deep --strict` 通过。
- ✅ 已将新 Logo 版本替换到 `~/Applications/SkillBox.app`，原安装版保留为带时间戳的可恢复备份；旧开发路径进程已退出，新应用已从正式安装位置真实启动，安装后签名与图标资源回读一致。
- ✅ 用户确认后清理已过期的旧安装备份与 SwiftPM 可重建构建缓存，共释放约 511 MB；清理前后均核对绝对路径，正式安装版、Logo 资源、签名、源代码和 Git 改动未受影响。
- ✅ 完成“删除 SkillBox 主 Skill”的双路径：默认只清理中央主 Skill 并解除管理关系，应用中的现有副本保持不变；也可先预览并卸载当前记录的全部受管理副本，事务安全完成后再清理中央主 Skill。其他同名记录和未受管理副本不受影响。
- ✅ 将已确认的 v19 双选项稿接入正式 SwiftUI，明确列出受影响应用、“不改应用文件”安全标签和后续卸载清单；卸载预览在该流程中改为“确认卸载并清理”，返回调整会取消待清理状态。
- ✅ 删除备份新增原管理关系；即时撤销只在应用副本指纹未变化且位置未被其他安装接管时恢复管理。副本已变化或缺失时只恢复中央 Skill，不向应用目录写入、不覆盖、不删除应用文件。
- ✅ 按 TDD 新增 5 项回归，覆盖中央清理不碰应用文件、未变化副本安全接管、已变化副本保持不受管理、卸载预览前中央 Skill 仍存在、确认卸载后才清理。中央仓库 27 项和删除流程 2 项分别通过，独立 Release 生产构建 33.42 秒通过，`git diff --check` 通过。
- 📌 无过滤的 Swift 全量运行再次在业务用例后半段出现 SwiftPM 测试助手 0% CPU 停顿；按组复验时 GitHub 授权 4 项和本次删除流程 2 项通过，随后 SourceProvider 组同样停在助手层。已停止无进展进程，不把它写成全量 PASS，也没有启动或替换用户当前使用的 SkillBox。

## 2026-08-20

- ✅ 按 TDD 完成发现质量收口：没有相关性证据的未评估候选不再进入推荐区；某一条 skills.sh 或 GitHub 查询失败时保留其他已核验结果，并准确记录为部分失败，不把整轮搜索清空。
- ✅ AI 连接状态改为绑定“服务地址 + 模型”的真实结构化探测，保存密钥不再等于已连接；内置服务地址固定为官方端点。发送 Skill 资料前会清除令牌、认证头、私钥、用户目录等敏感信息，未知仓库隐私默认不上传。
- ✅ 移除没有资料依据的用途、场景、三步流程和提示词补写；作者资料与 AI 辅助说明现在分别标注来源，旧的无来源 AI 缓存不会继续复用。AI 和 GitHub 下载均改为流式计数，超过上限立即停止读取。
- ✅ 高风险 Skill 在核心存储层和界面层都需要第二次明确确认，确认页展示命中文件、规则和证据；新增浏览器资料外传、编码载荷执行、启动项持久化三类高风险识别，已阻止内容仍不可绕过。
- ✅ 压缩包检查与解压子进程增加输出上限、30 秒超时、取消和强制终止兜底；展开大小累加增加溢出保护。自定义安装位置在保存前解析软链接，避免之后被软链接改指到另一个目录；纯标点 Skill 名称也不会再生成空记录。
- ✅ 同步事务改为“先写恢复记录，再改文件”，应用启动时会恢复中断的安装、卸载和中央 Skill 更新；中央更新与应用部署共用一条事务记录。恢复前会核对中央原件仍是操作前或操作后的版本，发现更晚的外部改动就停止，不会静默覆盖。多窗口改为共享同一份 AppModel，减少窗口之间互相覆盖状态。
- ✅ 14 个测试组共 211 项用例全部通过，含 30 项 GitHub 来源与压缩包用例逐条复验；独立 Release 生产构建 34.40 秒通过，最后一处事务保护后的增量 Release 复验 15.75 秒通过，`git diff --check` 通过。SwiftPM 全量运行在所有业务用例完成附近仍偶发只剩测试助手不退出，因此最终门采用独立 scratch + 分组逐项验收，不把运行器异常冒充业务死锁或完整全量进程 PASS。
- 📌 本轮未启动或替换用户正在使用的 SkillBox，未调用真实 AI、未读取或记录 API Key，也未向任何真实 Agent 目录安装 Skill；下一步是用户人工体验发现、风险二次确认和崩溃恢复界面。

- ✅ 真实前台验收复现 DeepSeek 候选比较失败：同轮需求整理 HTTP 200，但 8 个候选的比较请求返回 HTTP 400。根因是请求开启 `response_format: json_object`，候选比较与使用指南的 system 消息却没有明确包含 `json`，不符合 DeepSeek JSON Output 契约。
- ✅ 以公共 `OpenAICompatibleProvider` 边界先写失败测试，再在结构化请求公共层自动补齐 JSON 模式指令；需求整理、候选比较和四段式使用指南现在共同受这一规则保护，没有散落修改业务提示词。
- ✅ 新增 DeepSeek 三类结构化任务回归；AI 集成 20 项、Swift 全量 193 项 / 14 组全部通过，Release 生产构建 26.65 秒通过。验证未调用真实 API、未读取或记录 API Key，也未向任何应用目录安装 Skill。
- ✅ 用三路独立子 Agent 完成代码盲审，并按可复现证据修正了候选 ID 冲突崩溃、AI 部分返回误删候选、第 9 个以后候选漏掉、搜索记录并发覆盖、删除后被后台保存复活、取消被吞掉以及重复提交等问题。
- ✅ 修复后二次盲审继续截住混合虚构候选仍可污染 AI 回复、同 ID 新版 `SKILL.md` 继承旧指南、迟到的指南响应跨版本落盘，以及取消后的 GitHub 旧任务覆盖新候选和进度。异步搜索、指南、GitHub 预览、更新与内容整理现在都只允许当前任务代际写入状态。
- ✅ 收紧发现链路的 AI 上下文预算：单次最多比较 8 个候选，所有候选证据合计最多 8000 字符，评估输出最多 1500 tokens，需求规划输出最多 600 tokens；自动重试不得突破当前阶段预算。
- ✅ 使用指南的发送材料上限收紧为 12000 字符、整阶段输出上限 1200 tokens；诊断中任一次请求缺失 token usage 时保留为“未知”，不再用 0 或部分数值冒充整轮总量。
- ✅ 修正 AI 排序边界：已交给 AI 且被判为「其他」的 Skill 不会再被热度或本地回退规则推回推荐区；因 AI 预算未参与评估的已核验候选，仍按确定性规则排序，不会被静默丢失。
- ✅ 四段式使用指南从「批量搜索时全部生成」改为「用户点开某个候选时按需生成」；生成结果先保存到寻找记录，后续加入我的 Skills 时按内容指纹复用，失败时保留本地降级说明。
- ✅ 候选指南现在绑定实际 `SKILL.md` 的内容身份；下载到的 Release、分支或版本与搜索时内容不一致时，不复用旧说明，改从真正加入的内容重新生成。
- ✅ 按需指南同一时刻只保留一笔 AI 请求；切换候选会取消上一笔，详情页显示当前整理状态和“停止”操作，不再因快速点击并行产生多笔 30 秒请求。
- ✅ AI 诊断新增请求大小、输入/输出 token、耗时、尝试次数和候选数等数值指标，不保存用户原文、候选正文、推理内容或 API Key。
- ✅ 发现、AI、指南与扫描针对性 79 项测试通过；最终固定代码基线在沙箱外独立 scratch 目录中完成 14 组、192 项 Swift 全量测试，实际测试用时 1.668 秒；独立 Release 生产构建 40.47 秒通过，最后一行代码后的增量 Release 复验 18.73 秒通过。受限沙箱内偶发的无输出停顿定位在 SwiftPM 测试启动助手层，当时尚未创建 SkillBox 测试子进程，本轮不将它误写为业务代码死锁。
- ✅ 定位发现搜索的效率瓶颈：首次搜索曾对每个查询最多召回 1000 条，且在去重前反复读取候选的仓库资料和完整 `SKILL.md`；随后又把几十份长文同时送入 AI 比较，造成数分钟等待和网络超时。
- ✅ 首次搜索改为 24 条快速召回，用户主动「继续深挖」时再扩展到 96 条；这两个数字只是单次网络预算，不是候选展示上限，所有通过精品门槛的 Skill 仍会保留。
- ✅ skills.sh 先依据可信来源、Star 和安装量建立质量前沿，再读取 Skill 详情；GitHub 先合并不同查询的重复命中，再为有可靠公开证据的仓库读取真实 `SKILL.md`，同一 Skill 不再反复验证。
- ✅ AI 候选比较只接收通过本地质量门的候选和最多 6000 字的精简证据，不再上传几十份完整长文。
- ✅ 修正截图中的误导提示：某个 GitHub 查询词失败不再被说成「3 个公开来源未完成」；只有整个独立来源不可用时才显示来源级降级提示，已核验结果继续保留。
- ✅ 新增 AI 请求体积、质量前沿、快速/深挖预算、弱仓库跳过、跨查询去重和失败语义回归；Swift 全量 177 项测试通过，Release 生产构建与本地签名校验通过。

## 2026-08-19

- ✅ AI 设置已从三种服务扩展为 Agnes、DeepSeek、Kimi、MiniMax、GLM 与自定义 API。内置服务预设官方兼容地址和推荐模型；用户在同一页选择服务、填写 API Key、选择模型并“保存并测试”，只有完成最小结构化输出测试才显示已连接。
- ✅ 旧版 `ai-settings.json` 采用无损补齐迁移：保留用户已有的服务选择、模型、自定义地址与私人内容开关，只追加缺失的新服务；六个服务的密钥继续按服务分别保存在 macOS 钥匙串。
- ✅ 普通设置中的无操作“使用数据”陈列已移除；v18 原型与正式 SwiftUI 都不再用一行不可点击的说明占用界面。
- ✅ 移除总览页重复的 Skill 搜索框，搜索需求统一进入一级导航「发现 Skills」。设置页 v18 与正式 SwiftUI 均按「通用、AI、GitHub、数据」四类收口；API、仓库列表、清理与断开等复杂操作不再堆叠在同一张长页面。
- ✅ 新增旧 AI 设置迁移回归，AI 专项 11 项、Swift 全量 171 项测试全部通过；Release 生产构建完成。

## 2026-08-18

- ✅ 收口「发现 Skills」三栏交互：新建寻找改为记录图标左侧的加号；寻找、候选和详情共用 74 点固定标题高度与同一水平分隔线；候选标题下的重复备注已移除。
- ✅ 发现详情与「我的 Skills」统一为四块用户内容：「Skill 简要说明（特色、技巧）、适用场景、体验流程、提示词参考」。AI 优先依据真实 `SKILL.md`，可结合 README、`agents/`、`references/` 和 GitHub 作者简介，一次生成普通用户能直接看懂和使用的说明。
- ✅ 新增版本化使用说明缓存：按 Skill ID、内容指纹和提示词版本保存；发现页加入中央库时直接复用候选说明，本地或私人 Skill 只有用户开启明确的内容发送开关后才使用 AI；同一版本日常打开不会重复生成。
- ✅ 精品筛选进一步收紧：主候选区只显示强推荐，弱候选保留在内部证据但不向用户充数；GitHub Star 成为高优先级排序证据，同时继续要求真实 Skill 内容、需求匹配、来源可追溯和无阻止问题。推荐数量仍由质量决定，不设固定数量。
- ✅ 统一「发现 Skills」三栏顶部基线：寻找、候选与详情区域现在共同占满可用高度并从页面顶部开始排布，三个标题和标题分隔线保持同一水平位置；空状态仍在各自内容区居中。已完成真实前台截图验收、166 项 Swift 测试和 Release 构建验证。
- ✅ 完成「发现 Skills」v2.1 根因修复：DeepSeek 规划与评估请求启用 JSON 输出模式、关闭思考模式、扩大完整输出空间；现在会区分网络、鉴权、限流、空内容、截断、JSON 损坏和字段校验失败，空内容或截断只自动重试一次，不再把 HTTP 200 后的解析问题模糊改写成「AI 暂时不可用」。
- ✅ AI 设置的「测试连接」已升级为最小结构化输出测试，不再只证明模型列表可读。诊断只保存服务商、模型、HTTP 状态、结束原因、响应长度、请求编号和错误类别，不保存 API Key、思考内容或原始回答。
- ✅ 对话、进度和系统状态已彻底分离：对话区只保存用户真实发送和 AI 真实生成的内容；理解、搜索、核对、整理改为临时转圈状态；单个来源失败使用无 AI 头像的系统提示。历史 v3 中已经落盘的两类本地伪回复也会备份后清理。
- ✅ 修复正常搜索被误判为「上次中断」：会话重读不再改写活动运行，只有应用重启时发现上一进程遗留的未完成记录，才在对话区之外提供恢复。搜索失败保留上一轮有效候选。
- ✅ 「太少了、质量不好、知名的没找到」等句子现在只增加筛选要求和搜索深度，不会替换原始目标。真实验收中「去文案 AI 痕迹」仍保留「直接改写、排除只检测」两个硬条件。
- ✅ 搜索升级为 skills.sh + GitHub 真实 `SKILL.md` + 可扩展编辑信任信号的多源协调；按「仓库 + Skill 路径」去重，验证 frontmatter、名称与目录、下载可用性和安全阻止。候选分批只用于稳定并发，删除 5、8、13、15 等产品数量截断。
- ✅ 热度不再单独决定推荐：必须同时满足真实 Skill 证据、需求匹配、来源可追溯和无阻止安全问题。实测中安装量 17.6K 但只检测不改写的 `dbs-ai-check` 被降为「其他可能相关」，未被热度推成强推荐。
- ✅ 真实前台搜索「能直接改写中文文案、去除 AI 痕迹，不要只检测」完成：两次 DeepSeek 结构化请求均返回 HTTP 200 + `finish_reason=stop`；累计保留 91 个真实核验候选，其中 10 个通过强推荐门槛，包含 44,222 次安装、15,485 Star 的 `humanizer-zh` 等代表候选。本轮一个独立公开搜索源临时未完成，其他来源的已核验结果没有丢失；随后已增加「单条 GitHub 查询失败仍保留其他查询」的回归保护。
- ✅ Swift 全量 166 项测试、14 个测试组全部通过；Release 生产构建、`Info.plist`、Hardened Runtime ad-hoc 签名和 `codesign --verify --deep --strict` 通过，最终应用已完成真实启动且进程保持运行。
- 📌 本地 ad-hoc 签名在重打包后可能触发 macOS 钥匙串的一次密码确认；正式公开版仍需 Developer ID 签名与 Apple 公证，本轮没有把本地测试包冒充为可公开发行版。

## 2026-08-17

- ✅ 完成「发现 Skills」v2：左栏改为真实的用户/AI 连续对话，每条用户消息只对应一条自然短回复；搜索中、失败重试和旧结果保留都有明确状态。寻找记录升级为 Schema v2，旧记录保留用户原话和候选，不伪造历史 AI 回复，并在首次迁移前备份 v1 JSON。
- ✅ 搜索链路升级为“原话 + 最多 3 条语义补充”：每条最多召回 20 个结果，按仓库与 Skill 去重后只核对前 13 个；“质量不高”只改变筛选偏好，新需求不会与旧需求机械拼接，连续中文需求无需刻意加空格。
- ✅ 建立双证据门：skills.sh 的 Skill 级说明只用于理解用途，只有从 GitHub Tree/Contents 实际定位并读取到 `SKILL.md` 的候选才有资格进入推荐或备选；仓库简介不会冒充 Skill 用途，真实证据缓存 24 小时以减少重复查询。
- ✅ 推荐分为“推荐”和默认折叠的“其他可能相关”：主列表最多 5 个，备选最多 8 个；推荐必须用途相关、内容可核对，并满足安装量、仓库 Star 或可信发布者条件。没有强候选时会如实显示暂无可靠结果，不用低质量候选填满页面。
- ✅ AI 正式拆为两次受控调用：第一次只整理目标、条件和查询计划，第二次只能从真实候选 ID 中排序并生成面向用户的用途、场景、示例和限制。未知候选、虚构 ID、损坏 JSON 或未通过本地质量门的 AI 回复会被丢弃并安全回退；未配置 AI 仍可按确定性规则搜索。
- ✅ 三栏界面完成收口：左栏固定 380 点并采用聊天消息样式；输入框与模型/发送操作分成上下两层；中栏标题固定、弱候选折叠；右栏固定为「Skill 详情」，紧凑展示安装量、Star 和更新时间，正文只保留用途、适合原因、场景、示例话术、三步体验和限制。
- ✅ 真实公开目录冒烟验证覆盖中文写作去 AI 味、Swift 开发和 Obsidian/飞书小众场景：高安装量真实候选能够被召回，小众弱候选保留但不会伪装成推荐。最终 Swift 全量回归为 153 项全部通过。
- ✅ Release 应用构建通过，`Info.plist`、Hardened Runtime ad-hoc 签名与磁盘完整性校验通过；最终视觉原型为 `design/ui/skillbox-discovery-v17.png`。本轮未启动或替换用户正在使用的 SkillBox，也未下载、安装任何候选 Skill。
- ✅ 「发现 Skills」输入框改为“搜索你想要的 Skill 功能”，文字与发送按钮垂直居中；输入框右侧新增模型菜单，用户能直接看到当前模型、切换已配置模型或进入 AI 设置。
- ✅ 设置页新增可用的「AI 助手」配置：支持 Agnes、DeepSeek 与自定义 OpenAI 兼容接口，可选择模型、填写并测试 API Key、删除密钥；密钥只进入 macOS 钥匙串，`ai-settings.json` 不保存密钥。
- ✅ AI 开启并配置后，发现页会用用户明确选择的模型整理搜索需求；调用失败时保留原话并继续公开搜索，不静默切换服务商，也不影响本地管理、安装和更新。
- ✅ 当前 AI 请求只发送发现输入框里的需求文字，不发送本地 Skill、私人仓库或文件内容。用户说明整理与安全语义复核仍是后续独立切片，界面没有伪装成已完成。
- ✅ 新增 4 项 AI 集成测试并完成 144 项 Swift 全量回归，覆盖当前默认地址与模型、设置文件不含密钥、连接测试鉴权以及模型选择真实生效；Release 应用构建、`Info.plist`、ad-hoc 签名与磁盘完整性校验通过。
- ✅ 修复全局状态提示常驻遮挡主操作的问题：普通完成结果改为右上角短暂轻提示，3.5 秒后自动收起；进行中的本机查看改在顶部工具区显示，不再占用发现页输入区。
- ✅ v16 视觉方向确认后，正式 SwiftUI 新增一级「发现 Skills」：大窗口为寻找过程、候选列表、详情三栏，窄窗口收成寻找记录菜单、候选和详情两栏；总览页增加同一流程的快捷搜索。
- ✅ 接入可替换的 `SkillDiscoveryProvider`，首个实现直接读取 skills.sh 公开搜索，无需 Node/npm 或 SkillBox 账号；使用真实公开接口回读确认当前字段为名称、来源和安装量。
- ✅ 公开候选最多补充 5 个去重 GitHub 仓库的真实 Star、仓库介绍和最近更新时间，并使用 6 小时内存缓存降低查询；界面明确这些信号属于整个仓库，不拿仓库介绍或 Star 冒充具体 Skill 的能力和安全结论。
- ✅ 新增本地「寻找记录」：可连续补充条件，可保留多条任务和未首选候选，开始新寻找不删旧记录；设置页可单条删除、全部清空、查看占用和在 Finder 打开。记录保存到 `SearchSessions/`的可读文件夹和版本化 JSON，不与中央 Skill 库混在一起。
- ✅ 「加入我的 Skills」已接回现有 GitHub Release/默认分支、完整下载、纯净包解析和安全预览链路。skills.sh 的 Skill ID 不再被误当成 GitHub 物理路径；多 Skill 仓库会按名称预选用户点击的候选，没匹配时要求用户选择，不会擅自把整库全部加入。
- ✅ 新增 3 项发现与寻找记录测试，全量 Swift 回归增至 140 项并通过；Release 构建、`Info.plist`、Hardened Runtime ad-hoc 签名与磁盘完整性校验通过。
- 📌 本轮未自行启动或替换用户正在使用的 SkillBox，也未下载、安装任何候选 Skill。未配置 AI 时的真实搜索、记录和安全加入主链路仍可独立工作。

- 🟡 用户确认 SkillBox v1.1 方向：增加可选 AI 助手、按任务发现优质 Skill 和加入前安全复核；默认 Agnes，支持 DeepSeek 与自定义 OpenAI 兼容接口。
- ✅ Spec v1.1 与 ADR-0008 已固化关键边界：AI 只解释和复核，本地规则继续决定完整性、阻止、安装和撤销；API Key 只进钥匙串；私人或本地 Skill 未确认不发送；服务商不会静默切换。
- ✅ 用户确认 AI 采用“后台统一、前台按任务出现”的交互：不把首页做成通用聊天；左侧增加一级“发现 Skills”。2026-08-19 进一步收口为 Skill 搜索只在“发现 Skills”完成，总览不再重复放置搜索框。
- ✅ Spec v1.1 与 ADR-0008 已同步新信息架构：明确“添加 Skill”与“发现 Skills”的任务边界、仓库级 Star 口径、用户说明取证顺序、无依据内容省略和无聊天历史边界。
- ✅ 完成情境化 AI 与“发现 Skills”v16 可操作原型及 5 张关键 PNG，覆盖总览智能搜索、发现候选、完整发现详情、我的 Skill 用户说明和加入前安全说明。
- ✅ 实际点击验证总览跳转、导航选中、候选切换、仓库 Star 更新、话术复制、安全弹窗与服务商切换；1500px 和 1080px 窗口均无横向溢出，主操作与技术详情保持可见，控制台无错误。
- 🟡 等待用户确认 v16 视觉与信息层级；确认前不接入真实 AI、不读取钥匙串、不上传 Skill，也不改动现有安装事务。
- ♻️ v15 的“发现 Skill 藏在添加菜单”方案已被 v16 一级导航方案替代，旧文件仅保留为设计演进记录。
- ✅ 修复 GitHub 根目录 Skill 的严重完整性缺陷：旧规则会把 `scaffold.sh` 等根目录运行文件归为可选，导致“纯净整理”后功能缺失。真实数据确认 GitHub 源码快照和历史版本都包含该文件，丢失发生在 SkillBox 的安装内容筛选层。
- ✅ 安装内容策略升级为无损默认：用途不明的顶层内容默认保留；`SKILL.md` 明确引用的文件或目录锁定为必需；普通用户不再通过技术文件勾选承担完整性判断。
- ✅ 旧版自动选择和用户选择规则带版本标记。旧规则会阻止继续安装并从 GitHub 同一版本重新核对，不能拿残缺的中央副本继续“提纯”；Release ZIP、明确子目录和作者清单保持原有权威边界。
- ✅ 新增 4 项包完整性回归覆盖根目录关键脚本、被引用的仓库样式文件、旧残缺规则自愈和未知顶层内容默认保留；全量 Swift 测试增至 137 项并通过。
- ✅ 完成通用的「GitHub 仓库 → 可安装 Skill」分层：SkillBox 中央库和 Agent 目录优先保存运行内容；明确且未被 Skill 主说明引用的 README、LICENSE、CI、贡献指南等仓库包装不再跟着安装。
- ✅ 新增可持久化的「安装内容清单」：优先使用 Release ZIP、明确 Skill 子目录或 `skillbox.json`；根目录混装时只需确认一次，后续更新自动沿用。
- ✅ 旧版整仓库 Skill 增加可恢复迁移；从「我的 Skills」或「安装到应用」进入时会先整理纯净版，避免把仓库主页文件当成 Skill 覆盖 Agent 中的纯净副本。
- ✅ 更新检查改为只比较已确认的可安装路径；仓库资料变化保持安静，新的可能运行目录会单独请用户确认，不会误报成「新版本」。
- ✅ 持久化数据升级为 Schema v3；迁移不下载、不静默替换文件，中央版本与 GitHub 来源记录仍在同一操作中备份和撤销。
- ✅ 经独立子 Agent 审查后落地其五项边界：ZIP 继续安全检查、多 Skill 不擅自选择、清单绑定仓库与版本、仓库资料变化不打扰、迁移可回滚。
- ✅ 全量 Swift 测试增至 133 项并通过；Release 应用完成构建，`Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验通过。本轮未向任何真实 Agent 目录写入文件。

## 2026-08-16

- ✅ 根据独立代码审查修复 GitHub 限流状态边界：应用启动只迁移“已确认公开且旧记录缺少额度类型”的历史暂停，不再把启动误当成用户刚完成登录，重开应用不会反复解除新的等待时间。
- ✅ 用户刚完成 GitHub 登录时仍会立即解除明确的未登录额度暂停；旧私人仓库、公开性未知和已登录额度暂停继续遵守 GitHub 的恢复时间，不再因历史字段为空被误清除。
- ✅ 修复公开仓库匿名回退只作用于单个请求的问题：Release 回退后的 Commit、默认分支回退后的 Tree 查询都会沿用匿名方式，不再对每个后续端点重复一次无效的认证请求。
- ✅ 三条修复均先补失败测试再完成最小实现；GitHub 更新与来源专项 49 项、全量 118 项 Swift 测试通过。Release 应用重新打包，并通过 `Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验。本轮未启动应用、未请求真实 GitHub、未写入任何 Agent 目录。
- ✅ 修复“已连接 GitHub，公开仓库仍被匿名额度暂停”的根因：已知公开仓库现在优先携带钥匙串中的 GitHub 用户令牌，使用认证查询额度。
- ✅ 补齐已登录后仍被旧暂停记录拦住的第二层问题：本地现在区分未登录与已登录额度，连接成功后立即解除旧的未登录暂停，不再等到原提示时间。
- ✅ 保留公开读取的独立性：登录过期或公开仓库不在 GitHub App 选择范围时，同一次检查会自动转为匿名，不提示连接私人仓库，也不在后续端点重复认证失败。
- ✅ 按 TDD 将原先“公开仓库必须匿名”的回归测试改为“已连接时优先认证”，并保持登录失效、未选公开仓库、私人仓库重连和 GitHub 限流边界测试通过。Spec v1 与 ADR-0006 已同步新规则。
- ✅ GitHub 来源与更新状态专项 45 项测试通过，全量 114 项 Swift 测试全部通过；Release 应用重新打包，`Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验通过。本轮没有读取或写出 GitHub 令牌，没有写入任何 Agent 目录。
- ✅ 修复事务撤销只恢复文件、没有恢复用户安装选择的问题：撤销卸载会恢复“已选择”，撤销新安装、接管相同内容或覆盖已有内容会恢复“未选择”，普通已管理版本更新保持原安装关系。
- ✅ 按 TDD 逐条复现并修复新安装、卸载、接管和覆盖四条撤销路径；撤销后重新生成同步计划均无重复安装或重复卸载动作，覆盖与接管授权也会一并清除。
- ✅ 全量 Swift 测试增至 111 项并全部通过；Release 应用重新打包，`Info.plist` 与 Hardened Runtime ad-hoc 签名完整性校验通过。本轮没有写入任何真实 Agent 目录。
- ✅ 根据最新产品范围收口：撤回本轮新增的 VoiceOver 专用代码，不再把 VoiceOver、减少动态效果等无障碍专项作为 v1 发布门槛；后续验收只聚焦添加、更新、安装、卸载和撤销的真实用户闭环。
- ✅ 首次完成真实 SkillBox 主路径鼠标与键盘验收：Skill 切换、详情展开/收起、添加菜单、安装矩阵、同名内容比较、单项安装确认、Esc 取消和侧栏方向键导航均符合预期；所有安装与替换确认都在真正写入前取消。
- ✅ 前台验收发现并修复使用示例提取缺陷：README 在代码块前写有“你可以对 Codex 说：”等引导句时，SkillBox 现在会继续读取代码块中的真实示例，不再展示只有前缀的空复制框；先补失败测试，再完成最小修复。
- ✅ 全量 Swift 测试增至 108 项并全部通过；Release 应用重新打包，`Info.plist`、Hardened Runtime ad-hoc 签名及完整性校验通过。
- 📌 本轮真实前台已覆盖鼠标与基础键盘路径。未确认任何安装、替换、卸载或删除，也未写入任何 Agent 目录；下一步转入核心生命周期闭环验收。

- ✅ GitHub 更新检查收口为本机仓库级单队列：同一仓库的多个 Skill 只检查一次；Release 有 ZIP 时只读 Latest Release，默认分支只在 Commit 变化后继续读取 Skill 目录。
- ✅ 新增并持久化 GitHub `ETag`，`304 Not Modified` 保留已知版本结论并停止后续请求；失败或临时限制不再伪装成成功检查，不会错误延后下次自动检查。
- ✅ 应用内自动检查间隔改为 24 小时；手动检查显示仓库级进度并可取消。大型 Skill 库优先检查最久未成功的仓库，达到 GitHub 临时限制后也不会永远卡住后排来源。
- ✅ 依据 GitHub 官方权限边界纠正登录策略：公开仓库始终匿名读取，不消耗或依赖私人仓库令牌；「连接 GitHub」只用于用户选中的私人仓库。已登录时添加未授权的公开仓库也会自动回退到匿名读取，不再误报权限问题。
- ✅ 主界面统一为「连接 GitHub」，公开仓库与私人仓库的用途说明分开；GitHub 临时限制改为平静的服务状态，不再向普通用户展示「查询次数」等技术话术。
- ✅ Spec v1 与 ADR-0005 已固化该方案；新增仓库归并、条件请求、公私权限分离、公平队列、24 小时策略和重启持久化回归测试，107 项自动化测试全部通过，Release 构建成功。
- 📌 本轮没有启动真实 SkillBox，没有向任何 Agent 目录写入文件；鼠标、键盘、VoiceOver 和减少动态效果仍属于独立前台验收门。

- ✅ 完成用户指南的可靠性收口：README 中明确的「使用示例」优先于内部 `default_prompt`；偏技术的默认指令、`Use when` 原始片段和 Agent 内部规则不再冒充用户教程。
- ✅ 修复 YAML `>-` / `|` 多行简介解析，旧的中央 Skill 在启动重新检查时也会自动刷新简介；真实 `zhaoji-writing` 记录已从 `>-` 恢复为完整可读文字，无需删除重导。
- ✅ Skill 列表改用中性的名称首字标识，普通脚本、说明文档和其他低级别扫描结果不再显示蓝色 `i` 或绿色状态图标。
- ✅ 移除普通 Skill 详情中常驻的「文件检查」卡片；本地静态扫描、导入阻止和安装保护仍完整保留，只有高风险或已阻止内容才显示「安装前需要确认」。
- ✅ 用户已确认 v14 可视化方向；新增多行元数据、示例话术优先级、技术话术省略、纯触发语不冒充用途、旧数据刷新和风险展示政策测试，全量自动化测试增至 93 项并全部通过。release 构建、`Info.plist` 和 Hardened Runtime ad-hoc 签名校验通过。
- 📌 真实 release 进程已重新启动，且只读回读确认旧简介迁移已落盘；Computer Use 在退出旧进程后返回 `native pipe closed before response`，因此本轮没有把真实鼠标、键盘、VoiceOver 和减少动态效果验收写成已通过。未写入任何真实 Agent 目录。

- ✅ 修复公开仓库被误判为私人仓库授权失效的根因：GitHub 未登录查询达到每小时上限时会返回 `403`，旧逻辑把所有 `403` 都当成登录问题；现在依据限流响应头保存可重试时间并显示“公开仓库无需连接私人仓库”。
- ✅ GitHub 版本结论与连接问题分开保存；临时限流、网络异常、登录失效不会再抹掉“已是最新”或“有新版本”等已有结论。
- ✅ 公开仓库与私人仓库登录彻底解耦：钥匙串令牌失效时自动退回公开读取；只有已确认的私人仓库才会出现“重新连接”或“允许访问”，仓库改名、删除与网络故障使用独立提示。
- ✅ GitHub 限流后暂停所有来源请求直到服务端给出的恢复时间，连续点击、批量检查和启动检查都不会继续消耗请求；旧版遗留的错误红色授权状态会在打开新版本时自动迁移为中性连接提示。
- ✅ 失效令牌只会被当前进程拒绝到它被替换为止；用户重新连接取得新令牌后可立即恢复私人仓库访问，不需要重启 SkillBox。
- ✅ 新增 12 项网络、状态、迁移和批量摘要回归测试；全量自动化测试增至 85 项并全部通过。原有 Release 纯净包、更新事务、撤销与指纹保护测试保持通过。
- ✅ release 应用重新打包，`Info.plist` 与 Hardened Runtime ad-hoc 签名完整性检查通过；真实前台首次检查时三个来源均落盘为公开且已是最新，随后再次触发 GitHub 真实限流时，界面正确显示恢复时间和“公开仓库无需连接私人仓库”，没有再引导连接私人仓库。
- 📌 `spctl` 仍会拒绝 ad-hoc 本地测试包；公开分发仍需 Developer ID 签名与 Apple 公证，这与本次 GitHub 更新逻辑修复相互独立。

## 2026-08-15

- ✅ 修复 GitHub 更新误报：旧记录中 `release:id` 与新版 `release:id:source:commit` 表达同一个源码包时，启动和检查都会自动归一为“已是最新”，不再提示假更新。
- ✅ 同一个 Release 下新增或切换为作者上传的纯净 ZIP 时，不再显示“发现新版本”，改为“有更干净的安装包”，让用户知道这是安装包来源优化，不是 GitHub 刚发布了新版本。
- ✅ 新增 4 个回归测试覆盖旧源码包兼容、同版本纯净安装包状态、应用启动时清理旧误报和旧状态自动转为安装包提示；自动化测试增至 70 项并全部通过。
- ✅ release 应用重新打包，`Info.plist` 与 ad-hoc Hardened Runtime 签名校验通过；`spctl` 仍因缺少 Developer ID 公证而拒绝，这是本地测试包的已知发布边界。
- ✅ 继续收紧批量检查文案：同版本纯净安装包不再被汇总成“有新版本”，而是单独提示“有更干净的安装包”；新增 3 个摘要测试，自动化测试增至 73 项并全部通过，release 构建与本地签名校验通过。

- ✅ 收紧「我的 Skills」两处高频操作区：页头以「添加 Skill」为唯一主操作，更新检查与私人仓库连接统一为次级按钮；安装卡拆分说明、可用数量和执行操作，避免主按钮换行。
- ✅ 安装卡会直接显示本机可安装应用数量；一个可用位置都没有时禁用全局安装，并明确说明 SkillBox 不会代为创建目录。
- ✅ 新增 v13 可视化预览，用于核对顶部操作层级、按钮尺寸和安装卡布局。
- ✅ 66 项自动化测试、release 构建、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 本轮未启动真实 SkillBox、未写入 Agent 目录；正式窗口中的鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立体验验收门。

- ✅ GitHub Release 导入改为优先使用作者上传的 ZIP 安装包：单一 ZIP 直接继续，多个 ZIP 弹窗让用户选择，没有 ZIP 时明确提示将导入测试、CI 和开发文件。
- ✅ 同名 `.sha256` 会在下载后自动校验，校验错误直接阻止导入；来源记录新增 Release ID、Asset ID、Asset 名称与已验证 SHA-256，同一 Release 中替换资源也能识别为更新。
- ✅ Agent Team 纯净包回归样本只导入 5 个运行文件，不包含 `.github/workflows/ci.yml`；未修改 Agent Team 运行文件或删除仓库 CI。
- ✅ GitHub Actions 工作流中纯粹的 `${{ github.token }}` / `${{ secrets.GITHUB_TOKEN }}` 改为蓝色“仅说明”；本地脚本读取令牌、通过变量外传或直接外传令牌仍保持高风险。
- ✅ 新增 v12 多 ZIP 选择与源码回退可视化预览，正式 SwiftUI 已接入同一流程；66 项自动化测试、release 构建、打包、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 本轮未启动真实 SkillBox 或写入 Agent 目录；新 Release 弹窗的真实鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立验收门。

- ✅ 「这个 Skill 怎么用」重做为普通用户指南：先说能帮什么，有资料时再展示适用时机、不适用情况、可一键复制给 AI 的示例话术，以及最多 3 步的可选体验流程。
- ✅ 使用指南改为渐进降级：优先读取 `agents/openai.yaml` 的用户展示信息，再读作者明确写在 `README.md` / `SKILL.md` 的用户章节，最后只保留 `description` 中可确定的用途。不完整时自动省略缺失项，不使用 AI 补写。
- ✅ 修复原有误判：`SKILL.md` 中的「执行步骤」、安全规则和 Agent 内部指令不再显示为用户流程；只有作者明确写了「使用时会发生什么」才展示对应步骤。
- ✅ 新增 v11 完整与简化两种视觉预览；55 项自动化测试、release 构建、`Info.plist` 和 Hardened Runtime 本地签名校验通过。
- 📌 不接入 AI 的前提下，任意新 Skill 都能至少展示作者在标准 `description` 里写下的用途；适用时机、示例话术和体验流程只在作者提供可靠资料时出现，避免用看似完整的错误内容误导用户。

- ✅ “安装到应用”矩阵改为直接单项操作：点击加号、已安装或冲突状态后当场弹出当前 Skill 与当前应用的确认窗，移除右上角二次寻找和批量执行入口。取消时不保存选择，确认时只执行这一项。
- ✅ 未管理的同名 Skill 在点击前就显示橙色叠层图标；点击后通过真实内容指纹告知“完全相同”或“内容不同”，不同时展示新增、修改和移除文件。修复了旧阻塞动作丢失两边指纹、可能把空值当作相同的根因。
- ♻️ 旧版曾将 `SKILL.md` 的「执行步骤」直接展示为「这个 Skill 怎么用」，已在本日最新迭代中替换，不再将 Agent 内部规则当作用户教程。
- ✅ 原来占据主位的文件内容提示改为默认收起的「文件检查」，主界面只告知提示数量和“安装时不会运行”，具体文件与命令仅在用户主动展开后显示。
- ✅ 新增 v10 交互说明与安装矩阵、同名比较弹窗、使用流程三张 PNG 预览；55 项自动化测试、release 构建、打包、`Info.plist` 和 Hardened Runtime 本地签名校验通过。
- 📌 本轮没有向真实 Agent 目录写入测试样本；Computer Use 通道之前持续返回 `native pipe closed before response`，因此真实 SwiftUI 的鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立验收门。

- ✅ “安装到应用”矩阵改为默认从滚动区左上角展示，移除导致宽度异常的无限拉伸；大窗口九个内置应用完整可见，小窗口仍可水平滚动查看右侧列。
- ✅ 将难理解的“使用前检查”重写为文件内容提示：首先说明添加和安装时不运行文件，再分别解释可运行脚本、变量位置清理命令和说明文档中的命令。
- ✅ 风险标签统一为“仅说明、需要了解、需要确认、无法添加”，仍保留实际命令和文件位置作为可核对证据。
- ✅ GitHub 验证码卡片补充普通用户说明：此步只确认身份，不创建仓库或修改文件；重新开始只会换取一个会过期的临时验证码。
- ✅ 新增 v9 布局与安全提示设计说明及两张 PNG 预览；52 项自动化测试、release 构建、打包和 Hardened Runtime 本地签名验证通过。
- 📌 真实 SkillBox 已启动，但 Computer Use 以应用路径和 Bundle ID 读取都返回 `native pipe closed before response`；本轮用本地 HTML + PNG 完成视觉兜底，未将真实鼠标验收写成已通过。

- ✅ 已注册正式 `SkillBox by Zhaoji` GitHub App，开启桌面登录流程，仅申请仓库内容与基础信息只读权限，关闭 Webhook，并允许任意 GitHub 用户安装。
- ✅ 已将公开 Client ID 和安装地址写入正式构建配置；普通用户无需创建或下载 GitHub App。Client Secret、私钥和用户令牌均不进入工程或应用包。
- ✅ 真实 GitHub 账号已将 SkillBox 安装到一个明确选中的私人仓库，GitHub 回读权限为代码和基础信息只读。未授权其他私人仓库或所有未来仓库。
- ✅ 私人仓库连接改为连续向导：身份确认后自动打开选仓库页面，SkillBox 自动等待并回读结果；界面显示等待进度、恢复入口和完成状态。
- ✅ GitHub 真实 Device Flow 起点接口返回有效验证地址、用户码、有效期和轮询间隔；52 项自动化测试、release 构建、打包和 Hardened Runtime 签名验证通过。
- ✅ 根据真实导入的 `vibe-project-foundation` 排查风险提示：两个 `rm -rf` 分别用于失败回滚和临时测试目录清理；扫描器不再一律标红，变量范围清理降为蓝色“请留意”，宽泛路径删除仍保持高风险，并在详情中展示实际命令。
- ✅ 应用启动时重新扫描中央 Skill 的风险报告，已有 Skill 无需删除重导即可获得新分级。
- ✅ 中央文件夹改用 Skill 名称，例如 `Library/vibe-project-foundation/content`；同名不同版本自动追加短编号。旧 UUID 目录会连同历史版本迁移，删除恢复遇到同名新 Skill 时也不会冲突。
- ✅ 未配置私人仓库的构建不再显示容易误解的“正在准备中”，改为“当前版本尚未启用”，并明确这不是加载过程、无需等待。
- ✅ 新增风险范围、旧报告重扫、可读目录、UUID 迁移、同名版本和恢复冲突测试；自动化测试增至 52 项。

- ✅ 私人仓库入口改为普通用户三步流程「确认身份 → 选择仓库 → 回到 SkillBox」，界面不再出现 GitHub App、Device Flow、Client ID 或“测试版本”等开发术语，并明确无需下载其他软件。
- ✅ 设置页补齐「管理可访问仓库」「断开连接」「清除 GitHub 信息」「在 GitHub 撤销授权」；断开只清除本机令牌且不影响公开仓库，清除信息会保留 Skill 内容并移除仓库跟踪资料。
- ✅ 新增断开令牌与清除 GitHub 信息的行为测试；自动化测试维持 46 项全部通过，release 构建通过。
- ✅ SkillBox GitHub 产品身份已完成注册和构建配置；私人仓库安装、登录和授权仓库回读需在真实账号验收后再标记完成。
- 📌 Computer Use 以应用名和 Bundle ID 读取真实 SkillBox 窗口仍返回连接中断；已生成 HTML 视觉说明，真实设置页的鼠标、键盘、VoiceOver 与减少动态效果验收仍未冒充通过。

- ✅ 完成消费级功能闭环修复：「我的 Skills」新增搜索、更新/安装/问题筛选和更新徽标；私人 GitHub 连接入口同时出现在 Skill 库、GitHub 添加页和设置页。
- ✅ GitHub 下载与更新准备新增可见进度和取消按钮；仓库没有正式 Release 时可一键改用默认分支，仓库地址会在提交前直接校验。
- ✅ 中央原件更新现在始终生成可撤销记录；撤销 GitHub 更新会同时恢复原件和来源版本状态。删除 Skill 后提供即时撤销，并恢复分类、选择和 GitHub 来源。
- ✅ 「自定义安装」更名为「安装到应用」，矩阵增加常驻图例；自定义位置支持编辑和安全移除，仍有受管理副本时会阻止换目录或移除。
- ✅ 最近操作现在显示 Skill 名称、操作类型、应用详情和恢复按钮；设置页限制内容宽度，左下角只保留非关键应用数量，实时状态移到内容区浮层。
- ✅ 自动化测试增至 44 项，debug/release 构建和 Hardened Runtime ad-hoc 签名校验通过。
- 📌 私人仓库真实连接仍依赖已注册的 SkillBox GitHub App 公开 Client ID 与安装地址；当前测试包未配置，因此按钮会用普通用户语言说明暂不可用，公开仓库不受影响。
- 📌 前台验收时 SkillBox 进程正常运行，但 Computer Use 以应用路径和 Bundle ID 读取均返回 `native pipe closed before response`；本轮未把真实鼠标、键盘和 VoiceOver 标记为通过。

- ✅ 用户确认 GitHub v7 原型后，已将新交互正式接入 SwiftUI：GitHub 导入可选“最新正式 Release”或“默认分支”，并在下载前明确说明是完整版本。
- ✅ Skill 详情新增 GitHub 来源卡片，覆盖有新版本、已忽略、停止检查、需要首次核对、失去权限、来源不可用与已是最新；文字、主按钮和更多菜单均可操作。
- ✅ 新增正式更新预览页：展示新增/修改/移除文件、风险变化、`SKILL.md` 前后对照、原有安装位置及阻止原因，并提供“只更新我的 Skills”和“更新并安装到原有应用”。
- ✅ 设置页接入 GitHub Device Flow，可查看 GitHub App 实际授权的公开/私人仓库、管理仓库权限和断开连接；断开前明确说明本地 Skill 不会删除。
- ✅ “检查全部更新”已放入「我的 Skills」页头；手动停止的来源不会发起请求，授权失效与普通无更新不会混为同一结果。
- ✅ 补齐两个产品边界：设置页只展示 GitHub 真实授权仓库；更新 Skill 不会把“已取消选择但尚未确认卸载”的位置顺带删除。
- ✅ 自动化测试增至 40 项，Swift 并发构建、release 打包、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 真实前台验收时首次读取到了旧进程画面；正常退出旧进程后，Computer Use 连续返回 `Sky Computer Use native pipe closed before response`，因此新 SwiftUI 的真实鼠标、键盘、VoiceOver 和减少动态效果仍保留为独立验收门，本次未用其他自动点击方式绕过。
- 📌 私人仓库真实登录、授权仓库回读与网络更新仍需 GitHub App 的公开 Client ID 和安装地址；本次未写入任何真实 Agent 目录。

- ✅ 将 GitHub 正式纳入 Skill 更新源：新 Skill 可选择跟随“最新正式 Release”或“默认分支”，旧 GitHub Skill 升级后默认跟随原分支并标记“需要核对一次”。
- ✅ 持久化数据升级为 Schema v2，新增仓库编号、Skill 子目录、当前版本、Commit SHA、目录 SHA、上次检查时间、忽略版本、停止检查与授权状态；升级不下载、不替换现有内容。
- ✅ 拆分“查看是否有新版本”和“确认后下载”；前者只请求版本与目录信息，后者按 Release Tag 或已解析的 Commit SHA 下载完整快照，不使用 Release 附件。
- ✅ 多 Skill 仓库按具体 Skill 目录 SHA 判断变化，README 或其他 Skill 的改动不会误报；已确认更新会展示新增、修改、移除文件和 `SKILL.md` 前后内容。
- ✅ 新增“忽略这个版本”和“停止检查更新”；忽略只针对当前 Release ID 或 Commit SHA，下一版仍会提醒，停止后不发起网络请求。
- ✅ 完成中央原件与已管理 Agent 副本的联合更新、失败回滚和撤销；外部改动、已消失安装位置和未托管目录仍会被保护。
- ✅ 完成 GitHub Device Flow 和 macOS 钥匙串基础能力；仅在 App 中保存公开 Client ID，Access Token 与 Refresh Token 不进入 JSON、日志或操作记录，授权被撤销时保留本地 Skill 并暂停检查。
- ✅ 交付 GitHub v7 可操作原型和 5 张截图，覆盖账号连接、导入方式、新版本、更新差异、授权失效；鼠标点击与页面跳转自动验证通过。
- ✅ 自动化测试增至 38 项，release 应用重新打包，`Info.plist` 校验和 Hardened Runtime ad-hoc 签名完整性检查通过。
- ✅ 用户已确认 v7 原型，正式 SwiftUI 接入情况见本日最新记录。

## 2026-08-14

- ✅ 「我的 Skills」新增个人分类：可新建、重命名、删除文件夹，把 Skill 拖入分类、拖回未分类，并自由调整文件夹与同组 Skill 的显示顺序。
- ✅ 分类与顺序单独保存在 `organization.json`；整理列表不会移动、改名或删除中央 Skill 原件，也不会改变已选安装关系；删除文件夹只把其中 Skills 放回“未分类”。
- ✅ 所有详情操作补齐悬停、按下和禁用反馈；「查看 Skill 详情」改为整行可点击，文字、箭头和留白区域均可展开或收起。
- ✅ 更新 v6 可操作原型与分类、详情悬停、新建文件夹三张截图；原型完成按钮悬停、文字点击展开、拖入分类和新建文件夹验证。
- ✅ 全量自动化测试增至 25 项，release 应用重新打包，并通过本地签名完整性校验。

- ✅ 重排「我的 Skills」右侧详情：身份信息、主要安装动作、安装状态、安全检查和内容详情形成清晰层级；删除操作保留但降低视觉权重。
- ✅ 「查看技术信息」改为「查看 Skill 详情」，新增完整只读目录、文件层级与大小展示，以及主 `SKILL.md` 的独立预览按钮。
- ✅ 目录在后台读取，软链接只展示不跟随；新增目录完整性测试，自动化测试增至 24 项，release 应用打包与本地签名校验通过。
- ✅ 更新 v5 可操作原型与详情、Markdown 预览两张截图；展开/收起、目录显示、预览打开/关闭点击验证通过。

- ✅ 每份 Skill 新增“安装到全部可用应用”“从所有应用卸载”和“删除这份 Skill”；安装与卸载仍先展示变更清单，删除前独立确认。
- ✅ 删除中央原件时先检查是否仍有 SkillBox 管理的安装副本；可删除内容移入本机 `Deleted` 目录，保留恢复空间。
- ✅ “安装到哪里”更名为“自定义安装”，用于逐个微调；全部安装只选择已检测到且可写入的应用位置。
- ✅ 扫描、同步预览和真正执行三层都阻止缺失目录；本机未安装的 Agent 只提示并跳过，不会创建对应文件夹。
- ✅ 定位“查看技术信息”卡顿来自主线程读取与一次排版整份长文本；改为后台读取、轻量摘要和独立原生文本窗口。
- ✅ 更新 v4 可操作原型与三张预览图；原型的全部安装弹窗、技术信息展开和自定义安装导航点击验证通过。
- ✅ 核心自动化测试增至 23 项，新增可恢复删除、在用阻止删除、缺失目标不创建和执行前目标消失保护；release 应用打包与本地签名校验通过。

- ✅ 修复空 Skill 库和空安装页的垂直漂浮：页面标题固定在顶部，空状态只在剩余内容区域居中。
- ✅ 空 Skill 库移除无意义分栏并提供电脑、GitHub 添加入口；空安装页不再显示空矩阵，改为引导添加 Skill。
- ✅ 总览中的同名内容条目升级为可点击入口；可按单组查看来源、内容编号并单选要加入 SkillBox 的版本，未选择前不能确认。
- ✅ 更新可操作 HTML 原型并生成空 Skill 库、空安装页、同名版本选择三张 v3 预览；原型交互、19 项自动化测试与 release 打包通过。
- 📌 `computer-use` 以应用路径和 bundle id 读取窗口均返回 `Sky Computer Use native pipe closed before response`；真实 SwiftUI 鼠标验收仍待通道恢复后补做。

- ✅ 完成面向非技术用户的全局文案降噪：导航、总览、Skill 详情、安装选择、安装确认、最近操作、设置、引导和错误提示统一改为日常语言。
- ✅ 将“中央仓库、指纹、事务、阻塞、接管、Agent 目标”等内部术语移出主流程；内容校验码收入可展开的「技术信息」。
- ✅ 更新可操作 HTML 原型并生成总览、欢迎页和安装确认三张新版预览图；19 项自动化测试与 release 打包通过。
- 📌 `computer-use` 已识别 SkillBox 运行，但读取窗口时本机管道连续中断；本次使用 HTML + PNG 完成视觉兜底，真实鼠标前台验收仍待补。

- ✅ 用户确认原型方向，开始先打磨功能、后优化 UI。
- ✅ 建立 Swift 6.2 + SwiftUI 工程，拆分 `SkillBoxCore`、`SkillBox` 与只读诊断命令。
- ✅ 完成九个内置 Agent 适配器、自定义目录防护、只读扫描、SHA-256 指纹、归并与冲突识别。
- ✅ 完成中央仓库、版本化 JSON、损坏元数据保护、安装矩阵、同步预览、备份、回滚和撤销。
- ✅ 完成本地文件夹与公开 GitHub 导入、手动更新、静态风险报告、容量/数量/路径穿越/软链接阻断。
- ✅ 完成功能型 SwiftUI 界面：资产总览、Skill 库、Agents 矩阵、同步预览、操作记录、设置和首次引导。
- ✅ 19 项自动化测试通过，包括写入中断后多目标回滚、同目标版本互斥、替换授权失效、未托管冲突、外部改动、撤销保护、损坏 JSON 和恶意压缩包。
- ✅ 真实环境只读扫描找到 172 个候选、37 组重复和 24 组同名冲突；扫描前后目录清单与文件指纹一致，证明零修改。
- ✅ 生成启用 Hardened Runtime 的 ad-hoc 本地测试 `.app`，并通过 `codesign --verify`。
- 📌 当前边界：未向任何真实 Agent 目录写入，未做鼠标前台验收，未使用 Developer ID 签名、公证或发布 DMG。

- ✅ 完成竞品调研与产品讨论，确认公开、开源、原生 macOS、中央原件库和安全同步定位。
- ✅ 冻结 Spec v1、MVP 和九个内置 Agent 路径合同。
- ✅ 记录原生本地优先、中央库与事务式实体复制两项架构决策。
- ✅ 完成可操作 UI 原型、资产总览/首次盘点/同步预览三张截图。
- ✅ 自动化验证导航、Skill 详情切换、安装矩阵、同步提示和首次盘点流程；按钮可访问名称检查通过。
- 📌 下一门槛：用户明确授权后，选择一个可恢复样本做真实同步验收，然后进入 UI 优化。

---

- ✅ 搭好开发前地基:`docs/` `app/` `design/` `scratch/` + `CLAUDE.md` 等 AI 工作层。
- ✅ 已创建 `docs/spec.md` 作为后续开发唯一准绳,并准备 `AGENTS.md` / `CLAUDE.md` 两个轻入口。
- 📌 下一步:确认结构 → 进入 M1 Spec v0,先写清问题、用户、场景、边界和成功标准。

<!-- 新进展继续加在本日期最上方。 -->
