# 进度日志

> 按时间倒序记录「做了什么、发现了什么、卡在哪」。这是项目的"流水账",越勤快越值钱。
> 每条建议格式:日期 + 做了什么 + 下一步。

---

## 2026-09-01

- ✅ 修复“幽灵分类”：真实数据中存在一个名称为“操盘”、数量为 0 的历史分类；旧界面主列表会过滤空分类，右键“移动到”却列出全部分类，造成用户从未见过或管理过该分类却在菜单中突然看到。展示策略现保留全部已保存分类，空分类显示 `0` 并继续提供重命名、删除入口；新增回归先因缺少空分类展示策略编译失败，最小实现后转绿。
- ✅ 经用户授权，已精确清理真实使用数据中唯一的空“操盘”分类 `2A9F06F3-0BFE-47EA-BABD-189215C14DC8`。操作前保存 `organization.json` 与完整 `library-state.json` 的两代可恢复备份；候选按最新状态重新生成并逐字段对比，5 个 Skill 的顺序、来源、安装和其余状态未变化。正式 `~/Applications/SkillBox.app` 重新启动后，两份状态均保持 `folders=0` 且没有非空 `folderID`，没有恢复该分类。
- ✅ `app/Scripts/test-all.sh` 完整门禁通过，新增“已保存空分类仍可见”用例；Release arm64 构建、ad-hoc Hardened Runtime 与 `codesign --verify --deep --strict` 通过。当前修复版仅形成项目内 Release 候选，尚未替换正式安装版、未发布 GitHub；正式安装版的数据异常已经清理，但以后新建空分类保持可见的程序修复仍须独立安装与发布。
- ✅ 将已经完成真实拖动验收的本地工作区冻结为 `v0.2.0 (Build 3)`，发布提交为 `1b43cad89933181cf1b8cba39cc9ef697ca79faa`。提交包含「我的 Skills」单行来源与大小、同组拖动占位／让位、独立落点线、右键移动与可恢复删除，以及相应测试和 v22–v26 视觉依据；仓库内的个人绝对路径已改为通用 `~/Applications` 表述。
- ✅ 最终发布门重新发现并执行 277 项自动化测试，全部通过；Release arm64 构建、ad-hoc Hardened Runtime 严格签名、包内隐私、应用图标系统回读、DMG 系统完整性、来源清单和隔离属性模拟均通过。隔离副本仍按正式策略得到 `manual-approval-required`，没有冒充 Developer ID 或 Apple 公证包。
- ✅ GitHub `master`、`v0.2.0` 标签和 Latest Release 均回读到提交 `1b43cad…`；Release 为公开、非草稿、非预发布，包含 `SkillBox-0.2.0.dmg`、`.sha256` 与 `-release.json`。从 GitHub 重新下载后的 DMG SHA-256 为 `7b9a2ea76c62c1828f338716217853a3061f3cbd0cd5d4f5f1a75375e97e8bed`，清单 SHA-256 为 `508e2124f0394e46c8acc3d08ed246f9d23d39fe4d3ce3bedb67f91ebed8dc97`，二者均与线上摘要和校验文件一致。
- ✅ 只读挂载重新下载的 GitHub DMG 后，包内 App 为 `0.2.0 (3)`，可执行文件 SHA-256 为 `7392dea51b3e2f2a32fbb103ba1858c5cf1d4faebe5a10e7fdd7ef1ba7522def`，严格签名和发布隐私门再次通过。该 GitHub 下载副本已复制到暂存路径并二次校验，再可恢复切换到 `~/Applications/SkillBox.app`；旧 `0.1.0 (2)` 整包保留为 `~/Applications/SkillBox-before-v0.2.0-20260901-004221.app`。
- ✅ 新正式版从 `~/Applications/SkillBox.app` 启动成功，进程路径准确；真实窗口回读为「我的 Skills」，显示 5 个 Skill、GitHub／本地来源、中央库大小与拖动整理提示。安装没有改动 `Application Support` 中的中央 Skill、排序数据或任何 AI 应用目录。
- 📌 三端产品运行内容已经对齐到 v0.2.0。仍未把“另一台干净 Mac 通过浏览器下载并走一次隔离后的‘仍要打开’”冒充已验收；这属于面向陌生用户安装体验的独立门槛，不影响本机正式版、GitHub 附件和来源提交的一致性。

## 2026-08-30

- ✅ 用户明确要求清理过期版本并强调不得误删后，先对个人 Applications 目录中所有 `SkillBox*.app` 逐个核对名称、容量、修改时间、版本号、可执行文件 SHA-256 与签名；同时检查 `/Applications` 和正在运行的进程路径。正式版只有 `~/Applications/SkillBox.app`，未发现系统 Applications 中的另一份或指向历史版的链接。
- ✅ 只将 6 个精确命名的历史包移入 macOS 废纸篓：`SkillBox-backup-iconfix-20260821-115857.app`、`SkillBox-before-placeholder-20260829-073300.app`、`SkillBox-before-reorder-runtime-20260829-082414.app`、`SkillBox-before-v26-20260829-001559.app`、`SkillBox.backup-20260828-014342.app`、`SkillBox.backup-20260828-224135.app`。未清空废纸篓，6 个名称均已通过 Finder 回读，仍可恢复。
- ✅ 清理后 `~/Applications` 中只剩正式 `SkillBox.app`；其可执行文件 SHA-256 仍为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，严格签名和 Designated Requirement 复核通过。项目源码、构建目录、Skill 内容、排序数据与 AI 应用目录都未纳入本次清理。

## 2026-08-29

- ✅ 用户完成候选版真实拖动验收并明确要求“替换”后，已将修复拖动命中边界的 Release 候选可恢复安装到 `~/Applications/SkillBox.app`。正式安装版可执行文件 SHA-256 为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，与验收候选一致；严格签名与 Designated Requirement 复核通过。
- ✅ 被替换的 `f143d037…` 版本已整包保留为 `~/Applications/SkillBox-before-reorder-runtime-20260829-082414.app`，可执行文件 SHA-256 为 `f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`，签名仍有效。新版已从正式路径启动，验收进程 PID 为 `97344`，窗口与侧边栏状态回读正常。
- ✅ 用户刚才在候选版中进行真实拖动后，`organization.json` 在 `08:23:06` 已保存新顺序，时间早于 `08:24:14` 开始的安装替换；安装过程保留了该顺序，没有用旧数据覆盖。本次只替换 SkillBox 应用包，没有写入任何 AI 应用目录。
- ✅ 用户对已安装的等高占位版本实测后再次发现“拖动时卡片不让位”。运行时轨迹确认：每张 Skill 卡片的真实坐标都正确，但外层 `ScrollView` 的尺寸偏好始终回报 `0 × 0`，导致所有指针位置都被误判为“列表外”，`previewOrder` 从未更新。前一轮仅验证了让位状态计算，没有覆盖这个真实命中边界，因此当时的“已修复”结论不成立。
- ✅ 拖动命中区现改为由当前分组内已测量的真实 Skill 卡片边界合并得出，不再依赖会回报零值的滚动区尺寸；插入线的宽度和起点也改用同一份真实边界。新增回归先因缺少实卡片边界策略编译失败，实现后转绿。
- ✅ 已对清除诊断日志后的 Release 候选执行真实 macOS 鼠标拖动：`html-deck-studio` 跨过 `zhaoji-writing` 中线后，后者立即上移占据原位，目标槽位同步腾出；鼠标移出列表并松手后，浮层、蓝线和临时顺序全部清除。`organization.json` 验收前后 SHA-256 同为 `92c610b4abed80bdd00d45c3fc015d8876eb6b447d9f10b4f06307cdd4b274f3`，未落地改变用户排序。
- ✅ `app/Scripts/test-all.sh` 完整门禁 276/276 通过，`git diff --check` 通过；最终 Release 候选可执行文件 SHA-256 为 `acc6cb11342458d98c384149c076f8a01c269e83753f979c3e8694aeac17ace9`，严格签名与 Designated Requirement 验证通过。该候选已打开供用户直接试拖，尚未替换 `~/Applications/SkillBox.app`；已安装版仍是用户刚才验出问题的 `f143d037…` 版本。
- ✅ 用户确认后，已把“等高占位槽＋独立跟手卡片”修复版按同卷可恢复方式安装到 `~/Applications/SkillBox.app`。正式安装版 SHA-256 与 Release 候选一致（`f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`），严格签名与 Designated Requirement 复核通过，并已从正式路径成功启动。
- ✅ 被替换的上一版完整保留为 `~/Applications/SkillBox-before-placeholder-20260829-073300.app`，SHA-256 为 `afd7e97772eaaadc752758a831632285175fc3b87242c84aa7578ac09929531a`。启动复核发现一条自 00:17 起残留的旧进程，已精确结束；当前只运行新安装实例 PID `72258`。本次没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 用户真实拖动已安装 v26 后发现卡片没有形成“互相让位”的松手结果预览，被拖卡片会与相邻卡片重影、压叠。根因是 SwiftUI 中同一张卡片同时参加 `LazyVStack` 排位动画和鼠标位移，布局层与跟手层没有分离；HTML 原型其实已经使用独立 ghost 与 placeholder，正式实现没有完整复现这层结构。
- ✅ 正式 SwiftUI 已改为“等高空占位槽＋独立跟手卡片”：被拖卡片在列表中只保留透明占位，目标变化时占位槽随 `previewOrder` 移动并推动其他卡片让位；视觉卡片单独跟随鼠标，蓝线固定在占位槽底部。新增回归先因缺少占位／浮层分离接口失败，修复后转绿；`app/Scripts/test-all.sh` 自动发现的 276 项测试全部通过，`git diff --check` 通过。新 Release 候选的 SHA-256 为 `f143d0374f6fe5f43d6e8859e5f27b22b551f8e2da4d77cd4b7dcdb3539c6e62`，严格签名与 Designated Requirement 通过；正式安装版尚未再次替换。
- ✅ 用户明确确认替换后，已将 v26 Release 候选按同卷可恢复方式安装到 `~/Applications/SkillBox.app`。安装版可执行文件 SHA-256 与候选一致（`afd7e97772eaaadc752758a831632285175fc3b87242c84aa7578ac09929531a`），严格签名与 Designated Requirement 复核通过，并已从正式路径成功启动。
- ✅ 原安装版完整保留为 `~/Applications/SkillBox-before-v26-20260829-001559.app`，其可执行文件 SHA-256 为 `049b750e39d85e27541e1d0e81ccbcca56440a970c80c8d7a3c4298f4347ebdb`。本次只替换应用包，没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 用户真实拖动 v26 时反馈“看不到落点线”。根因是蓝线被绑在被拖卡片或占位层的底边，与卡片阴影重合，截图中虽有 `2 px` 色带，用户感知上几乎等于没有。
- ✅ v26 HTML 原型和正式 SwiftUI 已同步改为独立落点层：蓝线固定在最终插入槽位、层级高于跟手卡片和阴影；拖出列表、取消或松手后仍统一清除。原型页面使用 `file://` 打开，已有页面需手动按 `⌘R` 重载才会读取新文件。
- ✅ 交互回归先在旧实现上因缺少“独立插入线位置”而失败，修复后 8/8 通过；`app/Scripts/test-all.sh` 自动发现的 275 项测试全部通过，`git diff --check` 通过。新 Release 候选的 `Info.plist`、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过；未替换已安装 SkillBox。
- 📌 重建后的 Release 候选已在真实 macOS 窗口回读，普通列表无蓝线残留，拖出列表后五个 Skill 顺序保持不变。辅助操作未能在鼠标按住的中间帧同时回传截图，因此“拖动中蓝线的实际像素观感”仍留给用户在已刷新原型中直接确认，不用静态测试代替这道真实体验门。

## 2026-08-28

- ✅ 用户确认 v26 后已接入正式 SwiftUI：Skill 卡片的来源图标移到尾部固定列，GitHub 使用可直接识别的章鱼猫标志，本地使用蓝色实心文件夹，存储大小固定在最右侧，不再因名称长短左右不齐。
- ✅ 原生逐行拖放已改为统一拖动会话：移动超过 `8 pt` 才启动，被拖行在列表轨道内跟手，相邻行实时让位，落点只在被拖行下方显示一根 `2 pt` 蓝线。松手、取消、离开列表、窗口或应用失焦、页面消失及对象被刷新移除都会统一清空临时状态，不再遗留蓝线、空位或浮层。
- ✅ 新增并更新 8 项列表交互回归，覆盖三类来源图标、上下落点、邻接顺序、完成后只产生一次移动且清空状态、取消后回到空闲。`app/Scripts/test-all.sh` 自动发现的 275 项测试全部通过，`git diff --check` 通过。
- ✅ 已生成 `app/.build/release/SkillBox.app` Release 候选；`Info.plist` 校验、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过。本轮只生成项目内候选，没有替换 `~/Applications/SkillBox.app`，没有改动中央 Skill 数据或任何 AI 应用目录。
- ✅ 根据用户对 v22 真实拖动界面的反馈，完成 v23 克制版列表：拖动落点只保留一根 `2 pt` 系统蓝插入线，去掉端点和「放到某 Skill 上方／下方」文字胶囊，不再额外撑开列表空白。插入线只在拖动且存在有效落点时显示，离开、取消或放置后立即清除；精确落点文字仅保留给 VoiceOver。
- ✅ Skill 卡片改为单行「名称＋尾部来源图标＋中央库大小」：GitHub 使用远程网络图标，本地来源使用文件夹图标，应用导入使用导入图标；来源全称改放悬停说明与辅助功能名称。新增 2 项回归后完整门禁 274/274 通过，Debug App 的 `Info.plist`、ad-hoc Hardened Runtime 与严格签名通过。
- ✅ 真实 macOS 窗口已验收普通状态和取消式拖动：普通状态没有任何插入线，拖动中只出现系统蓝线，按 Esc 后线立即消失，html-deck-studio 与 agent-team 顺序保持不变。可视化产物保存为 `skillbox-library-reorder-v23.html`、`skillbox-library-reorder-v23-normal.png` 和 `skillbox-library-reorder-v23-drag.png`；本轮只验收 Debug 候选，没有再次替换已安装 SkillBox。
- ✅ 用户通过「我的 Skills」列表整理 v22 原型后，已接入正式 SwiftUI：列表行只保留 Skill 名称、GitHub／本地来源与中央库占用大小，移除原简介和状态徽标；存储大小递归计算中央主 Skill 内的普通文件，不计安装副本、来源缓存和备份。
- ✅ 同组拖动按目标行上下半区解析为上方／下方，拖动中显示蓝色插入线和「放到 <Skill> 上方／下方」文字；邻接行和向下移动会先排除被拖 Skill 再计算落点，避免索引偏移。减少动态效果时保留落点而取消过渡动画。
- ✅ Skill 行右键菜单现只提供「移动到」已有文件夹和「删除 Skill…」。删除复用现有系统废纸篓与即时撤销链路；有受管理安装副本时继续先选「保留副本」或「全部卸载并清理」，不会因为从右键入口进入就绕过确认和预览。
- ✅ 新增 5 项 TDD 回归，覆盖命名上下落点、邻接向下排序、起始卡片不冒充落点、中央库嵌套文件大小和三类来源文案。`app/Scripts/test-all.sh` 完整门禁 272/272 通过（主测试 240 项，GitHub 来源隔离测试 32 项），`git diff --check` 通过。
- ✅ 从当前源码生成独立 Debug App 并通过 `Info.plist`、ad-hoc Hardened Runtime 与严格签名校验。真实 macOS 窗口确认五份 Skill 正确显示来源与 34 KB／46 KB／97 KB／5 MB／600 KB 的中央库大小，右键菜单显示「移动到」和「删除 Skill…」，拖动前台真实显示「放到 agent-team 上方」的插入线。本轮只停留在拖动预览后关闭验收副本，未落地改变用户排序，未触发删除，也未替换已安装 SkillBox。
- ✅ 用户确认进入下一步后，重新执行 `app/Scripts/test-all.sh` 完整门禁并通过，再由当前工作区源码重新运行 `package-app.sh release`。新候选 `Info.plist` 校验通过，ad-hoc Hardened Runtime 签名在磁盘有效并满足 Designated Requirement；可执行文件 SHA-256 为 `6017e49fce3a2808cf6c3b62d33b459592f1e1ee8959838913ae9b341350856e`。
- ✅ 已完成本机安装版的可恢复更新：先把候选复制到 Applications 同卷暂存位置并复核摘要与签名，精确结束旧安装进程后，将原 `~/Applications/SkillBox.app` 整体保存为 `~/Applications/SkillBox.backup-20260828-014342.app`，再把候选原子改名到正式路径。旧版备份 SHA-256 为 `693681db29f41f47760e8dda8a013ffb1d64a2c2f9a9e48e4415b9ddf24ff6ca`，签名仍有效，可用于回退。
- ✅ 安装后从正式路径真实启动，运行进程为 `~/Applications/SkillBox.app/Contents/MacOS/SkillBox`；正式安装版可执行摘要与 Release 候选一致。系统截图与精确 PID 辅助操作确认总览正常加载，并在 `agent-team` 详情中真实显示「安装前，有 1 项内容需要你了解」「待了解」与显式「查看并确认」，没有裁切或遮挡。
- 📌 本次只替换 SkillBox 应用包，没有修改中央 Skill 数据、任何 AI 应用目录或 GitHub。应用元数据仍为 `0.1.0 (2)`，本轮更新以可执行摘要和真实前台内容识别；原生 Computer Use 管道仍返回 `native pipe closed before response`，按用户已授权的系统截图与辅助操作完成兜底验收。源码与本轮设计产物仍保留在未提交工作区，未擅自创建 Git 提交。
- ✅ 用户明确要求“替换”后，再次从当前 v22 源码构建 Release 候选，`Info.plist`、ad-hoc Hardened Runtime、严格签名和 Designated Requirement 均通过；候选可执行文件 SHA-256 为 `049b750e39d85e27541e1d0e81ccbcca56440a970c80c8d7a3c4298f4347ebdb`。
- ✅ 已完成第二次同卷可恢复替换：原安装版保留为 `~/Applications/SkillBox.backup-20260828-224135.app`，其可执行文件 SHA-256 为 `6017e49fce3a2808cf6c3b62d33b459592f1e1ee8959838913ae9b341350856e`，签名仍有效。新版已原子改名到 `~/Applications/SkillBox.app`，验收时进程 PID 为 `60474`，可执行路径与候选摘要完全一致。
- ✅ 从正式安装路径真实回读“我的 Skills”：五张卡片只显示名称、GitHub／本地来源和 34 KB／46 KB／97 KB／5 MB／600 KB 大小；右键菜单真实显示“移动到”和“删除 Skill…”。验收只展开菜单后按 Esc 关闭，没有移动、删除、改变排序或写入任何 AI 应用目录；正式 SkillBox 已留在“我的 Skills”页运行。

## 2026-08-27

- ✅ 用户通过消费级交互闭环 v21 设计后，已将高风险确认、批量安装/卸载预览和「恢复到操作前」三条闭环接入正式 SwiftUI；「安装到应用」现有矩阵布局与状态样式保持不变。
- ✅ 高风险卡片新增显式「查看并确认」；确认摘要绑定 Skill ID、当前内容指纹和稳定风险证据，重启后保留，内容或证据变化后自动失效。确认后列表橙色标记与详情大卡同步收起，`blocked` 内容仍不可安装；批量安装公共边界也无法绕过确认。
- ✅ 批量安装与卸载改为内存草稿预览；预览前不持久化安装选择，「取消，不保存」后不保留接管或替换授权，也不写入应用文件夹。用户最终确认后才持久化草稿，并基于最新状态重新计划再执行。
- ✅ 「恢复到操作前」改为先展示位置级恢复清单，并准确说明中央原件是否受影响；打开或取消预览不改文件，确认后才调用现有安全恢复引擎，操作后的外部改动仍会被保留。
- ✅ 按 TDD 新增 5 项消费级闭环回归，覆盖风险确认持久与失效、安装公共边界、取消预览不留选择、取消不留替换授权以及恢复预览。项目完整测试门自动发现并通过 267/267 项；最终 Release 生产构建 18.67 秒通过，`Info.plist`、本地签名和 Designated Requirement 校验通过，`git diff --check` 通过。
- ✅ 用户明确允许 macOS 系统截图与辅助操作后，已对精确路径 `app/.build/arm64-apple-macosx/release/SkillBox.app` 做真实前台验收。先用 PID 与可执行路径识别并退出会抢占同一 Bundle ID 的旧安装版，随后在最新 Release 窗口完成三条安全闭环：风险卡可见「查看并确认」，关闭后仍保持「待了解」；批量安装预览明确提示尚未保存或写入，点击「取消，不保存」后回执为未保存任何安装选择；「恢复到操作前」先列出 1 个具体位置，取消后回执为未修改任何文件。三条路径的按钮、弹窗、取消出口及返回状态均无裁切或遮挡。
- 📌 本次只验证上述三条核心闭环的真实鼠标/辅助操作与像素结果，没有点击「我已了解」「确认安装」或「确认恢复」，没有写入真实 Agent 目录、替换用户应用或发布 GitHub Release。验收后已关闭测试进程并重新打开原安装版。完整应用遍历、键盘、VoiceOver、干净账户安装和线上发行仍是独立验收门；原生 Computer Use 管道本轮仍不可用，系统截图与精确 PID 辅助操作由用户单独授权后作为前台证据。

## 2026-08-22

- ✅ 产品负责人明确授权取消 Developer ID Application 与 Apple 公证的强制上线条件。当前正式策略改为 GitHub 提供 ad-hoc Hardened Runtime DMG，用户首次安装或更新后按“系统设置 → 隐私与安全 → 仍要打开”放行；历史记录中的证书、公证待办已由 Spec v1.9 与 ADR-0010 取代。
- ✅ 上线前安全复核新增三项 TDD 修补：发现规划发送前统一清理用户输入与历史意图中的秘密和本机路径；脱敏扩展到 `/Volumes`、`/private`、`file://`、带引号字段和 YAML 块；高风险分析增加 `nc`、`scp`、`sftp`、`rsync` 等常见凭据外传通道。
- ✅ 发布链新增 Release 调试信息移除、本机路径映射、最小包内容白名单、凭据与个人路径扫描、来源快照、JSON 清单、SHA-256 和隔离属性模拟。脚本明确拒绝 Developer ID、公证凭据与测试 GitHub 身份覆盖，不会自动上传或安装。
- ✅ 最终门禁全量通过：主测试 230 项、GitHub 来源拆批 32 项，共 262 项；Release arm64 构建、ad-hoc Hardened Runtime、严格签名校验、最小包内容、隐私扫描、图标系统回读、DMG 系统完整性、隔离属性拒绝模拟、JSON 来源清单和 SHA-256 均通过。
- ✅ 只读挂载最终 DMG 后独立复核：包内只有 `SkillBox.app`、应用程序快捷方式和中文安装说明；挂载副本签名与隐私门再次通过，DMG 和 JSON 清单的 SHA-256 回读一致。
- 📌 Developer ID 与 Apple 公证不再是当前上线阻断项。产物已达到“可上传 GitHub 的发布候选”；尚未替用户安装、未上传 GitHub。上线宣称仍需由用户或干净账户真实完成一次“仍要打开”安装和核心鼠标流程。

- ✅ 关闭上线前剩余三项数据一致性债务：现有安装目录改为 macOS 同卷原子交换并在交换后校验真实旧内容；最终提交边界出现外部编辑时会交换回去并停止，不覆盖用户改动。卸载先原子移出并校验，再删除。
- ✅ 删除撤销新增写前恢复日志；内容从废纸篓移动后若应用退出，下次启动会把内容退回原废纸篓位置并把事务标记为已回滚，不会留下“文件已恢复、列表却没有”的半完成状态。
- ✅ 新增 `library-state.json` 完整状态提交点；原有八份分项 JSON 继续作为兼容镜像，但启动只采用最后原子提交的完整代际。旧版只有分项文件时自动无损迁移，单份镜像来自不同代际也不会污染当前状态。
- ✅ 三项问题均先建立失败用例再修复；中央仓库 38 项、事务相关筛选 58 项通过，`app/Scripts/test-all.sh` 全量通过（主测试 227 项，8 组 GitHub 来源拆批各 4 项，共 259 项）。
- ✅ Release 生产构建 36.68 秒通过；`Info.plist`、Hardened Runtime ad-hoc 签名、`codesign --verify --deep --strict` 与 `git diff --check` 通过。构建仍保留一条钥匙串非交互保护 API 的弃用警告；它不影响当前构建，暂不移除这层防止密码弹窗的兼容保护。
- 📌 本轮未安装或替换用户应用，未写入真实 Agent 目录，未上传 GitHub。当时记录的签名公证门已被本日 Spec v1.9 与 ADR-0010 取代；干净账户安装和真实前台验收仍保留。

## 2026-08-21

- ✅ 按上线前安全审计建议完成阻断项修补：大体积文本和被 `SKILL.md` 引用的说明文件继续执行高风险指令扫描；启动扫描增加文件、目录、深度、总量和时间预算；GitHub 元数据与目录树增加响应上限，并拒绝服务端标记为不完整的结果。
- ✅ 中央 Skill 更新在替换前核对实时内容，版本归档必须与目录名指纹一致，并通过独立临时目录完成校验后提交；本地导入的临时文件只按明确登记的精确目录清理，自定义安装位置同时拒绝中央库的上级目录。
- ✅ AI 验证结果绑定精确的服务地址、模型和授权代际；切换配置或删除密钥会立即撤销旧任务写回权限，避免迟到响应把新配置或已删除密钥重新标成可用。更新进入提交阶段后不再向用户显示虚假的“已取消”，GitHub 连接数量和凭据状态文案改为真实语义。
- ✅ 先用失败用例复现上述问题，再完成回归；`app/Scripts/test-all.sh` 全量通过（主测试 224 项，8 组 GitHub 来源拆批各 4 项），Release 生产构建、`codesign --verify --deep --strict` 与 `git diff --check` 通过。
- 📌 本轮修补未解决三个低风险架构债务：安装目标最终检查与替换之间仍有极短竞争窗口，删除/撤销恢复尚未全部纳入写前日志，多份 JSON 状态仍分文件提交。它们不再作为本轮高、中风险阻断项，但后续应继续事务化收口。
- 📌 当前产物仍是 ad-hoc 本地候选；完整鼠标前台回归、Developer ID 签名、Apple 公证、Gatekeeper、干净账户安装与 GitHub Release 下载哈希回读仍未通过，因此尚不能标记为正式上线。

- ✅ 修复 SkillBox 每次启动反复要求输入 Mac 登录密码：启动阶段不再读取 GitHub Token 或六类 AI API Key，只恢复不含秘密的连接提示；超过 24 小时的公开 GitHub 自动检查继续匿名执行，私人来源只在用户主动操作后使用凭据。自动钥匙串读取同时禁止 LocalAuthentication 与 Security 授权界面；用户明确“连接 GitHub”或“保存并测试”时，才允许为新应用身份重新绑定一次旧凭据。
- ✅ 真实 Release 冷启动复验先连续复现 `SecurityAgent` 的 `confirm-access-password` 窗口，再在最终实现中确认 SkillBox 进程保持运行且系统无 `SecurityAgent` / `authorizationhost`，统一日志也没有密码确认或钥匙串授权窗口记录。Computer Use 对开发构建的窗口树仍返回 `native pipe closed before response`，因此只将“启动密码弹窗消失”判为 PASS，完整鼠标前台门仍保持 BLOCK。
- ✅ 完成最新候选的九项安全修复：危险 `SKILL.md` 指令不再被文档规则降级；AI 密钥只会发往已验证的精确服务地址与模型；私人内容确认绑定接收方；候选比较与使用说明统一脱敏；未知或私人仓库证据默认不进入 AI；自动回滚、中央更新和中央删除均在崩溃或并发变化下保护较新内容；发现、GitHub 与 OAuth 响应增加硬上限；GitHub 和 AI 凭据改为仅本机且设备解锁时可访问。
- ✅ 新增 `Scripts/test-all.sh`，运行时自动发现全部测试，并把已知可能令 SwiftPM 助手停留的 GitHub 来源组拆批。当前 16 个普通测试组 214 项、GitHub 来源 30 项，共 244/244 通过；高风险、AI、发现、中心库、事务、删除中断、更新中断、网络上限、启动无秘密读取和钥匙串回归均在其中。
- ✅ 最终 Release 生产构建 17.33 秒通过；`Info.plist`、Hardened Runtime ad-hoc 签名与 `codesign --verify --deep --strict` 通过。生成包位于 `app/.build/release/SkillBox.app`，未替换用户安装版、未上传 GitHub Release。
- ✅ Release 生产构建 53.46 秒通过；`Info.plist`、品牌资源、Hardened Runtime ad-hoc 签名和 `codesign --verify --deep --strict` 通过。打包脚本现在只接受 `debug/release` 并在组包前清除精确旧包，避免陈旧文件混入。
- ✅ 图标集成回读在复用 `.build/release` 路径时复现 LaunchServices 旧路径缓存，特征距离 1.165；同一签名包复制到全新路径后为 0.592 并通过。正式发行脚本因此每次使用唯一暂存目录，并在制作 DMG 前调用 macOS `NSWorkspace` + Vision 复验真实系统图标，避免把缓存误判成包内资源故障，也避免漏掉真正的默认图标回归。
- ✅ 新增 `Scripts/release-distribution.sh`，公开发行强制要求干净 Git、全量测试、Developer ID、带时间戳签名、DMG、Apple 公证、票据装订、Gatekeeper 与 SHA-256；缺少真实发行证书时原子停止。本机 ad-hoc 包的 Gatekeeper 结果为 `rejected`，因此当前结论仍是“本地发布候选”，没有冒充已上线版本。
- 📌 仍待真实可恢复 Agent 闭环、当前候选完整鼠标前台回归、Developer ID/公证、干净账户安装和线上 Release 下载哈希校验；本轮未替换用户已安装应用，未写入真实 Agent 目录，未发布远端 Release。
- 📌 已尝试启动当前 Release 候选做只读前台烟雾验收，进程可启动，但 Computer Use 连续返回 `native pipe closed before response`，未取得窗口树或鼠标证据；本轮启动的测试进程随后已关闭。该门保持 BLOCK，不以进程启动或静态检查代替真实前台 PASS。
- ✅ 中央 Skill 删除位置由应用内 `Deleted/` 改为 macOS 系统废纸篓；使用系统返回的实际废纸篓 URL 支持一次即时撤销，清空废纸篓后明确无法恢复。SkillBox 不再为正常删除继续保留第二份中央内容。
- ✅ 内容早已缺失或历史数据与另一条同名记录共享中央路径时，只清理失效记录与管理关系，不移动新记录内容、不制造空备份；旧版 `Deleted/` 遗留内容会在新应用启动时迁入系统废纸篓。
- ✅ 新增系统废纸篓注入、失效记录零占用和旧删除目录迁移回归；中央库、同步事务、删除流程与本地开发源共 60 项相关测试通过。Release 生产构建 50.19 秒、Info.plist 与 ad-hoc Hardened Runtime 签名校验通过。新测试包真实启动后，应用内 `Deleted/` 已为空，已知旧备份出现在 `~/.Trash/1786718892-AD1EF41A-467A-4D32-AD52-CB75B5071419`；目录中只剩新的本地开发源记录。Apple 官方确认废纸篓未清空前仍占磁盘，只有用户清空后释放空间。
- ✅ 修复旧 Skill 点击清理后仍留在列表的问题。真实数据表明旧 `obsidian-feishu-sync` 的中央内容已经缺失，但目录记录仍占用 `Library/obsidian-feishu-sync/content`；新本地开发源导入只检查磁盘目录，错误复用了同一路径，导致两条记录共享中央内容位置。
- ✅ 中央目录分配现在同时检查磁盘和目录记录，内容缺失的旧记录也会继续保留其路径占用；残缺或历史共享路径记录可只清理元数据和管理关系，不再因中央内容缺失失败，也绝不会移动另一条同名记录的内容。本机已安装副本继续保留。
- ✅ 按 TDD 新增 3 项回归，覆盖缺失目录的路径保留、残缺同名记录清理不伤替代版本，以及 AppModel 完成清理后列表快照、安装副本和撤销入口的一致性。16 组共 228 项业务测试全部通过；`SourceProviderTests` 整组入口仍偶发停在 SwiftPM 助手，拆成 4 批后 30/30 通过。Release 生产构建 55.65 秒通过，Info.plist 与 ad-hoc Hardened Runtime 签名校验通过。
- ✅ 修复 Dock 仍显示 macOS 默认蓝图图标：根因是手工组装的 SwiftPM 应用同时声明 `CFBundleIconName=AppIcon`，LaunchServices 优先走了未正确关联的资产目录；同时构建号始终为 1，使错误解析继续被缓存。已删除该错误声明并将构建号递增为 2。
- ✅ 新增 `AppIconIntegrationCheck.swift`，直接通过 macOS `NSWorkspace` + Vision 特征距离比较“系统为 `.app` 解析的图标”与包内品牌图标；错误安装版稳定红灯 1.084，修复后正式安装版绿灯 0.593。构建号 2 已备份式替换、重新注册并从 `~/Applications/SkillBox.app` 真实启动；签名、运行路径和系统解析图标回读通过。
- ✅ 将用户确认的白色 `S` 角色正式定为 SkillBox IP 与应用 Logo；保留原始角色母版，并生成适合 macOS 浅、深色桌面的银白圆角图标母版。
- ✅ 接入 16–1024 px 完整 AppIcon 资源、`Assets.car` 与 `.icns` 兼容资源；打包脚本会将两者写入应用包，`Info.plist` 已声明 `SkillBox.icns` 品牌图标。Release 真实打包、资源回读、Hardened Runtime ad-hoc 签名与 `codesign --verify --deep --strict` 通过。
- ✅ 已将新 Logo 版本替换到 `~/Applications/SkillBox.app`，原安装版保留为带时间戳的可恢复备份；旧开发路径进程已退出，新应用已从正式安装位置真实启动，安装后签名与图标资源回读一致。
- ✅ 用户确认后清理已过期的旧安装备份与 SwiftPM 可重建构建缓存，共释放约 511 MB；清理前后均核对绝对路径，正式安装版、Logo 资源、签名、源代码和 Git 改动未受影响。
- ✅ 完成“删除 SkillBox 主 Skill”的双路径：默认只清理中央主 Skill 并解除管理关系，应用中的现有副本保持不变；也可先预览并卸载当前记录的全部受管理副本，事务安全完成后再清理中央主 Skill。其他同名记录和未受管理副本不受影响。
- ✅ 将已确认的 v19 双选项稿接入正式 SwiftUI，明确列出受影响应用、“不改应用文件”安全标签和后续卸载清单；卸载预览在该流程中改为“确认卸载并清理”，返回调整会取消待清理状态。
- ✅ 删除备份新增原管理关系；即时撤销只在应用副本指纹未变化且位置未被其他安装接管时恢复管理。副本已变化或缺失时只恢复中央 Skill，不向应用目录写入、不覆盖、不删除应用文件。
- ✅ 按 TDD 新增 5 项回归，覆盖中央清理不碰应用文件、未变化副本安全接管、已变化副本保持不受管理、卸载预览前中央 Skill 仍存在、确认卸载后才清理。中央仓库 27 项和删除流程 2 项分别通过，独立 Release 生产构建 33.42 秒通过，`git diff --check` 通过。
- 📌 无过滤的 Swift 全量运行再次在业务用例后半段出现 SwiftPM 测试助手 0% CPU 停顿；按组复验时 GitHub 授权 4 项和本次删除流程 2 项通过，随后 SourceProvider 组同样停在助手层。已停止无进展进程，不把它写成全量 PASS，也没有启动或替换用户当前使用的 SkillBox。

## 2026-08-20

- ✅ 按 TDD 完成发现质量收口：没有相关性证据的未评估候选不再进入推荐区；某一条 skills.sh 或 GitHub 查询失败时保留其他已核验结果，并准确记录为部分失败，不把整轮搜索清空。
- ✅ AI 连接状态改为绑定“服务地址 + 模型”的真实结构化探测，保存密钥不再等于已连接；内置服务地址固定为官方端点。发送 Skill 资料前会清除令牌、认证头、私钥、用户目录等敏感信息，未知仓库隐私默认不上传。
- ✅ 移除没有资料依据的用途、场景、三步流程和提示词补写；作者资料与 AI 辅助说明现在分别标注来源，旧的无来源 AI 缓存不会继续复用。AI 和 GitHub 下载均改为流式计数，超过上限立即停止读取。
- ✅ 高风险 Skill 在核心存储层和界面层都需要第二次明确确认，确认页展示命中文件、规则和证据；新增浏览器资料外传、编码载荷执行、启动项持久化三类高风险识别，已阻止内容仍不可绕过。
- ✅ 压缩包检查与解压子进程增加输出上限、30 秒超时、取消和强制终止兜底；展开大小累加增加溢出保护。自定义安装位置在保存前解析软链接，避免之后被软链接改指到另一个目录；纯标点 Skill 名称也不会再生成空记录。
- ✅ 同步事务改为“先写恢复记录，再改文件”，应用启动时会恢复中断的安装、卸载和中央 Skill 更新；中央更新与应用部署共用一条事务记录。恢复前会核对中央原件仍是操作前或操作后的版本，发现更晚的外部改动就停止，不会静默覆盖。多窗口改为共享同一份 AppModel，减少窗口之间互相覆盖状态。
- ✅ 14 个测试组共 211 项用例全部通过，含 30 项 GitHub 来源与压缩包用例逐条复验；独立 Release 生产构建 34.40 秒通过，最后一处事务保护后的增量 Release 复验 15.75 秒通过，`git diff --check` 通过。SwiftPM 全量运行在所有业务用例完成附近仍偶发只剩测试助手不退出，因此最终门采用独立 scratch + 分组逐项验收，不把运行器异常冒充业务死锁或完整全量进程 PASS。
- 📌 本轮未启动或替换用户正在使用的 SkillBox，未调用真实 AI、未读取或记录 API Key，也未向任何真实 Agent 目录安装 Skill；下一步是用户人工体验发现、风险二次确认和崩溃恢复界面。

- ✅ 真实前台验收复现 DeepSeek 候选比较失败：同轮需求整理 HTTP 200，但 8 个候选的比较请求返回 HTTP 400。根因是请求开启 `response_format: json_object`，候选比较与使用指南的 system 消息却没有明确包含 `json`，不符合 DeepSeek JSON Output 契约。
- ✅ 以公共 `OpenAICompatibleProvider` 边界先写失败测试，再在结构化请求公共层自动补齐 JSON 模式指令；需求整理、候选比较和四段式使用指南现在共同受这一规则保护，没有散落修改业务提示词。
- ✅ 新增 DeepSeek 三类结构化任务回归；AI 集成 20 项、Swift 全量 193 项 / 14 组全部通过，Release 生产构建 26.65 秒通过。验证未调用真实 API、未读取或记录 API Key，也未向任何应用目录安装 Skill。
- ✅ 用三路独立子 Agent 完成代码盲审，并按可复现证据修正了候选 ID 冲突崩溃、AI 部分返回误删候选、第 9 个以后候选漏掉、搜索记录并发覆盖、删除后被后台保存复活、取消被吞掉以及重复提交等问题。
- ✅ 修复后二次盲审继续截住混合虚构候选仍可污染 AI 回复、同 ID 新版 `SKILL.md` 继承旧指南、迟到的指南响应跨版本落盘，以及取消后的 GitHub 旧任务覆盖新候选和进度。异步搜索、指南、GitHub 预览、更新与内容整理现在都只允许当前任务代际写入状态。
- ✅ 收紧发现链路的 AI 上下文预算：单次最多比较 8 个候选，所有候选证据合计最多 8000 字符，评估输出最多 1500 tokens，需求规划输出最多 600 tokens；自动重试不得突破当前阶段预算。
- ✅ 使用指南的发送材料上限收紧为 12000 字符、整阶段输出上限 1200 tokens；诊断中任一次请求缺失 token usage 时保留为“未知”，不再用 0 或部分数值冒充整轮总量。
- ✅ 修正 AI 排序边界：已交给 AI 且被判为「其他」的 Skill 不会再被热度或本地回退规则推回推荐区；因 AI 预算未参与评估的已核验候选，仍按确定性规则排序，不会被静默丢失。
- ✅ 四段式使用指南从「批量搜索时全部生成」改为「用户点开某个候选时按需生成」；生成结果先保存到寻找记录，后续加入我的 Skills 时按内容指纹复用，失败时保留本地降级说明。
- ✅ 候选指南现在绑定实际 `SKILL.md` 的内容身份；下载到的 Release、分支或版本与搜索时内容不一致时，不复用旧说明，改从真正加入的内容重新生成。
- ✅ 按需指南同一时刻只保留一笔 AI 请求；切换候选会取消上一笔，详情页显示当前整理状态和“停止”操作，不再因快速点击并行产生多笔 30 秒请求。
- ✅ AI 诊断新增请求大小、输入/输出 token、耗时、尝试次数和候选数等数值指标，不保存用户原文、候选正文、推理内容或 API Key。
- ✅ 发现、AI、指南与扫描针对性 79 项测试通过；最终固定代码基线在沙箱外独立 scratch 目录中完成 14 组、192 项 Swift 全量测试，实际测试用时 1.668 秒；独立 Release 生产构建 40.47 秒通过，最后一行代码后的增量 Release 复验 18.73 秒通过。受限沙箱内偶发的无输出停顿定位在 SwiftPM 测试启动助手层，当时尚未创建 SkillBox 测试子进程，本轮不将它误写为业务代码死锁。
- ✅ 定位发现搜索的效率瓶颈：首次搜索曾对每个查询最多召回 1000 条，且在去重前反复读取候选的仓库资料和完整 `SKILL.md`；随后又把几十份长文同时送入 AI 比较，造成数分钟等待和网络超时。
- ✅ 首次搜索改为 24 条快速召回，用户主动「继续深挖」时再扩展到 96 条；这两个数字只是单次网络预算，不是候选展示上限，所有通过精品门槛的 Skill 仍会保留。
- ✅ skills.sh 先依据可信来源、Star 和安装量建立质量前沿，再读取 Skill 详情；GitHub 先合并不同查询的重复命中，再为有可靠公开证据的仓库读取真实 `SKILL.md`，同一 Skill 不再反复验证。
- ✅ AI 候选比较只接收通过本地质量门的候选和最多 6000 字的精简证据，不再上传几十份完整长文。
- ✅ 修正截图中的误导提示：某个 GitHub 查询词失败不再被说成「3 个公开来源未完成」；只有整个独立来源不可用时才显示来源级降级提示，已核验结果继续保留。
- ✅ 新增 AI 请求体积、质量前沿、快速/深挖预算、弱仓库跳过、跨查询去重和失败语义回归；Swift 全量 177 项测试通过，Release 生产构建与本地签名校验通过。

## 2026-08-19

- ✅ AI 设置已从三种服务扩展为 Agnes、DeepSeek、Kimi、MiniMax、GLM 与自定义 API。内置服务预设官方兼容地址和推荐模型；用户在同一页选择服务、填写 API Key、选择模型并“保存并测试”，只有完成最小结构化输出测试才显示已连接。
- ✅ 旧版 `ai-settings.json` 采用无损补齐迁移：保留用户已有的服务选择、模型、自定义地址与私人内容开关，只追加缺失的新服务；六个服务的密钥继续按服务分别保存在 macOS 钥匙串。
- ✅ 普通设置中的无操作“使用数据”陈列已移除；v18 原型与正式 SwiftUI 都不再用一行不可点击的说明占用界面。
- ✅ 移除总览页重复的 Skill 搜索框，搜索需求统一进入一级导航「发现 Skills」。设置页 v18 与正式 SwiftUI 均按「通用、AI、GitHub、数据」四类收口；API、仓库列表、清理与断开等复杂操作不再堆叠在同一张长页面。
- ✅ 新增旧 AI 设置迁移回归，AI 专项 11 项、Swift 全量 171 项测试全部通过；Release 生产构建完成。

## 2026-08-18

- ✅ 收口「发现 Skills」三栏交互：新建寻找改为记录图标左侧的加号；寻找、候选和详情共用 74 点固定标题高度与同一水平分隔线；候选标题下的重复备注已移除。
- ✅ 发现详情与「我的 Skills」统一为四块用户内容：「Skill 简要说明（特色、技巧）、适用场景、体验流程、提示词参考」。AI 优先依据真实 `SKILL.md`，可结合 README、`agents/`、`references/` 和 GitHub 作者简介，一次生成普通用户能直接看懂和使用的说明。
- ✅ 新增版本化使用说明缓存：按 Skill ID、内容指纹和提示词版本保存；发现页加入中央库时直接复用候选说明，本地或私人 Skill 只有用户开启明确的内容发送开关后才使用 AI；同一版本日常打开不会重复生成。
- ✅ 精品筛选进一步收紧：主候选区只显示强推荐，弱候选保留在内部证据但不向用户充数；GitHub Star 成为高优先级排序证据，同时继续要求真实 Skill 内容、需求匹配、来源可追溯和无阻止问题。推荐数量仍由质量决定，不设固定数量。
- ✅ 统一「发现 Skills」三栏顶部基线：寻找、候选与详情区域现在共同占满可用高度并从页面顶部开始排布，三个标题和标题分隔线保持同一水平位置；空状态仍在各自内容区居中。已完成真实前台截图验收、166 项 Swift 测试和 Release 构建验证。
- ✅ 完成「发现 Skills」v2.1 根因修复：DeepSeek 规划与评估请求启用 JSON 输出模式、关闭思考模式、扩大完整输出空间；现在会区分网络、鉴权、限流、空内容、截断、JSON 损坏和字段校验失败，空内容或截断只自动重试一次，不再把 HTTP 200 后的解析问题模糊改写成「AI 暂时不可用」。
- ✅ AI 设置的「测试连接」已升级为最小结构化输出测试，不再只证明模型列表可读。诊断只保存服务商、模型、HTTP 状态、结束原因、响应长度、请求编号和错误类别，不保存 API Key、思考内容或原始回答。
- ✅ 对话、进度和系统状态已彻底分离：对话区只保存用户真实发送和 AI 真实生成的内容；理解、搜索、核对、整理改为临时转圈状态；单个来源失败使用无 AI 头像的系统提示。历史 v3 中已经落盘的两类本地伪回复也会备份后清理。
- ✅ 修复正常搜索被误判为「上次中断」：会话重读不再改写活动运行，只有应用重启时发现上一进程遗留的未完成记录，才在对话区之外提供恢复。搜索失败保留上一轮有效候选。
- ✅ 「太少了、质量不好、知名的没找到」等句子现在只增加筛选要求和搜索深度，不会替换原始目标。真实验收中「去文案 AI 痕迹」仍保留「直接改写、排除只检测」两个硬条件。
- ✅ 搜索升级为 skills.sh + GitHub 真实 `SKILL.md` + 可扩展编辑信任信号的多源协调；按「仓库 + Skill 路径」去重，验证 frontmatter、名称与目录、下载可用性和安全阻止。候选分批只用于稳定并发，删除 5、8、13、15 等产品数量截断。
- ✅ 热度不再单独决定推荐：必须同时满足真实 Skill 证据、需求匹配、来源可追溯和无阻止安全问题。实测中安装量 17.6K 但只检测不改写的 `dbs-ai-check` 被降为「其他可能相关」，未被热度推成强推荐。
- ✅ 真实前台搜索「能直接改写中文文案、去除 AI 痕迹，不要只检测」完成：两次 DeepSeek 结构化请求均返回 HTTP 200 + `finish_reason=stop`；累计保留 91 个真实核验候选，其中 10 个通过强推荐门槛，包含 44,222 次安装、15,485 Star 的 `humanizer-zh` 等代表候选。本轮一个独立公开搜索源临时未完成，其他来源的已核验结果没有丢失；随后已增加「单条 GitHub 查询失败仍保留其他查询」的回归保护。
- ✅ Swift 全量 166 项测试、14 个测试组全部通过；Release 生产构建、`Info.plist`、Hardened Runtime ad-hoc 签名和 `codesign --verify --deep --strict` 通过，最终应用已完成真实启动且进程保持运行。
- 📌 本地 ad-hoc 签名在重打包后可能触发 macOS 钥匙串的一次密码确认；正式公开版仍需 Developer ID 签名与 Apple 公证，本轮没有把本地测试包冒充为可公开发行版。

## 2026-08-17

- ✅ 完成「发现 Skills」v2：左栏改为真实的用户/AI 连续对话，每条用户消息只对应一条自然短回复；搜索中、失败重试和旧结果保留都有明确状态。寻找记录升级为 Schema v2，旧记录保留用户原话和候选，不伪造历史 AI 回复，并在首次迁移前备份 v1 JSON。
- ✅ 搜索链路升级为“原话 + 最多 3 条语义补充”：每条最多召回 20 个结果，按仓库与 Skill 去重后只核对前 13 个；“质量不高”只改变筛选偏好，新需求不会与旧需求机械拼接，连续中文需求无需刻意加空格。
- ✅ 建立双证据门：skills.sh 的 Skill 级说明只用于理解用途，只有从 GitHub Tree/Contents 实际定位并读取到 `SKILL.md` 的候选才有资格进入推荐或备选；仓库简介不会冒充 Skill 用途，真实证据缓存 24 小时以减少重复查询。
- ✅ 推荐分为“推荐”和默认折叠的“其他可能相关”：主列表最多 5 个，备选最多 8 个；推荐必须用途相关、内容可核对，并满足安装量、仓库 Star 或可信发布者条件。没有强候选时会如实显示暂无可靠结果，不用低质量候选填满页面。
- ✅ AI 正式拆为两次受控调用：第一次只整理目标、条件和查询计划，第二次只能从真实候选 ID 中排序并生成面向用户的用途、场景、示例和限制。未知候选、虚构 ID、损坏 JSON 或未通过本地质量门的 AI 回复会被丢弃并安全回退；未配置 AI 仍可按确定性规则搜索。
- ✅ 三栏界面完成收口：左栏固定 380 点并采用聊天消息样式；输入框与模型/发送操作分成上下两层；中栏标题固定、弱候选折叠；右栏固定为「Skill 详情」，紧凑展示安装量、Star 和更新时间，正文只保留用途、适合原因、场景、示例话术、三步体验和限制。
- ✅ 真实公开目录冒烟验证覆盖中文写作去 AI 味、Swift 开发和 Obsidian/飞书小众场景：高安装量真实候选能够被召回，小众弱候选保留但不会伪装成推荐。最终 Swift 全量回归为 153 项全部通过。
- ✅ Release 应用构建通过，`Info.plist`、Hardened Runtime ad-hoc 签名与磁盘完整性校验通过；最终视觉原型为 `design/ui/skillbox-discovery-v17.png`。本轮未启动或替换用户正在使用的 SkillBox，也未下载、安装任何候选 Skill。
- ✅ 「发现 Skills」输入框改为“搜索你想要的 Skill 功能”，文字与发送按钮垂直居中；输入框右侧新增模型菜单，用户能直接看到当前模型、切换已配置模型或进入 AI 设置。
- ✅ 设置页新增可用的「AI 助手」配置：支持 Agnes、DeepSeek 与自定义 OpenAI 兼容接口，可选择模型、填写并测试 API Key、删除密钥；密钥只进入 macOS 钥匙串，`ai-settings.json` 不保存密钥。
- ✅ AI 开启并配置后，发现页会用用户明确选择的模型整理搜索需求；调用失败时保留原话并继续公开搜索，不静默切换服务商，也不影响本地管理、安装和更新。
- ✅ 当前 AI 请求只发送发现输入框里的需求文字，不发送本地 Skill、私人仓库或文件内容。用户说明整理与安全语义复核仍是后续独立切片，界面没有伪装成已完成。
- ✅ 新增 4 项 AI 集成测试并完成 144 项 Swift 全量回归，覆盖当前默认地址与模型、设置文件不含密钥、连接测试鉴权以及模型选择真实生效；Release 应用构建、`Info.plist`、ad-hoc 签名与磁盘完整性校验通过。
- ✅ 修复全局状态提示常驻遮挡主操作的问题：普通完成结果改为右上角短暂轻提示，3.5 秒后自动收起；进行中的本机查看改在顶部工具区显示，不再占用发现页输入区。
- ✅ v16 视觉方向确认后，正式 SwiftUI 新增一级「发现 Skills」：大窗口为寻找过程、候选列表、详情三栏，窄窗口收成寻找记录菜单、候选和详情两栏；总览页增加同一流程的快捷搜索。
- ✅ 接入可替换的 `SkillDiscoveryProvider`，首个实现直接读取 skills.sh 公开搜索，无需 Node/npm 或 SkillBox 账号；使用真实公开接口回读确认当前字段为名称、来源和安装量。
- ✅ 公开候选最多补充 5 个去重 GitHub 仓库的真实 Star、仓库介绍和最近更新时间，并使用 6 小时内存缓存降低查询；界面明确这些信号属于整个仓库，不拿仓库介绍或 Star 冒充具体 Skill 的能力和安全结论。
- ✅ 新增本地「寻找记录」：可连续补充条件，可保留多条任务和未首选候选，开始新寻找不删旧记录；设置页可单条删除、全部清空、查看占用和在 Finder 打开。记录保存到 `SearchSessions/`的可读文件夹和版本化 JSON，不与中央 Skill 库混在一起。
- ✅ 「加入我的 Skills」已接回现有 GitHub Release/默认分支、完整下载、纯净包解析和安全预览链路。skills.sh 的 Skill ID 不再被误当成 GitHub 物理路径；多 Skill 仓库会按名称预选用户点击的候选，没匹配时要求用户选择，不会擅自把整库全部加入。
- ✅ 新增 3 项发现与寻找记录测试，全量 Swift 回归增至 140 项并通过；Release 构建、`Info.plist`、Hardened Runtime ad-hoc 签名与磁盘完整性校验通过。
- 📌 本轮未自行启动或替换用户正在使用的 SkillBox，也未下载、安装任何候选 Skill。未配置 AI 时的真实搜索、记录和安全加入主链路仍可独立工作。

- 🟡 用户确认 SkillBox v1.1 方向：增加可选 AI 助手、按任务发现优质 Skill 和加入前安全复核；默认 Agnes，支持 DeepSeek 与自定义 OpenAI 兼容接口。
- ✅ Spec v1.1 与 ADR-0008 已固化关键边界：AI 只解释和复核，本地规则继续决定完整性、阻止、安装和撤销；API Key 只进钥匙串；私人或本地 Skill 未确认不发送；服务商不会静默切换。
- ✅ 用户确认 AI 采用“后台统一、前台按任务出现”的交互：不把首页做成通用聊天；左侧增加一级“发现 Skills”。2026-08-19 进一步收口为 Skill 搜索只在“发现 Skills”完成，总览不再重复放置搜索框。
- ✅ Spec v1.1 与 ADR-0008 已同步新信息架构：明确“添加 Skill”与“发现 Skills”的任务边界、仓库级 Star 口径、用户说明取证顺序、无依据内容省略和无聊天历史边界。
- ✅ 完成情境化 AI 与“发现 Skills”v16 可操作原型及 5 张关键 PNG，覆盖总览智能搜索、发现候选、完整发现详情、我的 Skill 用户说明和加入前安全说明。
- ✅ 实际点击验证总览跳转、导航选中、候选切换、仓库 Star 更新、话术复制、安全弹窗与服务商切换；1500px 和 1080px 窗口均无横向溢出，主操作与技术详情保持可见，控制台无错误。
- 🟡 等待用户确认 v16 视觉与信息层级；确认前不接入真实 AI、不读取钥匙串、不上传 Skill，也不改动现有安装事务。
- ♻️ v15 的“发现 Skill 藏在添加菜单”方案已被 v16 一级导航方案替代，旧文件仅保留为设计演进记录。
- ✅ 修复 GitHub 根目录 Skill 的严重完整性缺陷：旧规则会把 `scaffold.sh` 等根目录运行文件归为可选，导致“纯净整理”后功能缺失。真实数据确认 GitHub 源码快照和历史版本都包含该文件，丢失发生在 SkillBox 的安装内容筛选层。
- ✅ 安装内容策略升级为无损默认：用途不明的顶层内容默认保留；`SKILL.md` 明确引用的文件或目录锁定为必需；普通用户不再通过技术文件勾选承担完整性判断。
- ✅ 旧版自动选择和用户选择规则带版本标记。旧规则会阻止继续安装并从 GitHub 同一版本重新核对，不能拿残缺的中央副本继续“提纯”；Release ZIP、明确子目录和作者清单保持原有权威边界。
- ✅ 新增 4 项包完整性回归覆盖根目录关键脚本、被引用的仓库样式文件、旧残缺规则自愈和未知顶层内容默认保留；全量 Swift 测试增至 137 项并通过。
- ✅ 完成通用的「GitHub 仓库 → 可安装 Skill」分层：SkillBox 中央库和 Agent 目录优先保存运行内容；明确且未被 Skill 主说明引用的 README、LICENSE、CI、贡献指南等仓库包装不再跟着安装。
- ✅ 新增可持久化的「安装内容清单」：优先使用 Release ZIP、明确 Skill 子目录或 `skillbox.json`；根目录混装时只需确认一次，后续更新自动沿用。
- ✅ 旧版整仓库 Skill 增加可恢复迁移；从「我的 Skills」或「安装到应用」进入时会先整理纯净版，避免把仓库主页文件当成 Skill 覆盖 Agent 中的纯净副本。
- ✅ 更新检查改为只比较已确认的可安装路径；仓库资料变化保持安静，新的可能运行目录会单独请用户确认，不会误报成「新版本」。
- ✅ 持久化数据升级为 Schema v3；迁移不下载、不静默替换文件，中央版本与 GitHub 来源记录仍在同一操作中备份和撤销。
- ✅ 经独立子 Agent 审查后落地其五项边界：ZIP 继续安全检查、多 Skill 不擅自选择、清单绑定仓库与版本、仓库资料变化不打扰、迁移可回滚。
- ✅ 全量 Swift 测试增至 133 项并通过；Release 应用完成构建，`Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验通过。本轮未向任何真实 Agent 目录写入文件。

## 2026-08-16

- ✅ 根据独立代码审查修复 GitHub 限流状态边界：应用启动只迁移“已确认公开且旧记录缺少额度类型”的历史暂停，不再把启动误当成用户刚完成登录，重开应用不会反复解除新的等待时间。
- ✅ 用户刚完成 GitHub 登录时仍会立即解除明确的未登录额度暂停；旧私人仓库、公开性未知和已登录额度暂停继续遵守 GitHub 的恢复时间，不再因历史字段为空被误清除。
- ✅ 修复公开仓库匿名回退只作用于单个请求的问题：Release 回退后的 Commit、默认分支回退后的 Tree 查询都会沿用匿名方式，不再对每个后续端点重复一次无效的认证请求。
- ✅ 三条修复均先补失败测试再完成最小实现；GitHub 更新与来源专项 49 项、全量 118 项 Swift 测试通过。Release 应用重新打包，并通过 `Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验。本轮未启动应用、未请求真实 GitHub、未写入任何 Agent 目录。
- ✅ 修复“已连接 GitHub，公开仓库仍被匿名额度暂停”的根因：已知公开仓库现在优先携带钥匙串中的 GitHub 用户令牌，使用认证查询额度。
- ✅ 补齐已登录后仍被旧暂停记录拦住的第二层问题：本地现在区分未登录与已登录额度，连接成功后立即解除旧的未登录暂停，不再等到原提示时间。
- ✅ 保留公开读取的独立性：登录过期或公开仓库不在 GitHub App 选择范围时，同一次检查会自动转为匿名，不提示连接私人仓库，也不在后续端点重复认证失败。
- ✅ 按 TDD 将原先“公开仓库必须匿名”的回归测试改为“已连接时优先认证”，并保持登录失效、未选公开仓库、私人仓库重连和 GitHub 限流边界测试通过。Spec v1 与 ADR-0006 已同步新规则。
- ✅ GitHub 来源与更新状态专项 45 项测试通过，全量 114 项 Swift 测试全部通过；Release 应用重新打包，`Info.plist`、Hardened Runtime ad-hoc 签名与完整性校验通过。本轮没有读取或写出 GitHub 令牌，没有写入任何 Agent 目录。
- ✅ 修复事务撤销只恢复文件、没有恢复用户安装选择的问题：撤销卸载会恢复“已选择”，撤销新安装、接管相同内容或覆盖已有内容会恢复“未选择”，普通已管理版本更新保持原安装关系。
- ✅ 按 TDD 逐条复现并修复新安装、卸载、接管和覆盖四条撤销路径；撤销后重新生成同步计划均无重复安装或重复卸载动作，覆盖与接管授权也会一并清除。
- ✅ 全量 Swift 测试增至 111 项并全部通过；Release 应用重新打包，`Info.plist` 与 Hardened Runtime ad-hoc 签名完整性校验通过。本轮没有写入任何真实 Agent 目录。
- ✅ 根据最新产品范围收口：撤回本轮新增的 VoiceOver 专用代码，不再把 VoiceOver、减少动态效果等无障碍专项作为 v1 发布门槛；后续验收只聚焦添加、更新、安装、卸载和撤销的真实用户闭环。
- ✅ 首次完成真实 SkillBox 主路径鼠标与键盘验收：Skill 切换、详情展开/收起、添加菜单、安装矩阵、同名内容比较、单项安装确认、Esc 取消和侧栏方向键导航均符合预期；所有安装与替换确认都在真正写入前取消。
- ✅ 前台验收发现并修复使用示例提取缺陷：README 在代码块前写有“你可以对 Codex 说：”等引导句时，SkillBox 现在会继续读取代码块中的真实示例，不再展示只有前缀的空复制框；先补失败测试，再完成最小修复。
- ✅ 全量 Swift 测试增至 108 项并全部通过；Release 应用重新打包，`Info.plist`、Hardened Runtime ad-hoc 签名及完整性校验通过。
- 📌 本轮真实前台已覆盖鼠标与基础键盘路径。未确认任何安装、替换、卸载或删除，也未写入任何 Agent 目录；下一步转入核心生命周期闭环验收。

- ✅ GitHub 更新检查收口为本机仓库级单队列：同一仓库的多个 Skill 只检查一次；Release 有 ZIP 时只读 Latest Release，默认分支只在 Commit 变化后继续读取 Skill 目录。
- ✅ 新增并持久化 GitHub `ETag`，`304 Not Modified` 保留已知版本结论并停止后续请求；失败或临时限制不再伪装成成功检查，不会错误延后下次自动检查。
- ✅ 应用内自动检查间隔改为 24 小时；手动检查显示仓库级进度并可取消。大型 Skill 库优先检查最久未成功的仓库，达到 GitHub 临时限制后也不会永远卡住后排来源。
- ✅ 依据 GitHub 官方权限边界纠正登录策略：公开仓库始终匿名读取，不消耗或依赖私人仓库令牌；「连接 GitHub」只用于用户选中的私人仓库。已登录时添加未授权的公开仓库也会自动回退到匿名读取，不再误报权限问题。
- ✅ 主界面统一为「连接 GitHub」，公开仓库与私人仓库的用途说明分开；GitHub 临时限制改为平静的服务状态，不再向普通用户展示「查询次数」等技术话术。
- ✅ Spec v1 与 ADR-0005 已固化该方案；新增仓库归并、条件请求、公私权限分离、公平队列、24 小时策略和重启持久化回归测试，107 项自动化测试全部通过，Release 构建成功。
- 📌 本轮没有启动真实 SkillBox，没有向任何 Agent 目录写入文件；鼠标、键盘、VoiceOver 和减少动态效果仍属于独立前台验收门。

- ✅ 完成用户指南的可靠性收口：README 中明确的「使用示例」优先于内部 `default_prompt`；偏技术的默认指令、`Use when` 原始片段和 Agent 内部规则不再冒充用户教程。
- ✅ 修复 YAML `>-` / `|` 多行简介解析，旧的中央 Skill 在启动重新检查时也会自动刷新简介；真实 `zhaoji-writing` 记录已从 `>-` 恢复为完整可读文字，无需删除重导。
- ✅ Skill 列表改用中性的名称首字标识，普通脚本、说明文档和其他低级别扫描结果不再显示蓝色 `i` 或绿色状态图标。
- ✅ 移除普通 Skill 详情中常驻的「文件检查」卡片；本地静态扫描、导入阻止和安装保护仍完整保留，只有高风险或已阻止内容才显示「安装前需要确认」。
- ✅ 用户已确认 v14 可视化方向；新增多行元数据、示例话术优先级、技术话术省略、纯触发语不冒充用途、旧数据刷新和风险展示政策测试，全量自动化测试增至 93 项并全部通过。release 构建、`Info.plist` 和 Hardened Runtime ad-hoc 签名校验通过。
- 📌 真实 release 进程已重新启动，且只读回读确认旧简介迁移已落盘；Computer Use 在退出旧进程后返回 `native pipe closed before response`，因此本轮没有把真实鼠标、键盘、VoiceOver 和减少动态效果验收写成已通过。未写入任何真实 Agent 目录。

- ✅ 修复公开仓库被误判为私人仓库授权失效的根因：GitHub 未登录查询达到每小时上限时会返回 `403`，旧逻辑把所有 `403` 都当成登录问题；现在依据限流响应头保存可重试时间并显示“公开仓库无需连接私人仓库”。
- ✅ GitHub 版本结论与连接问题分开保存；临时限流、网络异常、登录失效不会再抹掉“已是最新”或“有新版本”等已有结论。
- ✅ 公开仓库与私人仓库登录彻底解耦：钥匙串令牌失效时自动退回公开读取；只有已确认的私人仓库才会出现“重新连接”或“允许访问”，仓库改名、删除与网络故障使用独立提示。
- ✅ GitHub 限流后暂停所有来源请求直到服务端给出的恢复时间，连续点击、批量检查和启动检查都不会继续消耗请求；旧版遗留的错误红色授权状态会在打开新版本时自动迁移为中性连接提示。
- ✅ 失效令牌只会被当前进程拒绝到它被替换为止；用户重新连接取得新令牌后可立即恢复私人仓库访问，不需要重启 SkillBox。
- ✅ 新增 12 项网络、状态、迁移和批量摘要回归测试；全量自动化测试增至 85 项并全部通过。原有 Release 纯净包、更新事务、撤销与指纹保护测试保持通过。
- ✅ release 应用重新打包，`Info.plist` 与 Hardened Runtime ad-hoc 签名完整性检查通过；真实前台首次检查时三个来源均落盘为公开且已是最新，随后再次触发 GitHub 真实限流时，界面正确显示恢复时间和“公开仓库无需连接私人仓库”，没有再引导连接私人仓库。
- 📌 `spctl` 仍会拒绝 ad-hoc 本地测试包；公开分发仍需 Developer ID 签名与 Apple 公证，这与本次 GitHub 更新逻辑修复相互独立。

## 2026-08-15

- ✅ 修复 GitHub 更新误报：旧记录中 `release:id` 与新版 `release:id:source:commit` 表达同一个源码包时，启动和检查都会自动归一为“已是最新”，不再提示假更新。
- ✅ 同一个 Release 下新增或切换为作者上传的纯净 ZIP 时，不再显示“发现新版本”，改为“有更干净的安装包”，让用户知道这是安装包来源优化，不是 GitHub 刚发布了新版本。
- ✅ 新增 4 个回归测试覆盖旧源码包兼容、同版本纯净安装包状态、应用启动时清理旧误报和旧状态自动转为安装包提示；自动化测试增至 70 项并全部通过。
- ✅ release 应用重新打包，`Info.plist` 与 ad-hoc Hardened Runtime 签名校验通过；`spctl` 仍因缺少 Developer ID 公证而拒绝，这是本地测试包的已知发布边界。
- ✅ 继续收紧批量检查文案：同版本纯净安装包不再被汇总成“有新版本”，而是单独提示“有更干净的安装包”；新增 3 个摘要测试，自动化测试增至 73 项并全部通过，release 构建与本地签名校验通过。

- ✅ 收紧「我的 Skills」两处高频操作区：页头以「添加 Skill」为唯一主操作，更新检查与私人仓库连接统一为次级按钮；安装卡拆分说明、可用数量和执行操作，避免主按钮换行。
- ✅ 安装卡会直接显示本机可安装应用数量；一个可用位置都没有时禁用全局安装，并明确说明 SkillBox 不会代为创建目录。
- ✅ 新增 v13 可视化预览，用于核对顶部操作层级、按钮尺寸和安装卡布局。
- ✅ 66 项自动化测试、release 构建、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 本轮未启动真实 SkillBox、未写入 Agent 目录；正式窗口中的鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立体验验收门。

- ✅ GitHub Release 导入改为优先使用作者上传的 ZIP 安装包：单一 ZIP 直接继续，多个 ZIP 弹窗让用户选择，没有 ZIP 时明确提示将导入测试、CI 和开发文件。
- ✅ 同名 `.sha256` 会在下载后自动校验，校验错误直接阻止导入；来源记录新增 Release ID、Asset ID、Asset 名称与已验证 SHA-256，同一 Release 中替换资源也能识别为更新。
- ✅ Agent Team 纯净包回归样本只导入 5 个运行文件，不包含 `.github/workflows/ci.yml`；未修改 Agent Team 运行文件或删除仓库 CI。
- ✅ GitHub Actions 工作流中纯粹的 `${{ github.token }}` / `${{ secrets.GITHUB_TOKEN }}` 改为蓝色“仅说明”；本地脚本读取令牌、通过变量外传或直接外传令牌仍保持高风险。
- ✅ 新增 v12 多 ZIP 选择与源码回退可视化预览，正式 SwiftUI 已接入同一流程；66 项自动化测试、release 构建、打包、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 本轮未启动真实 SkillBox 或写入 Agent 目录；新 Release 弹窗的真实鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立验收门。

- ✅ 「这个 Skill 怎么用」重做为普通用户指南：先说能帮什么，有资料时再展示适用时机、不适用情况、可一键复制给 AI 的示例话术，以及最多 3 步的可选体验流程。
- ✅ 使用指南改为渐进降级：优先读取 `agents/openai.yaml` 的用户展示信息，再读作者明确写在 `README.md` / `SKILL.md` 的用户章节，最后只保留 `description` 中可确定的用途。不完整时自动省略缺失项，不使用 AI 补写。
- ✅ 修复原有误判：`SKILL.md` 中的「执行步骤」、安全规则和 Agent 内部指令不再显示为用户流程；只有作者明确写了「使用时会发生什么」才展示对应步骤。
- ✅ 新增 v11 完整与简化两种视觉预览；55 项自动化测试、release 构建、`Info.plist` 和 Hardened Runtime 本地签名校验通过。
- 📌 不接入 AI 的前提下，任意新 Skill 都能至少展示作者在标准 `description` 里写下的用途；适用时机、示例话术和体验流程只在作者提供可靠资料时出现，避免用看似完整的错误内容误导用户。

- ✅ “安装到应用”矩阵改为直接单项操作：点击加号、已安装或冲突状态后当场弹出当前 Skill 与当前应用的确认窗，移除右上角二次寻找和批量执行入口。取消时不保存选择，确认时只执行这一项。
- ✅ 未管理的同名 Skill 在点击前就显示橙色叠层图标；点击后通过真实内容指纹告知“完全相同”或“内容不同”，不同时展示新增、修改和移除文件。修复了旧阻塞动作丢失两边指纹、可能把空值当作相同的根因。
- ♻️ 旧版曾将 `SKILL.md` 的「执行步骤」直接展示为「这个 Skill 怎么用」，已在本日最新迭代中替换，不再将 Agent 内部规则当作用户教程。
- ✅ 原来占据主位的文件内容提示改为默认收起的「文件检查」，主界面只告知提示数量和“安装时不会运行”，具体文件与命令仅在用户主动展开后显示。
- ✅ 新增 v10 交互说明与安装矩阵、同名比较弹窗、使用流程三张 PNG 预览；55 项自动化测试、release 构建、打包、`Info.plist` 和 Hardened Runtime 本地签名校验通过。
- 📌 本轮没有向真实 Agent 目录写入测试样本；Computer Use 通道之前持续返回 `native pipe closed before response`，因此真实 SwiftUI 的鼠标、键盘、VoiceOver 与减少动态效果仍保留为独立验收门。

- ✅ “安装到应用”矩阵改为默认从滚动区左上角展示，移除导致宽度异常的无限拉伸；大窗口九个内置应用完整可见，小窗口仍可水平滚动查看右侧列。
- ✅ 将难理解的“使用前检查”重写为文件内容提示：首先说明添加和安装时不运行文件，再分别解释可运行脚本、变量位置清理命令和说明文档中的命令。
- ✅ 风险标签统一为“仅说明、需要了解、需要确认、无法添加”，仍保留实际命令和文件位置作为可核对证据。
- ✅ GitHub 验证码卡片补充普通用户说明：此步只确认身份，不创建仓库或修改文件；重新开始只会换取一个会过期的临时验证码。
- ✅ 新增 v9 布局与安全提示设计说明及两张 PNG 预览；52 项自动化测试、release 构建、打包和 Hardened Runtime 本地签名验证通过。
- 📌 真实 SkillBox 已启动，但 Computer Use 以应用路径和 Bundle ID 读取都返回 `native pipe closed before response`；本轮用本地 HTML + PNG 完成视觉兜底，未将真实鼠标验收写成已通过。

- ✅ 已注册正式 `SkillBox by Zhaoji` GitHub App，开启桌面登录流程，仅申请仓库内容与基础信息只读权限，关闭 Webhook，并允许任意 GitHub 用户安装。
- ✅ 已将公开 Client ID 和安装地址写入正式构建配置；普通用户无需创建或下载 GitHub App。Client Secret、私钥和用户令牌均不进入工程或应用包。
- ✅ 真实 GitHub 账号已将 SkillBox 安装到一个明确选中的私人仓库，GitHub 回读权限为代码和基础信息只读。未授权其他私人仓库或所有未来仓库。
- ✅ 私人仓库连接改为连续向导：身份确认后自动打开选仓库页面，SkillBox 自动等待并回读结果；界面显示等待进度、恢复入口和完成状态。
- ✅ GitHub 真实 Device Flow 起点接口返回有效验证地址、用户码、有效期和轮询间隔；52 项自动化测试、release 构建、打包和 Hardened Runtime 签名验证通过。
- ✅ 根据真实导入的 `vibe-project-foundation` 排查风险提示：两个 `rm -rf` 分别用于失败回滚和临时测试目录清理；扫描器不再一律标红，变量范围清理降为蓝色“请留意”，宽泛路径删除仍保持高风险，并在详情中展示实际命令。
- ✅ 应用启动时重新扫描中央 Skill 的风险报告，已有 Skill 无需删除重导即可获得新分级。
- ✅ 中央文件夹改用 Skill 名称，例如 `Library/vibe-project-foundation/content`；同名不同版本自动追加短编号。旧 UUID 目录会连同历史版本迁移，删除恢复遇到同名新 Skill 时也不会冲突。
- ✅ 未配置私人仓库的构建不再显示容易误解的“正在准备中”，改为“当前版本尚未启用”，并明确这不是加载过程、无需等待。
- ✅ 新增风险范围、旧报告重扫、可读目录、UUID 迁移、同名版本和恢复冲突测试；自动化测试增至 52 项。

- ✅ 私人仓库入口改为普通用户三步流程「确认身份 → 选择仓库 → 回到 SkillBox」，界面不再出现 GitHub App、Device Flow、Client ID 或“测试版本”等开发术语，并明确无需下载其他软件。
- ✅ 设置页补齐「管理可访问仓库」「断开连接」「清除 GitHub 信息」「在 GitHub 撤销授权」；断开只清除本机令牌且不影响公开仓库，清除信息会保留 Skill 内容并移除仓库跟踪资料。
- ✅ 新增断开令牌与清除 GitHub 信息的行为测试；自动化测试维持 46 项全部通过，release 构建通过。
- ✅ SkillBox GitHub 产品身份已完成注册和构建配置；私人仓库安装、登录和授权仓库回读需在真实账号验收后再标记完成。
- 📌 Computer Use 以应用名和 Bundle ID 读取真实 SkillBox 窗口仍返回连接中断；已生成 HTML 视觉说明，真实设置页的鼠标、键盘、VoiceOver 与减少动态效果验收仍未冒充通过。

- ✅ 完成消费级功能闭环修复：「我的 Skills」新增搜索、更新/安装/问题筛选和更新徽标；私人 GitHub 连接入口同时出现在 Skill 库、GitHub 添加页和设置页。
- ✅ GitHub 下载与更新准备新增可见进度和取消按钮；仓库没有正式 Release 时可一键改用默认分支，仓库地址会在提交前直接校验。
- ✅ 中央原件更新现在始终生成可撤销记录；撤销 GitHub 更新会同时恢复原件和来源版本状态。删除 Skill 后提供即时撤销，并恢复分类、选择和 GitHub 来源。
- ✅ 「自定义安装」更名为「安装到应用」，矩阵增加常驻图例；自定义位置支持编辑和安全移除，仍有受管理副本时会阻止换目录或移除。
- ✅ 最近操作现在显示 Skill 名称、操作类型、应用详情和恢复按钮；设置页限制内容宽度，左下角只保留非关键应用数量，实时状态移到内容区浮层。
- ✅ 自动化测试增至 44 项，debug/release 构建和 Hardened Runtime ad-hoc 签名校验通过。
- 📌 私人仓库真实连接仍依赖已注册的 SkillBox GitHub App 公开 Client ID 与安装地址；当前测试包未配置，因此按钮会用普通用户语言说明暂不可用，公开仓库不受影响。
- 📌 前台验收时 SkillBox 进程正常运行，但 Computer Use 以应用路径和 Bundle ID 读取均返回 `native pipe closed before response`；本轮未把真实鼠标、键盘和 VoiceOver 标记为通过。

- ✅ 用户确认 GitHub v7 原型后，已将新交互正式接入 SwiftUI：GitHub 导入可选“最新正式 Release”或“默认分支”，并在下载前明确说明是完整版本。
- ✅ Skill 详情新增 GitHub 来源卡片，覆盖有新版本、已忽略、停止检查、需要首次核对、失去权限、来源不可用与已是最新；文字、主按钮和更多菜单均可操作。
- ✅ 新增正式更新预览页：展示新增/修改/移除文件、风险变化、`SKILL.md` 前后对照、原有安装位置及阻止原因，并提供“只更新我的 Skills”和“更新并安装到原有应用”。
- ✅ 设置页接入 GitHub Device Flow，可查看 GitHub App 实际授权的公开/私人仓库、管理仓库权限和断开连接；断开前明确说明本地 Skill 不会删除。
- ✅ “检查全部更新”已放入「我的 Skills」页头；手动停止的来源不会发起请求，授权失效与普通无更新不会混为同一结果。
- ✅ 补齐两个产品边界：设置页只展示 GitHub 真实授权仓库；更新 Skill 不会把“已取消选择但尚未确认卸载”的位置顺带删除。
- ✅ 自动化测试增至 40 项，Swift 并发构建、release 打包、`Info.plist` 与 Hardened Runtime ad-hoc 签名校验通过。
- 📌 真实前台验收时首次读取到了旧进程画面；正常退出旧进程后，Computer Use 连续返回 `Sky Computer Use native pipe closed before response`，因此新 SwiftUI 的真实鼠标、键盘、VoiceOver 和减少动态效果仍保留为独立验收门，本次未用其他自动点击方式绕过。
- 📌 私人仓库真实登录、授权仓库回读与网络更新仍需 GitHub App 的公开 Client ID 和安装地址；本次未写入任何真实 Agent 目录。

- ✅ 将 GitHub 正式纳入 Skill 更新源：新 Skill 可选择跟随“最新正式 Release”或“默认分支”，旧 GitHub Skill 升级后默认跟随原分支并标记“需要核对一次”。
- ✅ 持久化数据升级为 Schema v2，新增仓库编号、Skill 子目录、当前版本、Commit SHA、目录 SHA、上次检查时间、忽略版本、停止检查与授权状态；升级不下载、不替换现有内容。
- ✅ 拆分“查看是否有新版本”和“确认后下载”；前者只请求版本与目录信息，后者按 Release Tag 或已解析的 Commit SHA 下载完整快照，不使用 Release 附件。
- ✅ 多 Skill 仓库按具体 Skill 目录 SHA 判断变化，README 或其他 Skill 的改动不会误报；已确认更新会展示新增、修改、移除文件和 `SKILL.md` 前后内容。
- ✅ 新增“忽略这个版本”和“停止检查更新”；忽略只针对当前 Release ID 或 Commit SHA，下一版仍会提醒，停止后不发起网络请求。
- ✅ 完成中央原件与已管理 Agent 副本的联合更新、失败回滚和撤销；外部改动、已消失安装位置和未托管目录仍会被保护。
- ✅ 完成 GitHub Device Flow 和 macOS 钥匙串基础能力；仅在 App 中保存公开 Client ID，Access Token 与 Refresh Token 不进入 JSON、日志或操作记录，授权被撤销时保留本地 Skill 并暂停检查。
- ✅ 交付 GitHub v7 可操作原型和 5 张截图，覆盖账号连接、导入方式、新版本、更新差异、授权失效；鼠标点击与页面跳转自动验证通过。
- ✅ 自动化测试增至 38 项，release 应用重新打包，`Info.plist` 校验和 Hardened Runtime ad-hoc 签名完整性检查通过。
- ✅ 用户已确认 v7 原型，正式 SwiftUI 接入情况见本日最新记录。

## 2026-08-14

- ✅ 「我的 Skills」新增个人分类：可新建、重命名、删除文件夹，把 Skill 拖入分类、拖回未分类，并自由调整文件夹与同组 Skill 的显示顺序。
- ✅ 分类与顺序单独保存在 `organization.json`；整理列表不会移动、改名或删除中央 Skill 原件，也不会改变已选安装关系；删除文件夹只把其中 Skills 放回“未分类”。
- ✅ 所有详情操作补齐悬停、按下和禁用反馈；「查看 Skill 详情」改为整行可点击，文字、箭头和留白区域均可展开或收起。
- ✅ 更新 v6 可操作原型与分类、详情悬停、新建文件夹三张截图；原型完成按钮悬停、文字点击展开、拖入分类和新建文件夹验证。
- ✅ 全量自动化测试增至 25 项，release 应用重新打包，并通过本地签名完整性校验。

- ✅ 重排「我的 Skills」右侧详情：身份信息、主要安装动作、安装状态、安全检查和内容详情形成清晰层级；删除操作保留但降低视觉权重。
- ✅ 「查看技术信息」改为「查看 Skill 详情」，新增完整只读目录、文件层级与大小展示，以及主 `SKILL.md` 的独立预览按钮。
- ✅ 目录在后台读取，软链接只展示不跟随；新增目录完整性测试，自动化测试增至 24 项，release 应用打包与本地签名校验通过。
- ✅ 更新 v5 可操作原型与详情、Markdown 预览两张截图；展开/收起、目录显示、预览打开/关闭点击验证通过。

- ✅ 每份 Skill 新增“安装到全部可用应用”“从所有应用卸载”和“删除这份 Skill”；安装与卸载仍先展示变更清单，删除前独立确认。
- ✅ 删除中央原件时先检查是否仍有 SkillBox 管理的安装副本；可删除内容移入本机 `Deleted` 目录，保留恢复空间。
- ✅ “安装到哪里”更名为“自定义安装”，用于逐个微调；全部安装只选择已检测到且可写入的应用位置。
- ✅ 扫描、同步预览和真正执行三层都阻止缺失目录；本机未安装的 Agent 只提示并跳过，不会创建对应文件夹。
- ✅ 定位“查看技术信息”卡顿来自主线程读取与一次排版整份长文本；改为后台读取、轻量摘要和独立原生文本窗口。
- ✅ 更新 v4 可操作原型与三张预览图；原型的全部安装弹窗、技术信息展开和自定义安装导航点击验证通过。
- ✅ 核心自动化测试增至 23 项，新增可恢复删除、在用阻止删除、缺失目标不创建和执行前目标消失保护；release 应用打包与本地签名校验通过。

- ✅ 修复空 Skill 库和空安装页的垂直漂浮：页面标题固定在顶部，空状态只在剩余内容区域居中。
- ✅ 空 Skill 库移除无意义分栏并提供电脑、GitHub 添加入口；空安装页不再显示空矩阵，改为引导添加 Skill。
- ✅ 总览中的同名内容条目升级为可点击入口；可按单组查看来源、内容编号并单选要加入 SkillBox 的版本，未选择前不能确认。
- ✅ 更新可操作 HTML 原型并生成空 Skill 库、空安装页、同名版本选择三张 v3 预览；原型交互、19 项自动化测试与 release 打包通过。
- 📌 `computer-use` 以应用路径和 bundle id 读取窗口均返回 `Sky Computer Use native pipe closed before response`；真实 SwiftUI 鼠标验收仍待通道恢复后补做。

- ✅ 完成面向非技术用户的全局文案降噪：导航、总览、Skill 详情、安装选择、安装确认、最近操作、设置、引导和错误提示统一改为日常语言。
- ✅ 将“中央仓库、指纹、事务、阻塞、接管、Agent 目标”等内部术语移出主流程；内容校验码收入可展开的「技术信息」。
- ✅ 更新可操作 HTML 原型并生成总览、欢迎页和安装确认三张新版预览图；19 项自动化测试与 release 打包通过。
- 📌 `computer-use` 已识别 SkillBox 运行，但读取窗口时本机管道连续中断；本次使用 HTML + PNG 完成视觉兜底，真实鼠标前台验收仍待补。

- ✅ 用户确认原型方向，开始先打磨功能、后优化 UI。
- ✅ 建立 Swift 6.2 + SwiftUI 工程，拆分 `SkillBoxCore`、`SkillBox` 与只读诊断命令。
- ✅ 完成九个内置 Agent 适配器、自定义目录防护、只读扫描、SHA-256 指纹、归并与冲突识别。
- ✅ 完成中央仓库、版本化 JSON、损坏元数据保护、安装矩阵、同步预览、备份、回滚和撤销。
- ✅ 完成本地文件夹与公开 GitHub 导入、手动更新、静态风险报告、容量/数量/路径穿越/软链接阻断。
- ✅ 完成功能型 SwiftUI 界面：资产总览、Skill 库、Agents 矩阵、同步预览、操作记录、设置和首次引导。
- ✅ 19 项自动化测试通过，包括写入中断后多目标回滚、同目标版本互斥、替换授权失效、未托管冲突、外部改动、撤销保护、损坏 JSON 和恶意压缩包。
- ✅ 真实环境只读扫描找到 172 个候选、37 组重复和 24 组同名冲突；扫描前后目录清单与文件指纹一致，证明零修改。
- ✅ 生成启用 Hardened Runtime 的 ad-hoc 本地测试 `.app`，并通过 `codesign --verify`。
- 📌 当前边界：未向任何真实 Agent 目录写入，未做鼠标前台验收，未使用 Developer ID 签名、公证或发布 DMG。

- ✅ 完成竞品调研与产品讨论，确认公开、开源、原生 macOS、中央原件库和安全同步定位。
- ✅ 冻结 Spec v1、MVP 和九个内置 Agent 路径合同。
- ✅ 记录原生本地优先、中央库与事务式实体复制两项架构决策。
- ✅ 完成可操作 UI 原型、资产总览/首次盘点/同步预览三张截图。
- ✅ 自动化验证导航、Skill 详情切换、安装矩阵、同步提示和首次盘点流程；按钮可访问名称检查通过。
- 📌 下一门槛：用户明确授权后，选择一个可恢复样本做真实同步验收，然后进入 UI 优化。

---

- ✅ 搭好开发前地基:`docs/` `app/` `design/` `scratch/` + `CLAUDE.md` 等 AI 工作层。
- ✅ 已创建 `docs/spec.md` 作为后续开发唯一准绳,并准备 `AGENTS.md` / `CLAUDE.md` 两个轻入口。
- 📌 下一步:确认结构 → 进入 M1 Spec v0,先写清问题、用户、场景、边界和成功标准。

<!-- 新进展继续加在本日期最上方。 -->
