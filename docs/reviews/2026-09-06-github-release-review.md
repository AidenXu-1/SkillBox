# v0.2.2 最后发布评审

日期：2026-09-06。范围：准备发布到 AidenXu-1/SkillBox 的源码、Build 6 程序、DMG、说明与下载链路。用户已授权评审通过后发布；本报告不把待完成事项记为已通过。

## 已核对

- 远端默认分支 master、公开最新版本 v0.2.1；本地为远端的后续提交，没有分叉冲突。发布使用新标签 v0.2.2，保留旧 Release。
- 复核具名查询与作者范围、条件来源、对话任务边界、停止和队列、正文核验、请求计数与限流、模型输出约束及密钥脱敏；结合本代 516 项完整检查与原句真实窗口证据，没有发现需要改动程序的新增阻断缺陷。
- 正式应用和本轮候选运行摘要为 `a620076b613ebcc0ae0943e14aff86bb3a6c087842e38ec913a88aafb95a301a`，版本 0.2.2 Build 6。保留三栏布局与已有安装关系，不替换用户 Skill 内容。
- 对准备提交的文本扫描发现三处规则/测试样例命中：两处是隐私扫描脚本自己的规则，另一处是明确的虚构私钥测试片段；未发现真实凭据。实验输出与用户资料位于被忽略的 scratch 或应用资料目录，不进入源码与 DMG。
- README 功能截图不含凭据或用户私人材料。包只包含程序、必要资源与签名，不包含源码、测试资料或用户资料。

## 本轮已修正的发布材料问题

1. README 仍标 Build 5，改为实际 Build 6。
2. 上轮新增验收报告包含开发者本机绝对路径，实际触发隐私检查退出 66；改为用户目录占位表达后，同一检查退出 0。扫描规则没有放宽。
3. 工程约定仍指向已不用的 Xcode 目录和命令，按实际 Swift Package 工程同步；交接文档从旧 v0.2.1 状态同步到当前候选。

4. 对未上传历史追加扫描，发现一条旧开发记录含本机路径。当前源码已无此内容；公开分支从远端当前提交承接完整冻结源码，不携带这段未公开历史。原 master 开发历史在本机保留，未删除或改写。

这些修改不改变程序、安装内容或搜索行为。最终冻结后的完整检查、附件摘要和 GitHub 回读证据记录在本轮发布清单及 Release 说明。

## 发布边界

按照已采纳 ADR-0010，不使用 Developer ID 和 Apple 公证属于既定发行方式，不重新列为阻断项。“仍要打开”须由用户按 macOS 指示亲自完成。

Spec 第 10 节要求干净账户首次安装与发布后附件下载回读。当前尚未获得本代干净账户/另一台 Mac 的实际反馈，已向用户请求证据；在该门槛完成或用户明确调整发布范围以前，不宣称全部评审通过、不公开发布正式 Release。GitHub 下载回读必须在附件上传后执行。

已知产品限制：公开来源可能超时或达到请求预算；中文别名需要公开来源收录；使用量和 Star 没有取到时显示未知；本轮没有额外付费模型调用，因此不承诺所有模型生成回答的质量。

复测详情见 [指定对象与连续追问修复](2026-09-06-named-request-fix.md)。原始检查输出在本机 `scratch/github-release-022/`，不随仓库发布。
