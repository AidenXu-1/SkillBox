# v0.2.6 发布复核与交付

用户本轮明确授权 review、修复、本机更新与公开发布。审查范围为公开 v0.2.5 源码 19ceb0a 至本轮开发 HEAD 91e2a17 的产品与测试变化；使用 review-agent 只读完成审查，再按用户授权实施修复。

## 发现与修复

### [P2] 将受管替换确认限定在预览的原位置和内容

位置：`app/Sources/SkillBoxApp/AppModel.swift:3105`。

`AppModel.confirmAssignmentProposal` 的临时重新规划会把已删除目录或已变更安装路径当成新安装，绕过原预览的目标内容确认。新增删除、改路径两种隔离测试，旧实现两者均发生未经当前预览确认的 create，6 个断言失败。修复后要求唯一动作、原路径、原来源与目标内容凭据一致且属于替换/接管，再交给原事务执行。原来正常替换、撤销恢复、预览后再次编辑也通过。

正文组件另将异步标记状态绑定精确的前后文本，输入清空/切换时同步重建子视图，避免旧标记短暂应用到新文本。未声称在真实窗口复现正文崩溃。

## 验证

- 11 项安装与正文对比测试通过，包含受管替换的 4 种情形。红绿日志在 scratch/release-026-red.log 和 scratch/release-026-targeted.log。
- Build 26 实机列表与替换确认入口正常；查看更新操作再次导致 CUA native pipe closed，重连失败。进程采样显示主线程正常等待事件，无本次新崩溃报告。不能将其计作正文视觉及关闭返回验收通过。
- 完整发行脚本退出 0：Swift 报告 632 项，其中 1 项既有公网跳过，631 项实际通过；Python 28 项通过。生产构建、严格签名、隐私、框架、图标与 DMG 校验通过。公开 ZIP 使用应用内公钥独立验证 Ed25519 签名通过。
- GitHub 草稿 5 个附件下载与本地逐字节一致；公开后匿名下载也全部一致。部分 TLS 连接中断后重试完成，最终签名更新源 HTTP 200 且与发行清单一致。

## 已发布与本机结果

- [v0.2.6](https://github.com/AidenXu-1/SkillBox/releases/tag/v0.2.6) 已公开且为 Latest，Release ID `392213946`。标签固定产品提交 `c6927e457bc845bc4d3c96b93ec797ce6d553473`，来源清单 `gitClean=true`；更新清单提交 `5d42383`，后续验收文档不移动标签。
- 本机原位置更新为 0.2.6 Build 27，严格签名及程序与发行包一致。因 CUA 详情读取断线，本轮使用原位置包替换，未记作真实 Sparkle 下载重启验收。
- 新版真实启动显示原有 5 份 Skill；168 个资料文件在替换期间完全不变，启动后无新增缺失，仅 library-state.json、catalog.json、local-source-state.json 三个检查记录更新。没有复制完整用户资料；本次临时旧 App 已清理。
- 新版原生关于页显示 0.2.6（27），实际点击“检查更新”后显示“已是最新版本”。截图及回执在 `scratch/release-026/`。详情窗口最终视觉和关闭返回未验证的限制不变。

## 发行校验值

- 程序：`b873d0222bde6c0b610e018edbaa31398d1472378c2d307cc169750bc7ff741d`
- DMG：`fcdd6726bdd3403eb9ac3232845467c7b9ed200bf59a9f08078209a154f53896`
- 更新 ZIP：`e4be9d3e1c5b51f1216a0c171ee51bcf7975cb7985129818bb5d4ced471da644`
- 更新源：`a0753f849afd56fb5d3b5963415cb457dc246813976b805742ddebe587177b39`

## 发行边界

沿用已确认的 ad-hoc 签名、未公证 GitHub 发行方式。另一台 Mac / 干净账户首次安装未实测；既有异常恢复真实窗口缺口仍保留。更新仅替换应用包，不新增完整用户资料备份。
