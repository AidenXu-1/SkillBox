# v0.2.3 发布候选

日期：2026-09-08。用户要求推进最新版本上线。候选版本 0.2.3 Build 7，基于已完成本机真实验收的 v2.54 产品实现；本次不改变 Skill 内容筛选、来源跟踪或安装保护规则。

## 发布内容

- 取消启动时对应用安装目录中未知 Skill 的递归发现；单项安装和卸载只核对当前目标，确认窗显示处理状态与错误。
- 优化文件夹与 Skill 的拖动、跨组移动和排序；文件夹置顶，未分类 Skill 位于所有文件夹之后。
- 加强文件夹视觉层级，归类采用收纳盒图标；Skill 左侧显示本地来源或 GitHub 标识。
- 修复发现对话的新查找识别、暂停后的补充消息处理，以及撤回离线要求时的同义条件残留。

## 验证与边界

- 功能验证沿用同一源码的安装、对话、整理和图标原生验收记录；发布候选额外执行完整测试、构建、签名、隐私和 DMG 校验。
- 对方“打开即提示扫描范围过大”的具体触发条件尚未复现。用户允许复现不了时保留此事实继续；本版本不承诺该个案已彻底消失。
- 另一台 Mac 或干净账户的首次安装、手动放行和核心操作尚未获得本代证据。现有安装环境及模拟环境不替代此项，准备完成后须按当前发布规则处理该缺口。
- 公开发布后必须重新下载三个附件，核对 DMG、来源清单、校验文件与冻结候选一致。
- 回退：保留 v0.2.2 及旧附件，不覆盖已有标签；若发布后出现问题，停止推荐新版并恢复旧版下载入口。程序回退与用户资料恢复分开处理，不回写旧资料覆盖新操作。

## 候选检查完成

- 冻结源码 `e96f1fd2f667173c5b8333715c106910bea422e6`，生成时 Git 清洁。此后的验证文档提交不修改候选源码或移动其身份。
- 正式 `release-distribution.sh` 完整执行退出 0：534 项实际通过，1 项原有公网用例跳过；Release 构建、严格签名、隐私检查、图标集成、DMG 完整性和隔离属性模拟完成。
- DMG SHA-256：`f92bf9471e8eb7fc3b4e8d8a21c3c211eab11d84daf627915e9aa6547857a358`。
- 程序 SHA-256：`7dca9af650de919a1407a0f231d5316fed5c3656511ae3877c77765a89fbb6a9`。
- DMG 只读挂载核对版本 0.2.3 Build 7、程序摘要、签名和 Applications 链接通过，随后正常卸载。签名沿用既定 ad-hoc 路线，系统仍需用户手动放行；隔离属性模拟不能记为干净账户首次安装通过。
- 本机正式 App 已先备份再更新，748 个资料文件备份核对一致。真实窗口正常加载 7 份 Skill、两个归类文件夹和来源图标，安装矩阵可打开，点击刷新后显示“安装状态已刷新”。本次没有操作真实 Skill 的安装和卸载，也未调用付费模型。
- 更新后安装选择、安装记录、事务和 Library 内容字节一致；分类与排序按身份比较一致。备份 `~/Library/Application Support/SkillBoxBackups/release-023-20260908-130236/`。
- 新增 10 个待上传提交的文本隐私扫描无命中；实验、资料备份与验收原始输出均位于被忽略目录。

## 当前未公开发布

公开最新版本仍为 v0.2.2。本地附件位于 `app/.build/distribution/`，发布说明与验证原始证据位于 `scratch/github-release-023/`。尚未推送、创建标签或上传 GitHub Release。

唯一需要发布范围决定的缺口是另一台 Mac／干净账户首次安装。按 Spec 第 10 节保留此项，等待用户明确允许披露该未验证项后先发布，或补齐实测。之前对无法复现报错的容许，不冒充该首次安装场景已通过。
